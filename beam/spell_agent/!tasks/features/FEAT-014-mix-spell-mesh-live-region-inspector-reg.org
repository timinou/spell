#+TITLE: mix spell.mesh — live region inspector (Regions/Board/Spawn panes) over the freeform surface
#+STATE: ITEM
#+SESSION_ID: 151269c91db37358
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-spell-beam-spell_agent/2026-06-21T23-09-03-478Z_151269c91db37358.jsonl]]
#+CUSTOM_ID: FEAT-014-mix-spell-mesh-live-region-inspector-reg
#+BLOCKED_BY: FEAT-009

* Initial Message

what are distributed agents in a mesh network, really? please look into network theory, not hype. i want us to think about that deeply and from first principles, in order to let the BEAM lisp runtime call itself and create meshed sub-sessions. But I want strong theoretical backing beforehand

* Goal
The flagship mesh screen: watch regions fill with findings/claims/verdicts in real
time, the spawn tree, and pending watches — the mesh analog of mix spell.tui. Built
entirely on the EXISTING TUI surface (Pane project/2 + view/1, DefaultLayout tree),
and the layout is authorable as a PTC view/ tree (the homoiconic bonus).

Read first: docs/agent-mesh-theory/07-screens.org §0-§1 (how the surface works +
the screen spec + the PTC layout tree), 08-implementation.org §C (Screens),
03-executive-function.org (the claim-fold pane renders basal-ganglia gating).

* Wave / deps
Wave 4 (screens). BLOCKED BY FEAT-009 (needs Mesh.Store.region/2 read API). Reads
only — independent of FEAT-011/012/013 (it visualizes whatever state exists).

* Modules / edits
** lib/spell_agent/tui/panes/regions.ex  (NEW — SpellAgent.Tui.Panes.Regions)
- use SpellAgent.Tui.Pane. project/2 folds the mesh forest -> one row per region:
  %{region, goal_excerpt, n_findings, n_claims, sealed?, last_lamport}. view/1
  renders a view/list; cursor selects the region the Board pane reads. events/0
  wakes on [[:mesh, :post], [:mesh, :decide]] (the dirty filter, pane.ex).

** lib/spell_agent/tui/panes/board.ex  (NEW — SpellAgent.Tui.Panes.Board)
- project/2 folds the SELECTED region's records grouped by kind. view/1 renders a
  section per kind: goal, findings (table: about|author|lamport|excerpt), claims
  (the FOLD outcome — winner row highlighted, losers dimmed: basal-ganglia gating
  made visible, 03), verdict (with its sealed :watermark, DC-8).

** lib/spell_agent/tui/panes/spawn.ex  (NEW — SpellAgent.Tui.Panes.Spawn)
- project/2 folds spawn edges (parent_session -> child) into a tree — REUSE the
  SpanTree indentation projection (span_tree.ex) since the spawn tree is a span-tree
  shape — plus a pending-watches list (:intention records with fuel/ttl remaining).
  view/1 renders the tree + a view/list of watches.

** lib/spell_agent/tui/default_layout.ex  (EDIT — register mesh panes)
- add pane_module("regions")/("board")/("spawn") clauses (mirror the existing
  history/tree/detail clauses at pane_module/1). The mesh screen's tree uses the
  SAME frame spine (status/body/composer) as DefaultLayout.tree/2.

** lib/spell_agent/tui/view.ex  (EDIT — add the view/pane builder)
- add "view/pane" => &build_pane/1 (a thin tagger {"type"=>"pane", "slot"=>...,
  "pane"=>..., "focusable"=>true}) beside view/split (build_split/1). This is the
  ONE new builder the PTC layout tree in 07 §1 needs.

** lib/mix/tasks/spell.mesh.ex  (NEW — Mix.Tasks.Spell.Mesh)
- clone lib/mix/tasks/spell.tui.ex: a dedicated task (not iex — BUG-489) that
  mounts the mesh layout tree and runs the inspector loop against the mesh forest.
  @requirements ["app.start"]; restores the terminal on exit.

* CodePaths (existing) to mirror / reference
- lib/spell_agent/tui/pane.ex — the project/2 + view/1 + events/0 contract.
- lib/spell_agent/tui/panes/span_tree.ex — the tree-projection to reuse for Spawn.
- lib/spell_agent/tui/default_layout.ex::tree/2 + pane_module/1 — the frame + dispatch.
- lib/spell_agent/tui/view.ex::tools/0, build_split/1 — where view/pane is added.
- lib/mix/tasks/spell.tui.ex — the task template.
- lib/spell_agent/mesh/store.ex (FEAT-009) region/2, by_kind, claims_for — the data.

* Edge cases
- empty mesh (no regions): Regions renders a zero-row list (no crash) — the empty
  scene contract, like Scenes.empty in scenes.ex.
- a huge region (PT-8): Board should window/truncate (reuse PREVIEW_LIMITS /
  ui.truncate sanitization per AGENTS.md TUI rules) — do not render thousands of rows.
- all displayed text passes replaceTabs/truncateToWidth + shortenPath for any
  file paths in findings (AGENTS.md sanitization rules).

* Acceptance (observable)
- `mix spell.mesh` launches, renders the three panes, navigates with the
  spell.tui chord set (j/k, l/h, tab, esc), restores the terminal on quit.
- driven against a fixture region forest (FEAT-015 scenes), the claim-contention
  state shows one highlighted winner + dimmed losers.
- the PTC layout tree in 07 §1 (view/split + view/pane) materializes to the same
  screen (the homoiconic-authorability proof).
- `mix check` clean.

* Tests
- each pane's project/2 folds a fixture forest to the expected view-model shape
  (contract-level, like the span-tree projection tests).
- view/pane builder produces the correct node map.
- the empty-forest render (no crash, zero rows).
