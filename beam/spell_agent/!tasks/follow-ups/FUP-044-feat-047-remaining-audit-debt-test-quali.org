#+TITLE: FEAT-047 remaining audit debt: test-quality gaps + presentation-in-Elixir
#+STATE: ITEM
#+SESSION_ID: 1523e49fcb29d4e2
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-spell-beam-spell_agent/2026-07-05T12-59-55-564Z_1523e49fcb29d4e2.jsonl]]
#+CUSTOM_ID: FUP-044-feat-047-remaining-audit-debt-test-quali

* Initial Message

did you file a BUG about that?

* Origin
Two findings from the FEAT-047 chakra audit (see FUP-043) that were NOT fixed in the BUG-036..043 hardening pass \u2014 genuinely deferred, tracked separately so they aren't lost.

* 1. Sahasrara/Crown \u2014 test-quality gaps
- `help_overlay_test.exs` opens the overlay by setting `ui.flags["help"]` directly, never by pressing the real `?`/`C-g` key through `App.handle_event/2`. A regression that unbinds those keys would leave every test green while the feature is unreachable (the exact BUG-035 failure mode).
- `palette_app_test.exs`'s cockpit-fire test asserts only that the palette closed after Enter; since `Global.react(:\"app/cockpit\")` is identity and `fire_palette_selection/2` closes before dispatching, a SKIPPED App-intercept path would still pass this test. Needs an assertion on an OBSERVABLE cockpit/layout effect, not just palette-closed.
- Several never-brick claims (C-r-when-reset-itself-raises, the runtime-context-atom fire path, `safe_intent_atom` miss) are asserted in commit messages/moduledocs but not forced by a test that triggers the real failure.

Recommended fix shape: add `App.handle_event/2` tests that (a) press real `?`/`C-g` and assert the overlay's presence changes; (b) assert a real cockpit-layout side effect after an Enter-fired `app/cockpit`; (c) monkeypatch/force `LayoutRegistry.reset/1` to raise and assert the palette still closes via `reset_fallback/1`.

* 2. Vishuddha \u2014 presentation still welded into Elixir
Help/palette overlay copy, border colors, and glyphs are literals in `app.ex` (`help_overlay_placements/2`, `palette_overlay_placements/3`, `help_lines/1`, `palette_lines/3`). `default_layout.ptc` already proved the doctrine-correct pattern (presentation-as-data, PLAN-027 M3): Elixir derives raw scalars, `.ptc` authors the words/colors/arrangement, with a compiled `@fallback_presentation` floor.

Recommended fix shape: extract the overlay copy (title strings, border colors per state, item-line formatting) into a `.ptc` presentation hole mirroring `default_layout_presentation`, with the current Elixir literals becoming the compiled floor. Correctness-neutral \u2014 no urgency, do NOT bundle with a correctness fix; a good candidate for a quiet, low-risk single-session task.

* Non-goals
Do not touch the palette/reflection CENTER (state.palette, KeymapIntrospect's union logic, apply_intent's shared dispatch) \u2014 those are correct and were the subject of the BUG-036..043 hardening, already landed.
