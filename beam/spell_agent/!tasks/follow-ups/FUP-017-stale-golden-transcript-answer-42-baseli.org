#+TITLE: Stale golden transcript: answer_42 baseline 54 commits behind the live render
#+STATE: DONE
#+SESSION_ID: 15131f74dcf291df
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-spell-beam-spell_agent/2026-06-22T12-22-47-411Z_15131f74dcf291df.jsonl]]
#+CUSTOM_ID: FUP-017-stale-golden-transcript-answer-42-baseli

* Initial Message

which dissolution will have more impact now?

* What
`test/spell_agent/tui/llm_terminal_test.exs` "Design A — visual regression (golden
transcript)" fails: the committed baseline
`test/snapshots/llm/answer_42.transcript` no longer matches the live inspector
render.

* Why (root cause — verified, NOT a PLAN-016 regression)
The baseline was last written at commit `7d1de20d2` (FEAT-007). Between it and now,
54 commits changed the rendered screen, including:
- PLAN-012 W4/W5 (`65ca9a5e2`, `aefc5fcf2`, `aef8d7f2f`): status/composer rendered
  from tmpl:: holes over data/* — the status line text changed.
- PLAN-009 D1 (`f904f753e`): the history pane (the golden has no `history` pane; the
  live render does).
- BUG-012 / cells drawer (`c9baa84e2`): the footer gained `C-r reset layout`.
- clock/* wiring (`3e84605a9`) + others: the cassette run now takes a 2nd turn that
  errors (`✗ run`, `turn 2`, `✗ llm`) where the baseline shows a clean 1-turn ok.

The PLAN-016 self-view work is provably uninvolved: the transcript test references
none of its symbols (`view/think`, `SelfView`, `Budget`), and SelfView/Budget only
add a tool + a supervised GenServer — neither touches the inspector's golden screen.
`grep view/think|Budget|SelfView|build_tools` over the test + harness = 0 matches.

* The real question this defers
The divergence includes a BEHAVIORAL change (the cassette run now takes a 2nd,
erroring turn) — that is NOT obviously cosmetic. Before regenerating the baseline,
someone must decide whether the 2nd-turn error is INTENDED (a loop/cassette change)
or a latent bug the stale snapshot was masking. Do NOT blind-regenerate.

* Fix (when picked up)
1. Investigate the `turn 2 / ✗ llm` divergence: is the answer_42 cassette now driving
   a second turn that errors? If unintended, fix the loop/cassette; if intended,
   proceed.
2. Once the run behavior is correct, regenerate: delete
   `test/snapshots/llm/answer_42.transcript`, re-run the test (it rewrites the
   baseline), `git diff` to review, commit.

* Pre-existing
This failure exists independent of PLAN-016 and was surfaced (not caused) by running
the full suite during PLAN-016 W4. Filed so it is not lost.

NOTE [2026-06-27]: [2026-06-27] MESH milestones (PLAN-019) added always-present session tools (spawn-session, await-session, mesh/* combinators) to Tools.inventory + the system prompt via Session.run/build_session_tools. This SHIFTS the answer_42 render (the tool-list block + token counts), compounding the pre-existing 54-commit staleness. The golden was ALREADY failing before this session (tracked here since commit 5465b476); the MESH work neither caused nor fixed it. When regenerating per this FUP, the new baseline will include the mesh tool surface — still verify the 2nd-turn behaviour is intended (not a latent loop bug) before accepting the rewrite. The ToolsTest list-tools count assertion was the SIBLING brittle-count test; it was fixed in M-final to a contract-level assertion (count >= 8 + name membership) rather than a magic total, so it survives inventory growth. Consider the same robustness posture when regenerating the golden.

NOTE [2026-06-28]: DONE [2026-06-27] — golden regenerated, behavioral concern cleared. Procedure followed from this FUP's Fix steps:

1. The 2nd-turn-error worry is RESOLVED, not masked: Design C (loop-correctness) passes independently — out.result = {:ok, 42}, the single run span status == :ok, the llm span nested under it, kinds = [:llm, :run]. The cassette drives ONE clean turn; there is no `turn 2 / ✗ llm` in the live render. So step 1 (investigate the divergence) clears: the run behaviour is correct.

2. Regenerated per step 2: rm test/snapshots/llm/answer_42.transcript; re-ran the test (rewrote the baseline); reviewed git diff. The diff is 25/25 lines, PURELY cosmetic chrome that accumulated over the 54 commits:
   - + history pane (PLAN-009 D1) — the stale golden predated it.
   - + `C-r reset layout` in the footer (BUG-012 cells drawer).
   - header unchanged (turns 1 · tools 0), run still ✓ ok.
   NB: the mesh tool surface (this session's PLAN-019 work) does NOT appear in this screen — the inspector's answer_42 view renders the span tree + history + run detail panes, not the tool-list block, so the mesh inventory growth did not touch this golden after all (the earlier note's compounding-staleness prediction was conservative; the actual render is tool-list-free).

Both tests in llm_terminal_test.exs now pass. Committed with the test snapshot.