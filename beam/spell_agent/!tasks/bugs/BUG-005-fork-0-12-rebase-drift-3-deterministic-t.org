#+TITLE: Fork 0.12 rebase drift: 3 deterministic test failures slipped PLAN-007 verification
#+STATE: DONE
#+SESSION_ID: 150fca134364ee8b
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-spell-beam-spell_agent/2026-06-19T22-14-18-637Z_150fca134364ee8b.jsonl]]
#+CUSTOM_ID: BUG-005-fork-0-12-rebase-drift-3-deterministic-t
#+COMPLETION_HASH: 46251913097f5a32

* Initial Message

kk give me your plan for PLAN-008 and any other payoff that you see along the way

* Discovery
While running the FULL fork suite (`mix test --no-deps-check`) as PLAN-008's
FEAT-001/002 gate, found 16 failures. Triage proved they are PRE-EXISTING
0.12-rebase drift, NOT caused by the PLAN-008 fork prereqs:
- My 3 commits (b3fd78d4 handle, 7e885c8a turn, c355cb09 docs) touch only
  turn.ex/step.ex/handle.ex/loop.ex/metrics.ex/ptc_tool_call.ex/runner.ex.
- The failing test files (lisp_test.exs, schema_test.exs, upstream_*) are
  UNMODIFIED by my commits.
- Root cause they slipped: PLAN-007 verified only `test/ptc_runner/lisp/` (the
  subdir), so top-level `lisp_test.exs` + `schema_test.exs` + upstream HTTP
  fixtures were never run after the 0.12 rebase + spell patch re-application.

* The 16 failures split into two classes
** Class A — 3 DETERMINISTIC (real, fix here)
1. lisp_test.exs:260 "handles tool execution failures" — DOUBLE-WRAPPED tool
   error. BUG-462 (PLAN-007) changed the :tool_error ExecutionError `message`
   from the bare tool name to "tool 'NAME' failed: REASON" (lisp.ex:1656), but
   `format_execution_error/1` (eval.ex:1608-1611) still treats `message` as the
   NAME and wraps AGAIN -> "Tool 'tool 'kaboom' failed: boom' failed: ...". The
   BUG-462 patch missed this second formatter. REAL SHIPPED BUG (user-visible
   doubled error text), not a stale test.
2. lisp_test.exs:321 "undefined variable returns error" — expects
   {:error, ["foo"]} but PATCH-2 (preflight hints) now returns
   {:error, ["Undefined variable: foo. Did you mean: floor"]}. The test was not
   updated when PATCH-2 landed. STALE TEST EXPECTATION (PATCH-2 behaviour is
   intended) — update the assertion to the hint contract.
3. schema_test.exs:480 "schema file synchronized with generated schema" —
   generated `to_json_schema/0` output != priv/ptc_schema.json on disk. The
   0.12 rebase / spell patches changed the schema surface but the committed
   priv file was not regenerated (`mix schema.gen`). MECHANICAL: regenerate +
   verify the diff is only legitimate 0.12/patch surface.

** Class B — ~13 FLAKY under parallel load (HTTP fixtures)
upstream_roundtrip_test.exs / upstream_runtime_test.exs /
upstream/eval_run_subagent_test.exs — all use start_http_fixture + assert_receive
with a 1_000ms timeout. They PASS in isolation (upstream_roundtrip 3/3) but time
out when 16 async cases contend for the scheduler. Pre-existing flakiness, not a
correctness bug. Options: bump the assert_receive timeout, or tag :flaky/
async: false. Lower priority — does not gate correctness.

* Fix plan
- Class A #1: fix `format_execution_error/1` (eval.ex:1610) to NOT re-prefix when
  `message` already carries the actionable string. Either (a) change the
  :tool_error formatter to emit just the message+data without the "Tool 'NAME'
  failed:" prefix (message already says it), or (b) revert message to the bare
  name and keep the prefix in the formatter only. Prefer (a): keep the rich
  message at the raise site (BUG-462 intent: `(catch e ...)` sees WHY), make the
  formatter render it once. Update the test to the single-wrapped expectation.
- Class A #2: update lisp_test.exs:321 to assert the PATCH-2 hint shape
  (the bare-["foo"] contract is obsolete).
- Class A #3: run `mix schema.gen`, commit the regenerated priv/ptc_schema.json,
  confirm the diff is only 0.12/patch-legitimate.
- Class B: bump assert_receive timeout (e.g. 1_000 -> 5_000) OR mark the HTTP
  fixture tests async: false. Decide in a follow-up if time-boxed.

* Acceptance
- `cd beam/ptc_runner && mix test --no-deps-check` green (Class A fixed; Class B
  either fixed or quarantined and documented).
- No regression in the lisp/ subdir suite (was 3400+ green).
- Commits neat + attributed; FORK_CHANGES.md notes the post-rebase fixups.

* Note
This is fork-internal and adjacent to PLAN-008 (found by its stricter gate). It
does NOT block the Hist cutover seams, but a clean full fork suite is the right
bar before bumping the submodule. Fixing Class A here; Class B may defer.

NOTE [2026-06-20]: FIXED. All 3 Class A deterministic failures resolved on fork branch spell, commit 6f733c3a:
1. Tool-error double-wrap: added :tool_name field to ExecutionError; lisp.ex rescue uses it for the {:tool_error, NAME, reason} formatter slot so the rich BUG-462 message (still on .message for (catch e ...)) renders exactly once. 169 tool-error/catch tests green (BUG-462 contract preserved).
2. validate undefined-var test updated to the PATCH-2 hint contract (msg =~ "Undefined variable" / "foo").
3. priv/ptc_schema.json regenerated via mix schema.gen (pure key-order drift).

Class B (~13 HTTP-fixture flaky-under-load tests): turned out green in the full-suite run too — were scheduler-contention flakes, not bugs. Full fork suite now 6595 passed, 0 failures (PLAN-007 had only verified the test/ptc_runner/lisp/ subdir at 3400, which is why these top-level failures slipped). Pushed; submodule bumped to 69624b27. FORK_CHANGES.md ledger + verification count updated.