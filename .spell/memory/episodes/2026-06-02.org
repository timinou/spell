#+TITLE: Episodes 2026-06-02

** ITEM Review Gate 1 (PtcRuntime seam) outcome. Validated safe: cross-execute tool_call correlation (single GenServer serializes next_id, replies pinned to stored `from` by unique id); respondResult JSON.stringify throw IS caught (inside handleInboundRequest try); no orphaned beam.smp (mix/elixir/erl exec into beam.smp as single PID, SIGTERM reaches it). FIXED NOW: P1 non-encodable tool_call ARGS crash Peer — symmetric to P0 return-value bug; made write/2 non-raising (Jason.encode_to_iodata + error frame). P2 re-init handshake — Peer supervisor-restart loses init state, BEAM OS-proc survives so Node onClose never fires; FIX: client retains lastCatalog, replays init on -32001 then retries execute once. Regression tested (17 Elixir, 43 TS). FILED to org: PLAN-323 (concurrent-execute heap ceiling + task transitive-recursion depth guard), PLAN-324 (abort in-flight tool_calls on close + per-execute signal correlation + whole-BEAM respawn — do when P3 wires abort). PLAN-322 (stdin backpressure). Pattern: each review gate finds the SYMMETRIC twin of prior-gate bugs — guard ALL encode paths, not just the one that bit.
:PROPERTIES:
:CUSTOM_ID: EP-76d9ba3ef7f7d671
:KIND: episode
:CONFIDENCE: 0.6
:CREATED: 2026-06-02T00:03:57Z
:END:
:RELATIONS:
:END:
** ITEM Review Gate 2 (PtcRuntime catalog) outcome. P1 FIXED: optional-array fields emitted [:t]? which ptc_runner's signature grammar REJECTS (verified empirically: PARSE-FAIL). The :list_type combinator has no optional form (only primitives + :map keyword take ?). FIX: ptcParam() emits :any? for optional lists. Added catalog-signatures.integration.test.ts that validates EVERY generated signature against the real BEAM parser (would have caught this). P1 FIXED: memory tool under-tagged read but note/save/link WRITE the store → security under-tag (read-only policy would allow writes). FIX: memory→write (max-effect convention, matches org). P2 FIXED: EFFECT_ORDER ladder contradicted effectAllowed set; a ladder can't express {pure,read,write} (allow write deny exec) since the default V1 policy needs set semantics. FIX: removed ladder, renamed ALL_EFFECTS, documented flat-SET contract for P3 gate. Lesson: signatures/grammars must be validated against the REAL parser, not assumed — and effect taxonomies are flat sets not ladders (privilege isn't linearly ordered: exec and network are orthogonal, not 'more than' write). 64 tests green.
:PROPERTIES:
:CUSTOM_ID: EP-77683d9105509092
:KIND: episode
:CONFIDENCE: 0.6
:CREATED: 2026-06-02T00:16:16Z
:END:
:RELATIONS:
:END:
** ITEM Review Gate 3 (PtcRuntime execute tool + policy) outcome. Reviewer verdict: NO active policy bypass — bash/task(exec)/fetch/web_search(network) correctly denied; name/case/whitespace confusion all resolve to deny-by-default exec. All 4 execute.md prompt examples EMPIRICALLY RUN correctly under real ptc_runner 0.11.0 (reviewer ran them). FIXED: P2 structural denylist — added approvals/checkpoint/rewind/cancel_job/await/goals/canvas/canvas_cast to DEFAULT_DENYLIST (were safe only via exec-default accident; now structural, independent of effect tag — prevents future maintainer from tagging one read/write and opening escalation e.g. program self-granting via approvals or rewriting history via rewind). P3 defense-in-depth was illusory (lookup pre-filtered on same permitted set as enforcePolicy → re-check dead) — FIXED: execute.ts lookup now resolves unconditionally, enforcePolicy is the genuine independent gate. P3 result flood — FIXED: renderValue caps at 16KB MAX_RESULT_BYTES with truncation marker (PLAN-325 for future paged/artifact return). 81 tests green. Lesson: 'not program-callable' must be a STRUCTURAL guarantee (denylist), never an emergent property of a default — defaults drift.
:PROPERTIES:
:CUSTOM_ID: EP-f1473456f4ec288f
:KIND: episode
:CONFIDENCE: 0.6
:CREATED: 2026-06-02T00:42:40Z
:END:
:RELATIONS:
:END:
