#+TITLE: Episodes 2026-06-01

** ITEM Surfaced + fixed 8 pre-existing tool-test failures (independent of the body-scope edit fix), via 4 parallel subagents + direct review. Findings by subsystem: (1) bash-skill-urls expandInternalUrls x3 — STALE tests: artifact:// became kernel-owned (BUG-396), no longer JS-routed; rewrote to pass-through + re-pointed router tests to still-JS-routed plan://. (2) TOOL_TIERS core-tier — STALE test: lsp intentionally demoted core→specialized (PLAN-318 W6); moved lsp to specialized list. (3) org suboutline-add x2 — IMPL GAP: OrgTool TypeBox schema in packages/coding-agent/src/tools/org.ts lacked parentId/slug/depends + suboutline-add command + suboutline_id in formatOrgResult; backend @spell/pi-org supported it. Also fixed test dir tasks/→!tasks/ and corrected assertions to text format (formatOrgResult emits human-readable text, NOT JSON; original JSON.parse was wrong). (4) anthropic web-search OAuth — REAL IMPL BUG in packages/ai/src/providers/anthropic.ts buildAnthropicHeaders: condition was `oauth && baseUrl && !isAnthropicApiBaseUrl` (O&&N) which sent OAuth tokens to api.anthropic.com as X-Api-Key (rejected); fixed to `oauth || !isAnthropicApiBaseUrl` (O||N). Truth table: decisive case OAuth+Anthropic-base must be Bearer. Old comment about localhost-proxy passthrough was dead — localhost uses separate buildBetterCcflareHeaders. (5) autonomy-state module-resolution ERROR — packaging bug: exports map `./tools/*`→`./src/tools/*.ts` only handled .ts files, not dir/index.ts; autonomy-state is dir-only so added explicit `./tools/autonomy-state` export entry. CAUTION: image-generation/puppeteer/spell also have dirs but image-generation has BOTH image-generation.ts (the real entry w/ saveImageAsArtifact) AND image-generation/ dir — explicit dir-index entry would SHADOW the glob and break it; keep glob for those. NB: subagent fabricated commit hashes (377ffe909/05c7e73f7) not in file history — verified fix from first principles instead, never trust subagent narrative without repo verification. NB2: anthropic-alignment TLS test 'serverName api.anthropic.com' fails only due to ANTHROPIC_BASE_URL=localhost leaking from session shell env — env pollution, not code; passes with `env -u ANTHROPIC_BASE_URL`.
:PROPERTIES:
:CUSTOM_ID: EP-4c718b321b3abe9f
:KIND: episode
:CONFIDENCE: 0.6
:CREATED: 2026-06-01T13:37:25Z
:END:
:RELATIONS:
:END:
