function outer() {
  if (condition) {
    const x = 1;
    const y = 2;
    return x + y;
  }
}

const items = [1, 2, 3, 4, 5];

function add(a: number, b: number): number {
  return a + b;
}

class Foo {
  bar() { return 1; }
  baz() { return 2; }
}
