{
  "type": "module",
  "name": "@oh-my-pi/pi-qml",
  "version": "0.1.0",
  "description": "Qt 6 QML desktop window bridge for spell agents",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build:bridge": "bun scripts/build-bridge.ts",
    "dev:bridge": "bun scripts/build-bridge.ts --dev",
    "check": "biome check . && tsgo -p tsconfig.json"
  },
  "dependencies": {
    "@oh-my-pi/pi-utils": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "^1.3"
  },
  "files": ["src", "native", "scripts", "bridge", "CMakeLists.txt", "README.md", "CHANGELOG.md"]
}
