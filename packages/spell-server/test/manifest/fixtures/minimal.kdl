name "spell-minimal"
version "1.0"

setup "readonly-reviewer" {
	domain "coding"
}

goal "nightly-tests" {
	setup "readonly-reviewer"
	schedule type="cron" expression="0 2 * * *"
	prompt "Run the full test suite."
}
