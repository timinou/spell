name "spell-webhook"
version "1.1"

setup "executor" {
	domain "coding"
}

goal "incoming-webhook" {
	setup "executor"
	schedule type="webhook" path="/incoming" auth="bearer"
	prompt "Handle incoming webhooks."
}
