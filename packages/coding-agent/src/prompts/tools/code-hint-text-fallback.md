- Managed lifecycle commands still work: `code read`, `code diff`, `code edit { operations: [{ targetId, actions }] }`, `code undo`, `code redo`, `code save`.
- Semantic-only commands such as `code outline` and `code navigate` are unavailable until the file has semantic support.