#+TITLE: PLAN-315 deferred — W5 cutover, W6 TUI, W8 N=10 perf harness
#+STATE: ITEM
#+SESSION_ID: 14ea0d7add095343
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-spell/2026-05-21T14-46-31-540Z_14ea0d7add095343.jsonl]]
#+CUSTOM_ID: FUP-089-plan-315-deferred-code-graph-lane-w3-pus
#+LAYER: natives coding-agent tui
#+PLAN: PLAN-315
#+PRIORITY: #B

* Scope

Updated 2026-05-22 after PLAN-315 plan-315-complete branch session.

PLAN-315 critical path + W3 + W4 + all four T10 memory-loop fixes have
all shipped end-to-end. The remaining items are non-blocking
extensions and a deferred verification harness.

* Deferred items

** W5 cutover — delete in-process WarmEngine

The RPC fast-path is live and exercised by `rpc_dispatch_parity` and
`subscribe_client_e2e`. The in-process `WarmEngine` remains as a
fallback. Deleting it requires:
- removing ~800 LOC across `recall_engine.rs`, `code_graph.rs`
- rewriting ~12 unit tests that mock the embedder at the WarmEngine layer
- introducing `PI_KNOWLEDGE_WORKER=inprocess` env knob for CI / offline

Recommended ordering: own commit after a 1-release bake-in period to
confirm the RPC path is stable under real workloads. Estimated 2-4h.

** W6 — /memory TUI browser

4-tab panel (search / graph / recent / since) with subscribe-driven
live refresh, /memory slash command, ambient Ctrl-M binding.

Independent of W5; the W2+W3+W4 daemon already exposes everything the
TUI needs.

Work involves investigating `packages/coding-agent/src/modes/` panel
architecture (interactive-mode.ts, mode controllers) and a real
keybinding integration. Estimated 6-10h.

** W8 — N=10 perf harness

Multi-process orchestration test that measures the hard gates from
PLAN-315 §perf:
- libpi_natives.so (release) <= 92.5 MB
- per-session RSS <= 100 MB
- total RSS N=10 <= 1.55 GB
- memory.search warm P99 <= 50 ms
- cg_search warm P99 <= 60 ms
- push event delivery <= 500 ms
- cold daemon warm-up (12k symbols) <= 90 s

Estimated 3-4h.

** T10 — all four memory-loop failures resolved this session

Closed:
- T10.1: scope: ['episode'] added to match test name intent
- T10.4: timing budget split into rebuild vs warm-search phases
- T10.5: file-level :RELATIONS: drawer write path via cmd_link
- T10.6: birthtime-based added vs modified classification

Memory-loop runtime validation (full 11/11 green via bun test) is
deferred to a session where the bge-m3 model is available; the
underlying fixes are validated by Rust unit + integration tests
(8 new tests across pi-org-engine, pi-natives, coding-agent).

* Acceptance

When this FUP closes:
- W5 cutover commit landed; in-process WarmEngine deleted
- /memory TUI panel live; manual smoke artifact recorded
- N=10 perf harness commit landed with measured numbers
- All six W*.5 review waves applied (HIGH-confidence findings only)
- AGENTS.md + specs/org-graph-memory.md reflect final architecture

* Branch

Work continues on `plan-315-complete` branch at worktree
`/home/user/code/ora/spell-plan315`. See
`!tasks/plans/plan-artifacts/PLAN-315/STATUS-v2.md` for the current
delivery snapshot.