#+TITLE: PLAN-315 deferred — W5 WarmEngine erase, W8 measurement run, W8 push-bench binary
#+STATE: ITEM
#+SESSION_ID: 14ea0d7add095343
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-spell/2026-05-21T14-46-31-540Z_14ea0d7add095343.jsonl]]
#+CUSTOM_ID: FUP-089-plan-315-deferred-code-graph-lane-w3-pus
#+LAYER: natives coding-agent tui
#+PLAN: PLAN-315
#+PRIORITY: #B

* Scope

Updated 2026-05-22 after the second autonomous PLAN-315 session.

PLAN-315 has now delivered:
- W3 code-graph lane (FEAT-769)
- W4 push-subscribe daemon + client (FEAT-770)
- W5 PI_KNOWLEDGE_WORKER mode + fail-loud RPC (FEAT-771)
- W6 /memory TUI browser with Alt+M panel (FEAT-774)
- W7 file-level :RELATIONS: + :PROPERTIES: drawers (FEAT-773)
- W8 N=10 perf harness scaffolding + BenchPayload event (this session)
- W*.5 review+gap-fix waves on W5, W6, W8

20 commits on `plan-315-complete` branch (`/home/user/code/ora/spell-plan315`).
All package-level tests green: pi-knowledge-worker 52/52, pi-natives 349 lib +
integration tests, pi-knowledge-core 91/91, pi-org-engine 81/81.

See `!tasks/plans/plan-artifacts/PLAN-315/STATUS-v2.md` for the
delivery snapshot.

* Deferred items

** W5 erase — delete in-process WarmEngine (~2-4h)

The fail-loud RPC default is live; the `PI_KNOWLEDGE_WORKER=inprocess`
escape hatch preserves WarmEngine for offline/CI. After a 1-release bake-in
period of the fail-loud default, delete:
- WarmEngine struct + EngineState::Warm variant
- `ensure_warm` chain in recall_engine.rs
- ~12 WarmEngine-mocking unit tests (rewrite around RPC fixtures)
- code_graph.rs in-process semantic index (FUP-089 TODO comment)

Net code reduction: ~800 LOC. Replace with RPC-only path; keep the
`PI_KNOWLEDGE_WORKER=inprocess` env knob meaning "spin up a sidecar
daemon for this session" rather than "use in-process engine".

** W8 measurement run (~30-60 min once release built)

The harness `scripts/perf/plan315-n10.sh` is correctness-fixed (W8.5
gap-fix addressed all 9 HIGH findings: JSON shape, repo_handle threading,
RSS sampler ordering, totals math, cold warm-up sessions, emit_gate N/A
handling, threshold consistency, sustained python).

To produce real numbers:
1. cargo build --release -p pi-natives -p pi-knowledge-worker
2. bash scripts/perf/plan315-n10.sh
3. Appears at !tasks/plans/plan-artifacts/PLAN-315/W8-perf-run-<epoch>.md
4. Append the gate table to W8-perf.md

Five of seven gates are measured directly:
- libpi_natives.so size ≤ 92.5 MB
- per-session RSS attribution ≤ 100 MB
- total RSS N=10 ≤ 1.55 GB
- memory.search P99 ≤ 50 ms
- cold daemon warm-up (12k symbols) ≤ 90 s

Two remain explicitly DEFERRED in both the harness and the methodology
table:

** W8 push-delivery P99 bench binary (~50-80 LOC)

Need a dedicated bench at `crates/pi-knowledge-worker/benches/push_latency.rs`
that calls `publish_bench_event` in a loop (e.g. 50 iterations) and records
emit-to-receive deltas via the subscribe channel. Compute P50/P95/P99.

The existing `bench_payload_delivers_on_subscribed_channel` test in
subscribe.rs only asserts arrival within 1s; not a percentile.

** W8 cg_search corpus generator (~30-60 LOC bash)

Extend `scripts/perf/plan315-n10.sh` to also generate `.rs` files per session
(e.g. 12 files × 400 symbols each = 4800 code symbols total) and run the
`cg_search` workload. The current harness only generates `.org` corpus.

* Acceptance

When this FUP closes:
- W5 erase commit lands; in-process WarmEngine deleted
- W8 release-built harness has been run; numbers in W8-perf.md gate table
- push_latency.rs bench binary lands with P99 numbers
- cg_search gate has real measurement
- PLAN-315 archived

* Branch

Work continues on `plan-315-complete` branch at worktree
`/home/user/code/ora/spell-plan315`. After all items above land, the
branch can be merged back to main and the worktree removed.