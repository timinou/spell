#+TITLE: Consolidate contradictory maxRecursionDepth CHANGELOG entries
#+STATE: DONE
#+SESSION_ID: 14aeaef685347954
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-spell/2026-04-05T12-03-11-508Z_14aeaef685347954.jsonl]]
#+CUSTOM_ID: BUG-167-consolidate-contradictory-maxrecursionde
#+EFFORT: 5m
#+PRIORITY: #C
#+LAYER: docs

NOTE [2026-04-05]: Amended line 52 to remove maxRecursionDepth (net zero change), deleted line 58. Verified: no maxRecursionDepth in Changed section, exactly one DEFAULT_LOOP_DEPTH_LIMIT entry.

* Initial Message

Implement these audit recommendations from org://AUD-041-audit-implementation:

Good — clean removal. Here's the audit summary:

---

## Audit Findings

### 1. Contradictory CHANGELOG entries under `[Unreleased]`

- **What**: Line 52 says `task.maxRecursionDepth` was changed from 2→4. Line 58 (our addition) says it was reduced from 4→2. Both under `[Unreleased]` — the net effect is no change from the last release. A reader will see "increased then decreased" in the same release notes.
- **Where**: `packages/coding-agent/CHANGELOG.md` lines 52 and 58
- **Why**: Confusing release notes. A user reading `[Unreleased]` sees two contradictory entries about the same setting. The pre-existing entry (line 52) should be amended to only mention `DEFAULT_LOOP_DEPTH_LIMIT` (3→4, which is still net-changed), and our entry (line 58) should state the final value relative to the previous release (still 2, i.e., no entry needed for `maxRecursionDepth`).
- **Effort**: `trivial`

That's the only finding introduced by this plan. The five code changes are structurally correct:

- **BUG-164**: Stagger block correctly moved inside `try`; `throw` instead of `return` routes through `catch`/`finally`. The pre-existing missing `pump()` in the `taskIndex === undefined` guard is unchanged and not in scope.
- **BUG-165**: `throw` correctly replaces `return undefined!`. The pre-existing missing batch-scheduler cleanup (the throw at line 722 is outside the inner `try` at line 736, same as the old `return` was) is unchanged — not a regression.
- **FEAT-338**: Clean removal, no remnants.
- **BUG-166**: Correct, no duplicate names.
- **FEAT-339**: Correct aside from the contradiction noted above.

Two entries under ~## [Unreleased]~ in ~packages/coding-agent/CHANGELOG.md~ contradict each other:

- Line 52: ~Changed default task.maxRecursionDepth from 2 to 4, and DEFAULT_LOOP_DEPTH_LIMIT from 3 to 4.~
- Line 58: ~task.maxRecursionDepth default reduced from 4 to 2 (main -> subagent -> sub-subagent max)~

Net effect for ~maxRecursionDepth~ is 2→4→2 = no change from previous release. Net effect for ~DEFAULT_LOOP_DEPTH_LIMIT~ is 3→4 (real change).

* Implementation
** Consolidate entries (packages/coding-agent/CHANGELOG.md)

1. Replace line 52 with:
   ~- Changed default ~DEFAULT_LOOP_DEPTH_LIMIT~ from ~3~ to ~4~.~
   (Remove the ~maxRecursionDepth~ clause since the net change is zero.)

2. Delete line 58 entirely:
   ~- ~task.maxRecursionDepth~ default reduced from 4 to 2 (main -> subagent -> sub-subagent max)~
   (No net change from previous release, so no entry needed.)

* Acceptance
- ~## [Unreleased]~ / ~### Changed~ mentions ~DEFAULT_LOOP_DEPTH_LIMIT~ 3→4 exactly once
- No mention of ~maxRecursionDepth~ in ~### Changed~ section (net zero change)
- No already-released sections modified
