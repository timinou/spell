#+TITLE: .spell/settings.json overlay duplicates project config surface
#+STATE: ITEM
#+SESSION_ID: 14ea0e0f03e26235
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-spell/2026-05-21T14-49-03-247Z_14ea0e0f03e26235.jsonl]]
#+CUSTOM_ID: BUG-384-spell-settings-json-overlay-duplicates-p

* Initial Message

the settings thing doesn't work. I'd like a simple toggle on top for user/project/session. right now, when I do settings, it just shows [user] ... and keeps printing it, fucking up the layout of everything. I also don't see settings persisted when they need to be added to the kdl (please reproduce the error)

investigate.

* Problem

=discovery/builtin.ts:763-785= reads =<cwd>/.spell/settings.json= as a project-level =settings= capability item. This pre-dates the KDL cutover and was never removed.

∴ Three valid project-config files coexist: =spell.kdl=, =.spell/settings.json=, =.spell/agent/config.yml=. Merge order — not user intent — determines the effective value.

* Fix

Drop the JSON overlay branch. Migrate any existing project =settings.json= contents into =<cwd>/spell.kdl= via the one-shot migrator.

* Tracking

Subsumed by =FULL_KDL_SPELL= plan, WAVE 1.2.
