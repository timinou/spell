#+TITLE: Stale YAML test fixtures in settings-manager.test.ts
#+STATE: ITEM
#+SESSION_ID: 14ea0e0f03e26235
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-spell/2026-05-21T14-49-03-247Z_14ea0e0f03e26235.jsonl]]
#+CUSTOM_ID: BUG-385-stale-yaml-test-fixtures-in-settings-man

* Initial Message

the settings thing doesn't work. I'd like a simple toggle on top for user/project/session. right now, when I do settings, it just shows [user] ... and keeps printing it, fucking up the layout of everything. I also don't see settings persisted when they need to be added to the kdl (please reproduce the error)

investigate.

* Problem

=packages/coding-agent/test/settings-manager.test.ts= and =test/autocomplete-max-visible.test.ts= still write =config.yml= via =YAML.stringify= and read settings back, even though =Settings= now loads exclusively from =spell.kdl=.

Result: 4 failing tests baseline (pre-existing on =main=). They drift the green-bar and hide real regressions.

* Fix

Rewrite both files to use =writeKdlSettings()= test helpers. Drop the =YAML= import. Move them into the broader Wave 1 test rewrite.

* Tracking

Subsumed by =FULL_KDL_SPELL= plan, WAVE 1.6.
