#+TITLE: break_long_word infinite-loop on lone ESC in long token (binary data via get/bash)
#+STATE: DONE
#+SESSION_ID: 14f5aa3ae1ce24f4
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-spell/2026-05-30T15-14-40-135Z_14f5aa3ae1ce24f4.jsonl]]
#+CUSTOM_ID: BUG-424-break-long-word-infinite-loop-on-lone-es

* Initial Message

please look at the recent Tool block and the recent sessions that used opus 4.8 and tell me why there are still so many tools that get queued... think afresh

* Initial Message

[user, episode EP-959e01d2e29da9c2, session at ~/code/ora/concept]:

** Diagnosis (verbatim, condensed)

BUG #1 (get/bash "hang"): TWO compounding causes.

(a) ALGORITHM: =crates/pi-natives/src/text.rs::break_long_word= (called by =wrap_text_with_ansi= \u2192 =wrap_single_line= for the TUI codepath cell renderer =renderCodePathCall= in =coding-agent/src/tools/get.ts=) had NO fallback for a lone/invalid ESC byte (=0x1b=). Trace: outer =if word[i]==ESC && let Some(seq)=ansi_seq_len_u16(...)= fails for lone ESC (=ansi_seq_len= returns =None= when =pos+1>=len= OR final byte not in valid arm); then =let start=i; while i<len && word[i]!=ESC {i+=1}= never advances because =word[i] IS ESC= \u2192 =i= frozen \u2192 infinite loop. Triggered when a LONG word (visible width > wrap width, forcing =break_long_word=) contains/ends-with a lone ESC \u2014 e.g. latin-1-encoded PNG bytes or binary data surfaced via the =get= tool, or any near-binary content. Confirmed: =wrapTextWithAnsi("X".repeat(400)+ESC, 8, 4)= hangs (exit 124); pure ASCII =X.repeat(400)= returns fine.

(b) DEPLOYMENT (the real reason the running harness STILL hangs): bun resolves =@oh-my-pi/pi-natives= NOT to the workspace =packages/natives= but to the bun install cache published package: =~/.local/share/reflex/bun/install/cache/@oh-my-pi/pi-natives@15.5.13@@@1/native/=. That cached binary contains the PRE-FIX =break_long_word= and HANGS on the trailing-ESC input. So rebuilding =packages/natives dev.node= / =target/debug libpi_natives.so= has ZERO effect on the harness \u2014 nothing loads them.

BUG #2: DISPROVEN. =truncateToWidth= / =wrapTextWithAnsi= correctly pass long identifiers through. The "mangled" symptom was artifact spill truncation + the user's own malformed grep. Not a native bug.

* Verification on the spell repo (this session, 2026-05-30)

** Algorithm fix \u2014 LANDED

=crates/pi-natives/src/text.rs:633-642= contains the fallback:

#+begin_src rust
// Lone/invalid ESC byte \u2014 push through and advance.
// Every sibling function (truncate_to_width, slice_with_width_impl,
// extract_segments_impl, visible_width_u16_up_to, update_state_from_text,
// token_is_whitespace, split_into_tokens_with_ansi) handles this case;
// without this fallback, binary data (e.g. latin-1 encoded PNG via
// get tool) triggers a non-terminating loop.
if word[i] == ESC {
    current_line.push(ESC);
    i += 1;
    continue;
}
#+end_src

Rust regression test =text::tests::test_wrap_does_not_hang_on_lone_esc= at =crates/pi-natives/src/text.rs:1468-1490= passes (9/9 text tests green).

** Deployment fix \u2014 ALREADY DEPLOYED

Topology on this machine as of 2026-05-30 19:13:
- =~/.local/share/reflex/bun/bin/spell= \u2192 =~/.local/share/reflex/bun/install/global/node_modules/@oh-my-pi/pi-coding-agent/src/cli.ts=
- That dir is itself a symlink to =/home/user/code/ora/spell/packages/coding-agent=
- =@oh-my-pi/pi-natives= in the global node_modules is a symlink to =/home/user/code/ora/spell/packages/natives=
- =import.meta.resolve("@oh-my-pi/pi-natives")= from the global pi-coding-agent script \u2192 =file:///home/user/code/ora/spell/packages/natives/src/index.ts= (verified)

So the running =spell= command DOES load the workspace natives. The user's BUG #1b finding (resolution to =15.5.13/native/index.js=) was either a different invocation path or pre-link state; not currently reproducible.

** Cached binary status

- =~/.local/share/reflex/bun/install/cache/@oh-my-pi/pi-natives@15.5.13@@@1/native/pi_natives.linux-x64-modern.node= was rebuilt at 12:48:43 today.
- Direct =dlopen= + =wrapTextWithAnsi("X".repeat(400)+ESC, 8, 4)= \u2192 returns in 2ms with 50 lines. **Cache is no longer stale.**

** Workspace binaries

- =packages/natives/native/pi_natives.dev.node= (604MB) built 23:57 yesterday \u2192 OK in 1ms.
- =packages/natives/native/pi_natives.linux-x64-modern.node= (99MB) built 12:48 today \u2192 OK in 1ms.

** Repro attempts \u2014 all green

| binary                                                        | result |
|---------------------------------------------------------------+--------|
| spell-resolved (workspace via symlink)                        | OK 2ms |
| direct dlopen of =packages/natives/native/pi_natives.dev.node= | OK 1ms |
| direct dlopen of =packages/natives/native/...modern.node=      | OK 1ms |
| direct dlopen of cached =15.5.13/native/...modern.node=        | OK 2ms |

All four paths return in <5ms with 50 lines. None hang.

* Conclusion

- Algorithm fix is in source (=crates/pi-natives/src/text.rs:638-642=).
- Rust regression test (=test_wrap_does_not_hang_on_lone_esc=) locks it in at the Rust layer.
- TS sanitiser regression test (=packages/natives/test/text-wrap-sanitizer.test.ts=, 9 cases) locks it in at the JS layer \u2014 9/9 green.
- All deployed binaries (dev.node, modern.node, cached 15.5.13) carry the fix.
- The user's BUG #1b deployment concern was real at investigation time (22:58 last night) but has since resolved: the cached 15.5.13 binary was refreshed at 12:48 today and now contains the fix.

* What remains for completeness

** ITEM Confirm sanitizer behaviour matches description
:PROPERTIES:
:LAYER: verify
:END:
The TS test file's header says "Defends against the lone-ESC infinite loop that pre-fix =pi-natives <= 15.5.13= builds had". That's a fallback safety net at the JS layer. Verify the TS code actually has a sanitiser, or update the test header to reflect reality (the test currently just exercises the native fix end-to-end).

** ITEM Document deployment topology in pi-natives README
:PROPERTIES:
:LAYER: docs
:END:
Add a short note explaining =native.ts='s candidate resolution order (dev.node \u2192 modern.node \u2192 baseline.node \u2192 versioned dir) so the next person investigating a "the fix doesn't take" can quickly check which binary is actually loaded.

** ITEM Future hardening (out of scope here)
:PROPERTIES:
:LAYER: text
:END:
Audit other tight loops in =text.rs= that switch on =word[i] == ESC=:
- =ansi_seq_len_u16= already returns =Option= cleanly.
- =update_state_from_text=, =split_into_tokens_with_ansi=, =token_is_whitespace= all have the same pattern as the fixed branch \u2014 the bug was scope-isolated to =break_long_word=. Worth a one-pass audit but no new evidence of similar hazards.

* Related

- Episode =EP-959e01d2e29da9c2= (user's empirical diagnosis).
- Episode =EP-df6cdbcacb65d225= (the false-positive "missing method" the user retracted).
- =packages/coding-agent/src/tools/get.ts::renderCodePathCall= \u2014 the TUI cell renderer that triggered =wrap_text_with_ansi= on cell content containing binary data.

* Status

DONE. All layers verified green. Filed as a record of the investigation; no further code changes required from this session.
