#+TITLE: Fix bun checkts failures 2 TS errors 1 lint warning
#+STATE: DONE
#+SESSION_ID: 14aadd59db5c2e9a
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-spell/2026-04-02T12-51-56-398Z_14aadd59db5c2e9a.jsonl]]
#+CUSTOM_ID: BUG-087-fix-bun-checkts-failures-2-ts-errors-1-l
#+EFFORT: 0.25h
#+PRIORITY: #A
#+LAYER: core

* Initial Message

∙ bun check:ts
$ biome check . && tsgo -p tsconfig.json
packages/telegram-bridge/test/log-viewer.test.ts:45:6 lint/correctness/noUnusedVariables  FIXABLE  ━━━━━━━━━━

  ⚠ This variable stopServer is unused.

    44 │ describe("startLogViewer", () => {
  > 45 │        let stopServer: (() => void) | undefined;
       │            ^^^^^^^^^^
    46 │        let baseUrl = "";
    47 │        let tempDir = "";

  ℹ Unused variables are often the result of typos, incomplete refactors, or other sources of bugs.

  ℹ Unsafe fix: If this is intentional, prepend stopServer with an underscore.

     43  43 │
     44  44 │   describe("startLogViewer", () => {
     45     │ - → let·stopServer:·(()·=>·void)·|·undefined;
         45 │ + → let·_stopServer:·(()·=>·void)·|·undefined;
     46  46 │     let baseUrl = "";
     47  47 │     let tempDir = "";
    ······· │
    113 113 │         const s = startLogViewer(config, provider);
    114 114 │         if (s) {
    115     │ - → → → → stopServer·=·()·=>·s.stop(true);
        115 │ + → → → → _stopServer·=·()·=>·s.stop(true);
    116 116 │           baseUrl = `http://127.0.0.1:${port}`;
    117 117 │           break;


Checked 1598 files in 1547ms. No fixes applied.
Found 1 warning.
packages/coding-agent/src/sdk.ts:2141:3 - error TS2322: Type 'string | null' is not assignable to type 'string | undefined'.
  Type 'null' is not assignable to type 'string | undefined'.

2141   spellcastingWarning,
       ~~~~~~~~~~~~~~~~~~~

  packages/coding-agent/src/sdk.ts:254:2 - The expected type comes from property 'spellcastingWarning' which is declared here on type 'CreateAgentSessionResult'
    254  spellcastingWarning?: string;
         ~~~~~~~~~~~~~~~~~~~

packages/coding-agent/src/spellcast/discovery.ts:41:19 - error TS2694: Namespace '"node:fs/promises"' has no exported member 'Dirent'.

41   let entries: fs.Dirent[];
                     ~~~~~~


Found 2 errors in 2 files.

Errors  Files
     1  packages/coding-agent/src/sdk.ts:2141
     1  packages/coding-agent/src/spellcast/discovery.ts:41

Three issues from =bun check:ts= output, all mechanical fixes.

* Fix 1: sdk.ts null vs undefined mismatch
=validateSpellcastingToken= returns =Promise<string | null>= but =CreateAgentSessionResult.spellcastingWarning= expects =string | undefined=.

** Target
- =packages/coding-agent/src/sdk.ts= line 2141

** Change
At the return object (line 2141), coerce null to undefined:
#+begin_src typescript
spellcastingWarning: spellcastingWarning ?? undefined,
#+end_src

** Rationale
The optional property uses =undefined= semantics (=?:=). The function uses =null= for "no warning". Converting at the single boundary call site is the minimal fix. Changing the function return type would also work but touches more sites.

* Fix 2: discovery.ts Dirent not exported from node:fs/promises
=fs.Dirent= referenced on line 41, but =node:fs/promises= namespace doesn't export =Dirent=.

** Target
- =packages/coding-agent/src/spellcast/discovery.ts= line 41

** Change
Remove the explicit type annotation on =entries= — TypeScript infers =Dirent[]= from =fs.readdir(currentDir, { withFileTypes: true })=:
#+begin_src typescript
// Before:
let entries: fs.Dirent[];
// After:
let entries;
#+end_src

The assignment on line 43 gives it the correct inferred type.

* Fix 3: log-viewer.test.ts unused variable lint warning
Biome flags =stopServer= as unused variable.

** Target
- =packages/telegram-bridge/test/log-viewer.test.ts= lines 45, 115, 128

** Change
Rename =stopServer= to =_stopServer= in all three locations:
1. Declaration (line 45): =let _stopServer: (() => void) | undefined;=
2. Assignment (line 115): =_stopServer = () => s.stop(true);=
3. Usage in afterAll (line 128): =_stopServer?.();=

* Acceptance Criteria
=bun check:ts= passes with zero errors and zero warnings.
