#+TITLE: Fix syntax errors and broken links in spell-server docs
#+STATE: DONE
#+SESSION_ID: 14aff50756f59a40
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-spell/2026-04-06T11-47-47-675Z_14aff50756f59a40.jsonl]]
#+CUSTOM_ID: BUG-186-fix-syntax-errors-and-broken-links-in-sp
#+EFFORT: S
#+PRIORITY: #A
#+LAYER: docs

* Initial Message

Implement these audit recommendations from org://AUD-046-audit-implementation:

So `port` is parsed as a **child node** (line 89: `child.getName() === "port"`), not a property. The KDL schema reference example uses `http port=8787 webhook-secret=...` which is **property syntax** — this won't work because the parser iterates over `httpNode.children.nodes`. However, the KDL schema reference was pre-existing content (it was there before this plan), so it's out of scope for this audit. The getting-started.md correctly uses the child-node syntax in its newly-written examples (lines 66-73).

Actually, wait — the kdl-schema-reference.md at lines 15-19 still shows the pre-existing property syntax (`http port=8787 ...`). But that was pre-existing, not introduced by this plan. The new getting-started.md examples correctly use child-node syntax. The KDL syntax inconsistency between those two is pre-existing.

Let me compile my findings:

---

## Audit Findings

### 1. Wrong `env:` syntax in telegram-bridge.md (should be `env()`)
- **What**: telegram-bridge.md uses `"env:DEEPGRAM_API_KEY"` in KDL examples instead of the correct `"env(DEEPGRAM_API_KEY)"` syntax
- **Where**: `docs/spell-server/telegram-bridge.md` — lines 122, 127, 289, 309, 312
- **Why**: Users copying these examples will get runtime errors — the `resolveEnvString` function only recognizes `env()` syntax. This is the primary interactive use case doc.
- **Effort**: `trivial`

### 2. Broken cross-reference link in telegram-bridge.md
- **What**: Links to `configuration.md` which doesn't exist; also references `spell-server.kdl` which isn't a real file
- **Where**: `docs/spell-server/telegram-bridge.md` — line 9: `See [Configuration Reference](configuration.md)` and `alongside spell-server.kdl`
- **Why**: Broken link in the Overview section. Should link to `kdl-schema-reference.md` and reference `server.kdl`.
- **Effort**: `trivial`

### 3. Wrong `auth="token"` in getting-started.md (should be `auth="bearer"`)
- **What**: Bearer auth example uses `auth="token"` in the schedule block but the manifest parser only accepts `"hmac"` or `"bearer"` — `"token"` throws a parse error
- **Where**: `docs/spell-server/getting-started.md` — lines 310, 328
- **Why**: User who copies this config gets a manifest parse error at startup. The description text says "token" but the actual value must be `"bearer"`.
- **Effort**: `trivial`

### 4. Non-canonical KDL boolean/null syntax in telegram-bridge.md
- **What**: Uses bare `true`, `false`, `null` instead of canonical `#true`, `#false`, `#null` in KDL examples
- **Where**: `docs/spell-server/telegram-bridge.md` — lines 75, 162, 198, 301, 318, 325
- **Why**: While the parser may accept bare forms, the kdl-schema-reference.md explicitly documents `#null` as canonical and the getting-started.md uses `#true`/`#null` consistently. Inconsistency between docs is confusing for users following both.
- **Effort**: `trivial`

### 5. `auto-send-images #true` default mismatch in telegram-bridge.md examples
- **What**: Multi-project example shows `auto-send-images true` (bare boolean) while kdl-schema-reference.md documents it as `auto-send-images #true`
- **Where**: `docs/spell-server/telegram-bridge.md` — line 301
- **Why**: Same consistency issue as finding #4 — bare vs canonical KDL syntax.
- **Effort**: `trivial` (covered by fix for finding #4)

* Goal

Fix five syntax errors and broken references introduced in PLAN-158 spell-server docs. All are trivial text replacements in two files that would cause runtime errors or confusion if users copy the KDL examples.

* Files to modify

- =docs/spell-server/telegram-bridge.md=
- =docs/spell-server/getting-started.md=

After both files are fixed, re-run =bun packages/coding-agent/scripts/generate-docs-index.ts= to update the embedded docs index with corrected content.

* Fix 1: Wrong =env:= syntax in telegram-bridge.md

The =resolveEnvString= function in =packages/spell-server/src/config/channels-parser.ts= recognizes =env(VAR_NAME)= syntax. The doc uses =env:VAR_NAME= which will NOT resolve.

Lines to fix (exact current content -> replacement):

| Line | Current | Replacement |
|------|---------|-------------|
| 122 | =stt-api-key "env:DEEPGRAM_API_KEY"= | =stt-api-key "env(DEEPGRAM_API_KEY)"= |
| 127 | =tts-api-key "env:ELEVENLABS_API_KEY"= | =tts-api-key "env(ELEVENLABS_API_KEY)"= |
| 289 | =bot-token "env:TELEGRAM_BOT_TOKEN"= | =bot-token "env(TELEGRAM_BOT_TOKEN)"= |
| 309 | =stt-api-key "env:DEEPGRAM_API_KEY"= | =stt-api-key "env(DEEPGRAM_API_KEY)"= |
| 312 | =tts-api-key "env:ELEVENLABS_API_KEY"= | =tts-api-key "env(ELEVENLABS_API_KEY)"= |

* Fix 2: Broken cross-reference link in telegram-bridge.md

Line 9 currently reads:
#+begin_example
Configuration lives in `channels.kdl` alongside `spell-server.kdl`. See [Configuration Reference](configuration.md) for the full server config.
#+end_example

Two problems:
- =spell-server.kdl= is not a real file; the file is called =server.kdl=
- =configuration.md= does not exist; the correct target is =kdl-schema-reference.md=

Replace with:
#+begin_example
Configuration lives in `channels.kdl` alongside `server.kdl`. See [KDL Schema Reference](kdl-schema-reference.md) for the full config reference.
#+end_example

* Fix 3: Wrong =auth="token"= in getting-started.md

The manifest parser at =packages/spell-server/src/manifest/parser.ts= line 287 only accepts ="hmac"= or ="bearer"=. Using ="token"= throws a parse error at startup.

Lines to fix in =docs/spell-server/getting-started.md=:

Line 310 (prose): Replace =use `auth="token"` in the schedule= with =use `auth="bearer"` in the schedule=.

Line 328 (KDL example): Replace =schedule type="webhook" path="ci-run" auth="token"= with =schedule type="webhook" path="ci-run" auth="bearer"=.

* Fix 4: Non-canonical KDL boolean/null syntax in telegram-bridge.md

The kdl-schema-reference.md documents =#null= as canonical. The getting-started.md uses =#true= / =#null= consistently. The telegram-bridge.md should match.

Lines to fix in =docs/spell-server/telegram-bridge.md=:

| Line | Current | Replacement |
|------|---------|-------------|
| 75 | =idle-timeout null= (in prose) | =idle-timeout #null= |
| 162 | =auto-send-images false= | =auto-send-images #false= |
| 198 | =notify-owners true= | =notify-owners #true= |
| 301 | =auto-send-images true= | =auto-send-images #true= |
| 318 | =notify-owners true= | =notify-owners #true= |
| 325 | =idle-timeout null= | =idle-timeout #null= |

* Verification

1. Grep =docs/spell-server/telegram-bridge.md= for ="env:= — zero matches
2. Grep =docs/spell-server/telegram-bridge.md= for =configuration.md= — zero matches
3. Grep =docs/spell-server/telegram-bridge.md= for =spell-server.kdl= — zero matches
4. Grep =docs/spell-server/getting-started.md= for =auth="token"= — zero matches
5. Grep =docs/spell-server/telegram-bridge.md= for bare = true=, = false=, = null= at end of KDL lines — zero matches (all should be =#true=, =#false=, =#null=)
6. =bun packages/coding-agent/scripts/generate-docs-index.ts= succeeds
7. Quick eyeball: no other =env:= or bare boolean/null occurrences remain in either file
