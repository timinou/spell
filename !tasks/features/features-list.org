#+TITLE: Autonomy State Builtin Tool
#+STATE: DONE
#+SESSION_ID: 14aae19d4658f90a
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-spell/2026-04-02T10-59-46-191Z_14aac3ad63d8f8fa/8-CreateStateTool.jsonl]]
#+CUSTOM_ID: FEAT-217-autonomy-state-builtin-tool
#+LAYER: packages/coding-agent
#+DEPENDS: FEAT-215-kdl-manifest-schema-and-parser
#+EFFORT: 4h
#+PRIORITY: #A

NOTE [2026-04-02]: Implemented autonomy_state hidden builtin tool with SQLite KV store, schema validation, registered in BUILTIN_TOOLS. Tests passing.

* Scope
- In: Register =autonomy_state= as hidden builtin tool, per-goal SQLite DB with daemon metadata table (=run_id=, =started_at=, =status=, =duration=, =output_summary=) and agent KV store (=get/set/list/delete=), optional typed schema validation from manifest config
- Out: Shared setup-level state (FUP), complex SQL queries, state migration tooling
- Boundary: The tool is registered in =BUILTIN_TOOLS= but hidden from =--help=. Activated when =--tools= includes =autonomy_state=. DB path passed via env var =SPELL_AUTONOMY_STATE_DB=.

* Tests
- TDD file: =packages/coding-agent/test/tools/autonomy-state.test.ts=
- Scenario: =get= returns =null= for non-existent key
- Scenario: =set= stores string value, =get= retrieves it
- Scenario: =set= stores JSON object value, =get= retrieves parsed object
- Scenario: =set= overwrites existing key
- Scenario: =list= returns all keys sorted alphabetically
- Scenario: =delete= removes key, subsequent =get= returns =null=
- Scenario: =delete= non-existent key is no-op (no error)
- Scenario: metadata table has =run_id= and =started_at= populated
- Scenario: typed schema rejects write with wrong type (number field gets string)
- Scenario: typed schema allows write with correct type
- Scenario: no schema configured allows any key-value
- Scenario: tool not in =--tools= list is not available to agent
- Scenario: tool in =--tools= list is available and functional
- Tests written BEFORE implementation steps below

* Implementation
** Define tool schema and types
:PROPERTIES:
:CUSTOM_ID: FEAT-217-autonomy-state-builtin-tool::tool-types
:END:
- File: =packages/coding-agent/src/tools/autonomy-state/types.ts=
- Operations: =get(key)=, =set(key, value)=, =list()=, =delete(key)=, =get_metadata()=
- Define request/response shapes shared by tests, tool handler, and store API

** Write failing tool tests first
:PROPERTIES:
:CUSTOM_ID: FEAT-217-autonomy-state-builtin-tool::write-tests
:DEPENDS: FEAT-217-autonomy-state-builtin-tool::tool-types
:END:
- File: =packages/coding-agent/test/tools/autonomy-state.test.ts=
- Add availability gating assertions for =--tools= and functional coverage for KV + metadata + schema validation
- Lock TDD order: tests land before store, schema, handler, and registry wiring

** Implement SQLite state store
:PROPERTIES:
:CUSTOM_ID: FEAT-217-autonomy-state-builtin-tool::state-store
:DEPENDS: FEAT-217-autonomy-state-builtin-tool::write-tests
:END:
- File: =packages/coding-agent/src/tools/autonomy-state/store.ts=
- Use =bun:sqlite=
- Create DB at =SPELL_AUTONOMY_STATE_DB= path
- Tables: =_metadata(run_id, started_at, status, duration, output_summary)= and =kv_store(key TEXT PRIMARY KEY, value TEXT, updated_at INTEGER)=
- Enable WAL mode for concurrent safety

** Implement optional schema validator
:PROPERTIES:
:CUSTOM_ID: FEAT-217-autonomy-state-builtin-tool::schema-validator
:DEPENDS: FEAT-217-autonomy-state-builtin-tool::write-tests
:END:
- File: =packages/coding-agent/src/tools/autonomy-state/schema.ts=
- Load schema from =SPELL_AUTONOMY_STATE_SCHEMA= env var (JSON)
- Validate value types on =set()=
- On manifest/schema drift, log warning and allow write to avoid breaking a running goal

** Implement tool handler
:PROPERTIES:
:CUSTOM_ID: FEAT-217-autonomy-state-builtin-tool::tool-handler
:DEPENDS: FEAT-217-autonomy-state-builtin-tool::state-store FEAT-217-autonomy-state-builtin-tool::schema-validator
:END:
- File: =packages/coding-agent/src/tools/autonomy-state/tool.ts=
- Follow standard tool registration pattern for builtin tools
- Expose KV operations plus =get_metadata=

** Register as hidden builtin
:PROPERTIES:
:CUSTOM_ID: FEAT-217-autonomy-state-builtin-tool::register-builtin
:DEPENDS: FEAT-217-autonomy-state-builtin-tool::tool-handler
:END:
- File: =packages/coding-agent/src/tools/index.ts=
- Add =autonomy_state= to =BUILTIN_TOOLS=
- Keep hidden from help/listing unless explicitly activated through =--tools=

* Edge Cases
- DB file doesn't exist: create with schema on first access
- DB file exists from previous run: open, reuse data (state persists across runs)
- Concurrent writes from bash subprocess: WAL mode handles this
- Very large values (>1MB): allow but log warning
- Schema mismatch after manifest change: log warning, allow write (don't break running goal)

* Acceptance Criteria
- =bun test packages/coding-agent/test/tools/autonomy-state.test.ts= passes
- =autonomy_state= tool appears in =BUILTIN_TOOLS= registry
- Tool is NOT shown in =--help= or tool listing unless explicitly activated
- =bun check:ts= passes with new tool code
* DONE CLI Commands and Package Setup
:PROPERTIES:
:CUSTOM_ID: FEAT-272-cli-commands-and-package-setup
:EFFORT: 6h
:PRIORITY: A
:LAYER: application
:DEPENDS: FEAT-265-sync-kdl-config-schema-and-parser FEAT-267-ssh-and-rsync-operations-module FEAT-268-compiled-binary-bundle-builder FEAT-269-secret-management-with-age-encryption FEAT-270-systemd-service-management FEAT-271-continuous-bidirectional-watch-mode
:END:

  NOTE [2026-04-03]: Completed: CLI entry point, subcommand routing (init/push/pull/watch/status/secrets/bootstrap), resolveContext with target validation. Tests pass.

### Scope
- IN: Package scaffolding (package.json, tsconfig.json), CLI entry point (main.ts), subcommand routing (init/push/pull/watch/status/secrets/bootstrap), integration with coding-agent `spell deploy`, terminal output formatting, error handling, dry-run support
- OUT: Underlying module implementations (handled by FEAT-265 through FEAT-271)

### Tests
- File: packages/spell-deploy/test/cli/commands.test.ts
- Scenarios:
  1. Parse `spell deploy push production` resolves correct target from sync.kdl
  2. Default target used when target name omitted
  3. Unknown subcommand produces help text
  4. Missing .spell/sync.kdl produces actionable error
  5. Unknown target name lists available targets
  6. --dry-run flag propagated to all operations
  7. Bootstrap runs init + secrets + push in sequence
  8. Push orchestration: stop service -> atomic push -> start service -> health check

### Implementation
** Package scaffolding
:PROPERTIES:
:CUSTOM_ID: FEAT-272-cli-commands-and-package-setup::scaffold
:END:
- Files: packages/spell-deploy/package.json, tsconfig.json, src/index.ts
- Workspace deps: @oh-my-pi/pi-utils, @bgotink/kdl
- Binary: spell-deploy in package.json bin
- Register in root workspace

** CLI entry and arg parsing
:PROPERTIES:
:CUSTOM_ID: FEAT-272-cli-commands-and-package-setup::cli-entry
:DEPENDS: FEAT-272-cli-commands-and-package-setup::scaffold
:END:
- File: packages/spell-deploy/src/main.ts
- Subcommands: init, push, pull, watch, status, secrets, bootstrap
- Flags: --target, --dry-run, --config-dir, --verbose

** CLI tests
:PROPERTIES:
:CUSTOM_ID: FEAT-272-cli-commands-and-package-setup::cli-tests
:DEPENDS: FEAT-272-cli-commands-and-package-setup::cli-entry
:END:
- File: packages/spell-deploy/test/cli/commands.test.ts

** Push command orchestration
:PROPERTIES:
:CUSTOM_ID: FEAT-272-cli-commands-and-package-setup::cmd-push
:DEPENDS: FEAT-272-cli-commands-and-package-setup::cli-tests
:END:
- Composes: config loader + service stop + rsync push + dir swap + service start + health check
- Handles errors at each step with rollback guidance

** Init command
:PROPERTIES:
:CUSTOM_ID: FEAT-272-cli-commands-and-package-setup::cmd-init
:DEPENDS: FEAT-272-cli-commands-and-package-setup::cli-tests
:END:
- Creates remote dirs, user, pushes bundle, installs systemd unit

** Pull command
:PROPERTIES:
:CUSTOM_ID: FEAT-272-cli-commands-and-package-setup::cmd-pull
:DEPENDS: FEAT-272-cli-commands-and-package-setup::cli-tests
:END:
- Composes: config loader + SQLite backup + rsync pull

** Watch command
:PROPERTIES:
:CUSTOM_ID: FEAT-272-cli-commands-and-package-setup::cmd-watch
:DEPENDS: FEAT-272-cli-commands-and-package-setup::cli-tests
:END:
- Starts continuous sync, handles SIGINT for graceful shutdown

** Status command
:PROPERTIES:
:CUSTOM_ID: FEAT-272-cli-commands-and-package-setup::cmd-status
:DEPENDS: FEAT-272-cli-commands-and-package-setup::cli-tests
:END:
- Remote service status via systemctl + health check via HTTP

** Secrets command
:PROPERTIES:
:CUSTOM_ID: FEAT-272-cli-commands-and-package-setup::cmd-secrets
:DEPENDS: FEAT-272-cli-commands-and-package-setup::cli-tests
:END:
- Decrypt .env.age and push to remote

** Bootstrap command
:PROPERTIES:
:CUSTOM_ID: FEAT-272-cli-commands-and-package-setup::cmd-bootstrap
:DEPENDS: FEAT-272-cli-commands-and-package-setup::cmd-init FEAT-272-cli-commands-and-package-setup::cmd-secrets FEAT-272-cli-commands-and-package-setup::cmd-push
:END:
- Orchestrates init -> secrets -> push in sequence

** Coding-agent integration
:PROPERTIES:
:CUSTOM_ID: FEAT-272-cli-commands-and-package-setup::spell-integration
:DEPENDS: FEAT-272-cli-commands-and-package-setup::cmd-push
:END:
- File: packages/coding-agent/src/commands/deploy.ts
- `spell deploy` delegates to spell-deploy binary

### Edge Cases
- Running from wrong directory (no .spell/sync.kdl)
- Ctrl+C during watch mode (graceful shutdown)
- Multiple concurrent deploy commands to same target (advisory lock via /tmp/spell-deploy-{target}.lock)
- Exit codes: 0=success, 1=error, 2=partial (push succeeded but health check failed)

### Acceptance Criteria
- All CLI tests pass
- `spell deploy push production` runs full orchestrated push cycle
- `spell deploy bootstrap production` runs init+secrets+push
- `spell deploy watch production` starts continuous sync
- --dry-run shows commands without executing
- Errors include actionable suggestions

* Scope
- IN: Package scaffolding (package.json, tsconfig.json), CLI entry point (main.ts), all deploy subcommands (init, push, pull, watch, status, secrets, bootstrap), integration with coding-agent's =spell deploy= command, terminal output formatting, error handling
- OUT: Implementation of underlying operations (handled by other modules), NixOS module

* Tests
- File: =packages/spell-deploy/test/cli/commands.test.ts=
- Scenarios:
  1. Parse CLI args: =spell deploy push production= resolves correct target
  2. Default target used when target name omitted
  3. Unknown subcommand produces help text
  4. Missing =sync.kdl= produces actionable error
  5. Unknown target name produces error with available targets
  6. =--dry-run= flag propagated to operations
  7. Bootstrap runs init + secrets + push in sequence

* Implementation
1. Package scaffolding (=FEAT-272-cli-commands-and-package-setup::scaffold=)
   - Files: =packages/spell-deploy/package.json=, =tsconfig.json=, =src/index.ts=
   - Workspace dependency on =@oh-my-pi/pi-utils=, =@bgotink/kdl=
   - Binary: =spell-deploy=
2. CLI entry and arg parsing (=FEAT-272-cli-commands-and-package-setup::cli-entry=, depends on =FEAT-272-cli-commands-and-package-setup::scaffold=)
   - File: =packages/spell-deploy/src/main.ts=
   - Subcommands: =init=, =push=, =pull=, =watch=, =status=, =secrets=, =bootstrap=
   - =--target= flag, =--dry-run= flag, =--config-dir= flag
3. Write CLI tests (=FEAT-272-cli-commands-and-package-setup::cli-tests=, depends on =FEAT-272-cli-commands-and-package-setup::cli-entry=)
   - File: =packages/spell-deploy/test/cli/commands.test.ts=
4. Implement init command (=FEAT-272-cli-commands-and-package-setup::cmd-init=, depends on =FEAT-272-cli-commands-and-package-setup::cli-tests=)
   - Create remote dirs, push bundle, install systemd unit, create user
5. Implement push command (=FEAT-272-cli-commands-and-package-setup::cmd-push=, depends on =FEAT-272-cli-commands-and-package-setup::cli-tests=)
   - Load config, atomic push, restart service, health check
6. Implement pull command (=FEAT-272-cli-commands-and-package-setup::cmd-pull=, depends on =FEAT-272-cli-commands-and-package-setup::cli-tests=)
   - Load config, SQLite backup, rsync pull
7. Implement watch command (=FEAT-272-cli-commands-and-package-setup::cmd-watch=, depends on =FEAT-272-cli-commands-and-package-setup::cli-tests=)
   - Start continuous sync, handle SIGINT
8. Implement status command (=FEAT-272-cli-commands-and-package-setup::cmd-status=, depends on =FEAT-272-cli-commands-and-package-setup::cli-tests=)
   - Remote service status + health check
9. Implement secrets command (=FEAT-272-cli-commands-and-package-setup::cmd-secrets=, depends on =FEAT-272-cli-commands-and-package-setup::cli-tests=)
   - Decrypt and push =.env=
10. Implement bootstrap command (=FEAT-272-cli-commands-and-package-setup::cmd-bootstrap=, depends on =FEAT-272-cli-commands-and-package-setup::cmd-init=, =FEAT-272-cli-commands-and-package-setup::cmd-secrets=, =FEAT-272-cli-commands-and-package-setup::cmd-push=)
    - Orchestrate =init= → =secrets= → =push=
11. Integration with coding-agent (=FEAT-272-cli-commands-and-package-setup::spell-integration=, depends on =FEAT-272-cli-commands-and-package-setup::cmd-push=)
    - File: =packages/coding-agent/src/commands/deploy.ts=
    - =spell deploy= delegates to =spell-deploy=

* Edge Cases
- Running from wrong directory (no =.spell/sync.kdl=)
- Ctrl+C during watch mode (graceful shutdown)
- Multiple concurrent deploy commands to same target (lock file)
- stdout vs stderr: progress to stderr, results to stdout
- Exit codes: 0=success, 1=error, 2=partial failure

* Acceptance Criteria
- All CLI tests pass
- =spell deploy push production= runs full push cycle
- =spell deploy bootstrap production= runs init+secrets+push
- =spell deploy watch production= starts continuous sync
- Errors produce actionable messages with suggestions
- =--dry-run= shows what would happen without executing

* Scope
- IN: Package scaffolding (package.json, tsconfig.json), CLI entry point (main.ts), all deploy subcommands (init, push, pull, watch, status, secrets, bootstrap), integration with coding-agent's =spell deploy= command, terminal output formatting, error handling
- OUT: Implementation of underlying operations (handled by other modules), NixOS module

* Tests
- File: =packages/spell-deploy/test/cli/commands.test.ts=
- Scenarios:
  1. Parse CLI args: =spell deploy push production= resolves correct target
  2. Default target used when target name omitted
  3. Unknown subcommand produces help text
  4. Missing =sync.kdl= produces actionable error
  5. Unknown target name produces error with available targets
  6. =--dry-run= flag propagated to operations
  7. Bootstrap runs init + secrets + push in sequence

* Implementation
1. Package scaffolding (=PARENT-ID::scaffold=)
   - Files: =packages/spell-deploy/package.json=, =tsconfig.json=, =src/index.ts=
   - Workspace dependency on =@oh-my-pi/pi-utils=, =@bgotink/kdl=
   - Binary: =spell-deploy=
2. CLI entry and arg parsing (=PARENT-ID::cli-entry=, depends on =PARENT-ID::scaffold=)
   - File: =packages/spell-deploy/src/main.ts=
   - Subcommands: =init=, =push=, =pull=, =watch=, =status=, =secrets=, =bootstrap=
   - =--target= flag, =--dry-run= flag, =--config-dir= flag
3. Write CLI tests (=PARENT-ID::cli-tests=, depends on =PARENT-ID::cli-entry=)
   - File: =packages/spell-deploy/test/cli/commands.test.ts=
4. Implement init command (=PARENT-ID::cmd-init=, depends on =PARENT-ID::cli-tests=)
   - Create remote dirs, push bundle, install systemd unit, create user
5. Implement push command (=PARENT-ID::cmd-push=, depends on =PARENT-ID::cli-tests=)
   - Load config, atomic push, restart service, health check
6. Implement pull command (=PARENT-ID::cmd-pull=, depends on =PARENT-ID::cli-tests=)
   - Load config, SQLite backup, rsync pull
7. Implement watch command (=PARENT-ID::cmd-watch=, depends on =PARENT-ID::cli-tests=)
   - Start continuous sync, handle SIGINT
8. Implement status command (=PARENT-ID::cmd-status=, depends on =PARENT-ID::cli-tests=)
   - Remote service status + health check
9. Implement secrets command (=PARENT-ID::cmd-secrets=, depends on =PARENT-ID::cli-tests=)
   - Decrypt and push =.env=
10. Implement bootstrap command (=PARENT-ID::cmd-bootstrap=, depends on =PARENT-ID::cmd-init=, =PARENT-ID::cmd-secrets=, =PARENT-ID::cmd-push=)
    - Orchestrate =init= → =secrets= → =push=
11. Integration with coding-agent (=PARENT-ID::spell-integration=, depends on =PARENT-ID::cmd-push=)
    - File: =packages/coding-agent/src/commands/deploy.ts=
    - =spell deploy= delegates to =spell-deploy=

* Edge Cases
- Running from wrong directory (no =.spell/sync.kdl=)
- Ctrl+C during watch mode (graceful shutdown)
- Multiple concurrent deploy commands to same target (lock file)
- stdout vs stderr: progress to stderr, results to stdout
- Exit codes: 0=success, 1=error, 2=partial failure

* Acceptance Criteria
- All CLI tests pass
- =spell deploy push production= runs full push cycle
- =spell deploy bootstrap production= runs init+secrets+push
- =spell deploy watch production= starts continuous sync
- Errors produce actionable messages with suggestions
- =--dry-run= shows what would happen without executing
