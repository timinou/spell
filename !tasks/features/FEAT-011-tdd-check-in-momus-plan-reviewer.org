#+TITLE: TDD check in Momus plan reviewer
#+STATE: DONE
#+SESSION_ID: 14a4e5b57e629d71
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-spell/2026-03-28T21-37-34-201Z_14a4e5b57e629d71.jsonl]]
#+CUSTOM_ID: FEAT-011-tdd-check-in-momus-plan-reviewer
#+EFFORT: 15m
#+PRIORITY: B
#+LAYER: prompts

NOTE [2026-03-28]: Implemented in `packages/coding-agent/src/prompts/agents/momus.md`; local grep/read verification passed. Global `bun check:ts` remains blocked by unrelated workspace TypeScript error outside this item’s scope.

* Initial Message

have a slight nudge towards writing unit tests first (TDD) in plan modes, especially ultraplan. Slight nudge. Ask me questions

* Scope
- IN: ~packages/coding-agent/src/prompts/agents/momus.md~
- OUT: No other agent prompts, no TypeScript code

* Tests
- Verify with ~grep~ that Momus includes a testing-related check under quality criteria or child-item integrity
- Verify the check is lightweight — a note or sub-bullet, not a new top-level criterion section
- Read the full file after edit to confirm quality thresholds and output format are untouched

* Implementation
File: ~packages/coding-agent/src/prompts/agents/momus.md~

Add a lightweight testing check under the existing "Child-item integrity" section (lines 41-45). Currently this section checks scope, acceptance criteria, manifest references, and file references. Add a bullet:
- "Child items with behavioral changes SHOULD include concrete test scenarios; flag items where testing appears absent or is deferred to a generic 'add tests' step"

Alternatively, add under "Verification — HOW DO WE KNOW IT WORKED" (lines 26-28) a sub-point:
- "For behavioral changes, test scenarios defined before implementation steps are preferred; flag items where tests are missing or appear only as a post-implementation afterthought"

Choose the location that reads most naturally. The child-item integrity section is the better fit since it already validates per-item quality.

* Edge Cases
- Momus has an explicit approval bias: "reject only for true blockers." Missing TDD ordering is NOT a blocker — it's a note. The check must use "flag" language, not rejection language.
- Don't add a new output tag (like ~[TESTING]~) — use existing ~[VERIFICATION]~ tag for test-related findings
- Don't modify quality thresholds

* Acceptance Criteria
- Momus's child-item integrity section includes a bullet about flagging missing or afterthought test scenarios
- The check is advisory (flag/note), consistent with Momus's approval bias
- Quality thresholds and output format unchanged
