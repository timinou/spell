#+TITLE: Move spell-server docs to docsspell-server and regenerate pi index
#+STATE: DONE
#+SESSION_ID: 14afee2ae0359a29
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-spell/2026-04-06T11-17-49-056Z_14afee2ae0359a29.jsonl]]
#+CUSTOM_ID: FEAT-362-move-spell-server-docs-to-docs-spell-ser
#+EFFORT: S
#+PRIORITY: #C
#+LAYER: docs

NOTE [2026-04-06]: Moved 3 docs from packages/spell-server/docs/ to docs/spell-server/. Fixed stale CLI entrypoint claim. Added missing subsystems (workflow, socket, state, actions, telegram/voice). Old directory deleted. Index regenerated with all 3 keys.

* Initial Message

what documentation exists around spell-server and its local usage as a telegram bridge? is there a quickstart? is there something accessible through a resource, so that any spell session can call that link or resource and know what's up (setting it up, getting hte kdl schema refernce, etc.)?

* Goal

Move spell-server documentation from =packages/spell-server/docs/= to =docs/spell-server/= so it is embedded in the =pi://= internal URL system. The existing =generate-docs-index.ts= script uses =new Glob("**/*.md")= to scan =docs/=, so files at =docs/spell-server/*.md= auto-generate keys like ="spell-server/getting-started.md"= accessible as =pi://spell-server/getting-started.md=.

No changes to the generator script or protocol handler are needed.

* Files to change

** Move (create at new location, delete old)
- =packages/spell-server/docs/getting-started.md= → =docs/spell-server/getting-started.md=
- =packages/spell-server/docs/architecture.md= → =docs/spell-server/architecture.md=
- =packages/spell-server/docs/kdl-schema-reference.md= → =docs/spell-server/kdl-schema-reference.md=

** Delete
- =packages/spell-server/docs/= directory (after move)

** Regenerate
- Run =bun packages/coding-agent/scripts/generate-docs-index.ts=
- This updates =packages/coding-agent/src/internal-urls/docs-index.generated.ts=

** Minimal architecture.md fix (during the move)
While moving =architecture.md=, fix this stale claim:

Line 19 currently reads: "The missing piece is the CLI entrypoint that wires these modules together..."

Replace with something like: "The CLI entrypoint (=src/main.ts=) wires these modules together: loads config from =.spell/=, builds dependencies, starts the HTTP server and scheduler, and handles process shutdown."

Also add a brief note below the subsystem bullet list (around line 18) mentioning these new subsystems that exist but are not yet fully documented here:
- =workflow/= — approval engine, downstream jobs, checkpoints
- =socket/= — JSON-lines session bridge for blocking events
- =state/= — state store manager with SQLite backends and HTTP API
- =actions/= — action descriptor registry and validation
- =telegram/voice/= — STT/TTS providers (Deepgram, OpenAI, ElevenLabs)

Keep these as a brief enumeration only — no need to write full sections.

* Verification

1. =docs/spell-server/= directory exists with 3 .md files
2. =packages/spell-server/docs/= directory does not exist
3. Run =bun packages/coding-agent/scripts/generate-docs-index.ts= succeeds
4. The generated file =packages/coding-agent/src/internal-urls/docs-index.generated.ts= contains keys starting with ="spell-server/"=
5. Grep the generated file for ="spell-server/getting-started.md"=, ="spell-server/architecture.md"=, ="spell-server/kdl-schema-reference.md"= — all three must appear
6. The "missing piece" sentence in architecture.md is replaced
