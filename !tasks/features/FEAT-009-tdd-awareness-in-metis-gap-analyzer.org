#+TITLE: TDD awareness in Metis gap analyzer
#+STATE: DONE
#+SESSION_ID: 14a4e5b57e629d71
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-spell/2026-03-28T21-37-34-201Z_14a4e5b57e629d71.jsonl]]
#+CUSTOM_ID: FEAT-009-tdd-awareness-in-metis-gap-analyzer
#+EFFORT: 15m
#+PRIORITY: B
#+LAYER: prompts

NOTE [2026-03-28]: Implemented in `packages/coding-agent/src/prompts/agents/metis.md`; local grep/read verification passed. Global `bun check:ts` remains blocked by unrelated workspace TypeScript error outside this item’s scope.

* Initial Message

have a slight nudge towards writing unit tests first (TDD) in plan modes, especially ultraplan. Slight nudge. Ask me questions

* Scope
- IN: ~packages/coding-agent/src/prompts/agents/metis.md~
- OUT: No other agent prompts, no TypeScript code

* Tests
- Verify with ~grep~ that Metis's "Questioning completeness" section references test-first preference
- Verify the added text uses awareness/flagging language, not hard rejection language
- Read the full file after edit to confirm no structural damage to the output format sections

* Implementation
File: ~packages/coding-agent/src/prompts/agents/metis.md~

In the "Questioning completeness" section (lines 36-41), the existing check is:
#+begin_quote
Did prior questioning establish a testing approach?
#+end_quote

Expand this to also flag when:
- Testing is mentioned only as a post-implementation afterthought
- Test scenarios are vague or absent despite clear behavioral requirements
- The proposed approach treats tests as a validation step rather than a design input

Suggested addition after line 40 (under the testing approach bullet):
- "Flag if the testing approach is purely reactive (tests after implementation) rather than proactive (tests drive implementation). A test-first approach is preferred."

* Edge Cases
- This is Metis, a gap analyzer — it surfaces gaps, it doesn't reject. The language should be "flag" not "reject."
- Don't change the output format (GAPS/RISKS/RECOMMENDATIONS/MISSING CONTEXT sections)
- Don't add a new top-level section — this fits naturally under "Questioning completeness"

* Acceptance Criteria
- Metis prompt's Questioning completeness section includes a bullet about flagging reactive-only testing approaches
- The language is advisory ("flag", "preferred") not mandatory
- Output format section unchanged
