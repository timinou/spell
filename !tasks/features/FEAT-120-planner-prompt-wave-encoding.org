#+TITLE: Planner Prompt Wave Encoding
#+STATE: DONE
#+SESSION_ID: 14a82678a0be5f38
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-spell/2026-03-30T22-16-33-669Z_14a781d021545ee2/24-CreateFeatH.jsonl]]
#+CUSTOM_ID: FEAT-120-planner-prompt-wave-encoding
#+EFFORT: 2h
#+PRIORITY: #A
#+LAYER: prompt
#+BLOCKER: PROJ-007-sub-outline-custom-id-and-dependency-ext PROJ-006-consolidate-blocker-to-depends

NOTE [2026-03-31]: Updated plan-mode-active.md with sub-outline CUSTOM_ID format, DEPENDS property, wave-structured Execution Manifest, and org wave usage in ultraplan Phase 5.

* Scope
** In-scope
- =packages/coding-agent/src/prompts/system/plan-mode-active.md= -- ALL changes in this file:
  - Org Item Body Standard: sub-outline CUSTOM_IDs on implementation steps
  - DEPENDS property for sub-outline dependencies (replaces BLOCKER)
  - Wave-structured Execution Manifest format (always, not optional)
  - Ultraplan meta-planning: recursive wave-based planning
  - Example showing complete wave-structured plan

** Out-of-scope
- Executor prompts (plan-mode-approved.md, todo-write.md -- handled by FEAT executor prompts)
- Code changes (none needed)
- Wave computation logic (PROJ emergent wave computation)

Boundary: owns ALL changes to plan-mode-active.md.

Requires: sub-outline =::= CUSTOM_ID format from PROJ-007, DEPENDS naming from PROJ-006.

* Tests
No automated tests -- prompt-only change. Verification by reading the file.

* Implementation
** Step 1: Update Org Item Body Standard
- Implementation steps MUST have CUSTOM_ID properties using =PARENT-ID::sub-slug= format
- Steps declare dependencies via =:DEPENDS:= property referencing other sub-outline IDs
- Wave tags emerge automatically from topological sort (planner does NOT manually assign waves)
- Example:
#+begin_example
* Implementation
** Define TypeScript interfaces
:PROPERTIES:
:CUSTOM_ID: FEAT-001::define-types
:END:
- File: src/types/foo.ts

** Implement core parser
:PROPERTIES:
:CUSTOM_ID: FEAT-001::implement-parser
:DEPENDS: FEAT-001::define-types
:END:
- File: src/parser.ts

** Write unit tests
:PROPERTIES:
:CUSTOM_ID: FEAT-001::write-tests
:DEPENDS: FEAT-001::implement-parser
:END:
- File: test/parser.test.ts
#+end_example

** Step 2: Update Execution Manifest format
- ALL plans use wave-structured Execution Manifest (not optional)
- Waves are generated by =org wave= command from computed topological layers
- Planner runs =org wave= to get skeleton, fills in descriptions
- Format: wave headings with =:wave:= tag, sub-outline refs under each
- Example:
#+begin_example
* Execution Manifest
** foundation                                      :wave:
- [[id:FEAT-001::define-types]] Define TypeScript interfaces
- [[id:FEAT-002::define-schema]] Define parser schema types
** core                                            :wave:
- [[id:FEAT-001::implement-parser]] Implement parser logic
- [[id:FEAT-002::implement-validator]] Implement validation
** verify                                          :wave:
- [[id:FEAT-001::write-tests]] Write parser tests
- [[id:FEAT-002::write-tests]] Write validator tests
#+end_example

** Step 3: Update ultraplan section
- Phase 1: parallel exploration per subsystem
- Phase 5: use =org wave= to compute wave structure, generate skeleton manifest
- Meta-planning is recursive: the plan's own manifest is wave-structured

** Step 4: Replace BLOCKER references with DEPENDS
- All =properties: { BLOCKER: ... }= examples become =properties: { DEPENDS: ... }=
- All =:BLOCKER:= references become =:DEPENDS:=

* Edge Cases
- Plans with single item: one wave, still uses wave format
- Plans with no sub-outline deps: all steps in wave 1
- Backward compat: flat [[id:...]] manifests deprecated but may still exist in old plans

* Acceptance Criteria
- plan-mode-active.md documents sub-outline CUSTOM_ID format with examples
- plan-mode-active.md documents DEPENDS property for sub-outline dependencies
- Execution Manifest always uses wave structure (not optional)
- Ultraplan section includes =org wave= usage and recursive meta-planning
- No references to =BLOCKER= as org property remain in plan-mode-active.md
