#+TITLE: Clean up unused FluidOrchestrator execution paths
#+STATE: DONE
#+SESSION_ID: 14a989b9bbdd6755
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-spell/2026-04-01T12-08-05-615Z_14a989b9bbdd6755.jsonl]]
#+CUSTOM_ID: FEAT-171-clean-up-unused-fluidorchestrator-execut
#+EFFORT: S
#+PRIORITY: #C
#+LAYER: core
#+DEPENDS: FEAT-167-unify-plan-approval-to-agent-driven-exec FEAT-170-canvas-fluid-mode-convergence-with-agent

NOTE [2026-04-01]: Completed under PLAN-062. Pruned unused fluid barrel exports and kept the remaining public surface focused on compilation/validation helpers still exercised by current tests.

* Initial Message

Session Info

 File:
 /home/user/.spell/agent/sessions/-code-ora-spell/2026-04-01T12-03-10-333Z_14a988995f409bb6.jsonl
 ID: 14a988995f409bb6


can you investigate if fluid plan was used? Was it used? Is todo write the right entry point for it? I'm open, ask me a lot of questiosn until you find the gap betweent he implenetation of the fluid orchestration/plan and how agents are actually used. Thanks

* Scope

After /plan and canvas paths both use agent-driven execution, audit what remains in the fluid orchestrator module. Remove dead code, simplify the module surface.

** Files
- MODIFY: ~packages/coding-agent/src/orchestrators/fluid/index.ts~ — update barrel exports
- ASSESS: ~packages/coding-agent/src/orchestrators/fluid/queue-scheduler.ts~ — still needed?
- ASSESS: ~packages/coding-agent/src/orchestrators/fluid/fluid-orchestrator.ts~ — still needed?
- ASSESS: ~packages/coding-agent/src/orchestrators/fluid/org-fluid-bridge.ts~ — delete if unused
- MODIFY: ~packages/coding-agent/src/orchestrators/fluid/types.ts~ — prune unused types
- MODIFY: existing test files — remove tests for deleted code

** Decision Matrix
| Component              | /plan uses? | Canvas uses? | Keep? |
|------------------------+-------------+--------------+-------|
| FluidPlan types        | Yes (input) | Yes (input)  | Yes   |
| DAG utilities (dag.ts) | Yes (validation) | Yes (validation) | Yes |
| plan-to-todos.ts       | Yes         | Yes          | Yes   |
| FluidOrchestrator      | No          | Maybe (validation only) | Assess |
| QueueScheduler         | No          | No (agent drives) | Remove |
| OrgFluidBridge         | No          | No           | Remove |
| coordinator-runner.ts  | No          | Maybe        | Assess |
| plan-validator.ts      | Unclear     | Yes          | Keep   |
| event-router.ts        | No          | Yes (QML)    | Keep   |

** Key Principle
Keep: types, DAG utilities, plan-schema, event-router, plan-validator
Remove: anything whose responsibility moved to todo_write or agent-driven execution
The module should be a *plan compilation and validation library*, not an execution engine.

* Acceptance
- No dead code warnings from ~bun lint:ts~ in the fluid module
- ~bun check:ts~ clean
- All remaining exports are actually imported somewhere
- Test files cleaned: no tests for removed code
