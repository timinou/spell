#+TITLE: Enhance todo_write: verify.review LLM-gating + wave viz
#+STATE: DONE
#+SESSION_ID: 1502540eec30c834
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-spell/2026-06-09T11-18-25-968Z_1502540eec30c834.jsonl]]
#+CUSTOM_ID: FEAT-806-enhance-new-todo-write-capability-scope

* Initial Message

i restarted the session, can you test drive this?

PLAN-330b. Blocked on 330a + capability naming. Candidates: (1) verify.review live LLM-gating — actually evaluate the advisory review via a model call instead of pure display; (2) multi-blocker wave visualization in summary; (3) ref auto-close-on-complete — when closesRef node verified, write org DONE inline. Pick one, define acceptance, then build.

SCOPED [2026-06-09]: PLAN-330b = BOTH (user decision):
1. verify.review live LLM-gating — review{} stops being display-only; on completion, evaluate acceptance criteria via a model call; unmet → block like two-phase gate.
2. multi-blocker wave visualization — surface DAG wave/depth in summary so parallelizable nodes are visible at a glance.
NB: ref auto-close already exists (todo-write.ts:485) — dropped from candidate set.