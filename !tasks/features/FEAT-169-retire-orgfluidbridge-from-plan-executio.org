#+TITLE: Retire OrgFluidBridge from plan execution
#+STATE: DONE
#+SESSION_ID: 14a989b9bbdd6755
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-spell/2026-04-01T12-08-05-615Z_14a989b9bbdd6755.jsonl]]
#+CUSTOM_ID: FEAT-169-retire-orgfluidbridge-from-plan-executio
#+EFFORT: S
#+PRIORITY: #B
#+LAYER: core
#+DEPENDS: FEAT-166-todo-write-org-lifecycle-hooks FEAT-167-unify-plan-approval-to-agent-driven-exec

NOTE [2026-04-01]: Completed under PLAN-062. interactive-mode no longer instantiates OrgFluidBridge; child lifecycle transitions now flow through todo_write hooks.

* Initial Message

Session Info

 File:
 /home/user/.spell/agent/sessions/-code-ora-spell/2026-04-01T12-03-10-333Z_14a988995f409bb6.jsonl
 ID: 14a988995f409bb6


can you investigate if fluid plan was used? Was it used? Is todo write the right entry point for it? I'm open, ask me a lot of questiosn until you find the gap betweent he implenetation of the fluid orchestration/plan and how agents are actually used. Thanks

* Scope

~OrgFluidBridge~ is no longer used in the /plan execution path (replaced by todo_write org hooks). Remove its usage from interactive-mode and assess whether it's still needed for canvas fluid-mode.

** Files
- MODIFY: ~packages/coding-agent/src/orchestrators/fluid/org-fluid-bridge.ts~ — potentially delete or mark for canvas-only
- MODIFY: ~packages/coding-agent/src/orchestrators/fluid/index.ts~ — update barrel exports
- MODIFY: ~packages/coding-agent/src/modes/interactive-mode.ts~ — remove OrgFluidBridge import (if not already removed by FEAT-167)

** Decision Points
- If canvas fluid-mode (FEAT-170) will also stop using OrgFluidBridge → DELETE the file
- If canvas fluid-mode still needs it during transition → keep but document as canvas-only
- The ~completePlanItem~ call from OrgFluidBridge.finalize() must be wired elsewhere:
  - Option: todo_write detects all-phases-completed and calls completePlanItem
  - Option: plan-mode-approved prompt instructs agent to close plan item manually (simpler, already done)

** Plan Completion Logic
~completePlanItem~ (from ~../../plan-mode/org-plan~) marks the PLAN org item as DONE. Currently called by OrgFluidBridge.finalize(). In the new model:
- Preferred: Add an all-phases-completed hook in todo_write that calls completePlanItem when the session has an active plan reference
- Alternative: Keep it in the prompt — agent marks PLAN item DONE after all tasks are verified

* Acceptance
- No ~OrgFluidBridge~ instantiation in ~interactive-mode.ts~
- Plan execution still transitions org items correctly (via todo_write hooks)
- PLAN item still gets marked DONE on plan completion
- ~bun check:ts~ clean
- Existing tests adapted or removed: check if ~packages/coding-agent/test/~ has OrgFluidBridge tests
