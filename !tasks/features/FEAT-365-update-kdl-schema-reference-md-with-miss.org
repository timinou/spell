#+TITLE: Update kdl-schema-referencemd with missing fields
#+STATE: DONE
#+SESSION_ID: 14afee2ae0359a29
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-spell/2026-04-06T11-17-49-056Z_14afee2ae0359a29.jsonl]]
#+CUSTOM_ID: FEAT-365-update-kdl-schema-reference-md-with-miss
#+EFFORT: M
#+PRIORITY: #B
#+LAYER: docs
#+DEPENDS: FEAT-362-move-spell-server-docs-to-docs-spell-ser

NOTE [2026-04-06]: Updated kdl-schema-reference.md. Added voice config block with flat child node structure per parseVoiceNode. Added auto-send-images field. Added per-user voice overrides section. Documented bot-token env() support. Confirmed socket default path correct. Updated null acceptance note.

* Initial Message

what documentation exists around spell-server and its local usage as a telegram bridge? is there a quickstart? is there something accessible through a resource, so that any spell session can call that link or resource and know what's up (setting it up, getting hte kdl schema refernce, etc.)?

* Goal

Update =docs/spell-server/kdl-schema-reference.md= (moved from =packages/spell-server/docs/= by FEAT-362) to document missing fields and fix inaccuracies found in the audit.

* Changes required

** 1. Add =voice= config block to channels.kdl section

After the =session-notifications= section (around line 103), add a new subsection documenting the voice config block. Source: =packages/spell-server/src/config/types.ts= lines 18-40 and =packages/spell-server/src/config/channels-parser.ts= =parseVoiceNode=.

#+begin_src kdl
telegram {
  voice {
    stt {
      provider "deepgram"
      api-key "env(DEEPGRAM_API_KEY)"
      model "nova-2"
      language "en"
    }
    tts {
      provider "elevenlabs"
      api-key "env(ELEVENLABS_API_KEY)"
      model "eleven_turbo_v2_5"
      voice "Rachel"
    }
    reply-mode "mirror"
  }
}
#+end_src

Document each field:

*** =voice= block (child of =telegram=)

| Child node | Type | Required | Default | Notes |
| =stt= | block | No | disabled | Speech-to-text config |
| =tts= | block | No | disabled | Text-to-speech config |
| =reply-mode= | string | No | ="mirror"= | One of: ="never"=, ="mirror"=, ="always"= |

*** =stt= block

| Child node | Type | Required | Default | Notes |
| =provider= | string | Yes | | ="deepgram"= or ="openai"= |
| =api-key= | string | Yes | | Supports =env()= resolution |
| =model= | string | No | provider default | Provider-specific model name |
| =language= | string | No | ="en"= | BCP-47 language code |

*** =tts= block

| Child node | Type | Required | Default | Notes |
| =provider= | string | Yes | | ="elevenlabs"= or ="deepgram"= |
| =api-key= | string | Yes | | Supports =env()= resolution |
| =model= | string | No | provider default | Provider-specific model name |
| =voice= | string | No | provider default | Voice name/ID |

** 2. Add =auto-send-images= field

In the telegram field listing (around line 74), add:
- =auto-send-images= (boolean, optional, default =#true=): When true, images generated by the =generate_image= tool are automatically sent to the chat. Set to =#false= to suppress auto-sending.

Source: =packages/spell-server/src/config/types.ts= line 86 (=autoSendImages: boolean=) and =channels-parser.ts=.

** 3. Add per-user =voice= block

In the user block section, add that users can override voice settings:

#+begin_src kdl
user 123456789 {
  modes "telegram-readonly" "telegram-full"
  voice {
    reply-mode "always"
    tts-voice "Antoni"
  }
}
#+end_src

| Child node | Type | Required | Default | Notes |
| =reply-mode= | string | No | inherits global | ="never"=, ="mirror"=, ="always"= |
| =tts-voice= | string | No | inherits global | Override TTS voice for this user |

** 4. Document =bot-token= env() support

In the "Important rules" section (line 74-76), add a note:
"=bot-token= supports =env()= resolution: =bot-token \"env(TELEGRAM_BOT_TOKEN)\"=. Same for =bot-token-file=."

Source: =channels-parser.ts= uses =resolveEnvString= for both fields.

** 5. Fix socket default path

Line 42 states: =| path | string | No | No default | Supports env(...) resolution. |=

Fix to: =| path | string | No | ~/.spell/server.sock | Supports env(...) resolution. |=

Source: =packages/spell-server/src/config/server-parser.ts= line 116: =socketNode?.getProperty("path") ?? path.join(os.homedir(), ".spell", "server.sock")=

** 6. Document null acceptance

Line 76 states: "Use =#null=, not bare =null=, for nullable values such as =idle-timeout #null=."

Update to: "Use =#null= for nullable values such as =idle-timeout #null=. Bare =null= is also accepted by the parser but =#null= is the canonical KDL syntax."

Source: =channels-parser.ts= line 279 handles both.

* Files to modify

- =docs/spell-server/kdl-schema-reference.md=

* Source files to reference

- =packages/spell-server/src/config/types.ts= — VoiceConfig (line 36), UserVoiceConfig (line 42), SttConfig (line 22), TtsConfig (line 29), TelegramChannelConfig (line 64)
- =packages/spell-server/src/config/channels-parser.ts= — parseVoiceNode (line 373), parseUserVoiceNode (line 465), auto-send-images parsing, bot-token env resolution
- =packages/spell-server/src/config/server-parser.ts= — socket default path (line 116)

* Verification

1. Voice config block documented with example and field tables
2. =auto-send-images= field present in telegram field listing
3. Per-user voice block documented
4. =bot-token= env() support mentioned
5. Socket default path shows =~/.spell/server.sock=, not "No default"
6. Null acceptance note updated
