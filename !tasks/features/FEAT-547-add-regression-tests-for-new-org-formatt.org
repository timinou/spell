#+TITLE: Add regression tests for new org formatter and preview behaviors
#+STATE: DONE
#+SESSION_ID: 14ba406cf0d96a78
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-spell/2026-04-14T11-42-01-155Z_14ba406cf0d96a78.jsonl]]
#+CUSTOM_ID: FEAT-547-add-regression-tests-for-new-org-formatt
#+DEPENDS: BUG-257-fix-plan-224-regressions-missing-type-fi
#+EFFORT: 1h
#+LAYER: tools
#+PRIORITY: #A

* Initial Message

Implement these audit recommendations from org://AUD-066-audit-implementation:

- **What**: `createOrgTool()` help text appears out of sync with the actual command surface; `delete` / `validate-plan` were added in code paths but the inner description string was still observed without them.
  - **Where**: `packages/org/src/tool.ts`, `createOrgTool()` description block near the command enum/switch
  - **Why**: `OrgTool.get description()` forwards this inner description; stale docs make the agent/tool prompt under-describe valid commands, which directly hurts discoverability and can recreate the ergonomics problem the plan was fixing.
  - **Effort**: `trivial`

- **What**: New org-wrapper presentation behavior lacks direct regression coverage.
  - **Where**: `packages/coding-agent/src/tools/org.ts`, `formatOrgResult()` success branch and `buildOrgCallPreview()` delete/validate-plan handling; no corresponding targeted assertions were added in `packages/coding-agent/test/tools/org-format-result.test.ts` / `packages/coding-agent/test/tools/org.test.ts`
  - **Why**: `bodyLength` rendering, `delete` preview metadata, and `validate-plan` formatting are now part of the user/agent-facing contract; without focused tests, future prompt/formatter churn can silently regress them while core org package tests still pass.
  - **Effort**: `trivial`

- **What**: `org create` body echo is produced from the locally rewritten request body, not a read-back of persisted content.
  - **Where**: `packages/org/src/tool.ts`, `cmdCreate()` success response near `return { success: true, id, file, category, state, body, bodyLength }`
  - **Why**: the new transparency contract says the response tells the agent what was persisted; if native create-time normalization or category-specific create behavior mutates body content, the echoed `body`/`bodyLength` can drift from disk and reintroduce “must org get to verify” uncertainty in edge cases.
  - **Effort**: `small`

- **What**: `validate-plan` is exposed as a first-class org command but only works when an external callback is injected.
  - **Where**: `packages/org/src/tool.ts`, `cmdValidatePlan()` + `CreateOrgToolOptions`; invoked from `packages/coding-agent/src/tools/org.ts`
  - **Why**: standalone consumers of `createOrgTool()` can successfully discover/call `validate-plan` yet only receive “not available in this org tool context”; that is surface-level contract drift, and it makes the command behave like a partial feature rather than a real org capability.
  - **Effort**: `small`

* Scope

Add focused test coverage for 4 new org tool behaviors introduced by PLAN-224 that currently have zero assertions:
1. =bodyLength= rendering in =formatOrgResult()= success branch
2. =delete= command preview in =buildOrgCallPreview()=
3. =validate-plan= command preview in =buildOrgCallPreview()=
4. =validate-plan= result formatting (valid/issues shape) in =formatOrgResult()=

*In scope*:
- Extend =packages/coding-agent/test/tools/org-format-result.test.ts= with assertions for bodyLength and validate-plan result formatting
- Extend =packages/coding-agent/test/tools/org.test.ts= with assertions for delete and validate-plan command previews

*Out of scope*:
- Testing the actual org command dispatch (already covered in =packages/org/test/tool-commands.test.ts=)
- Changing the formatting logic itself

* Existing Patterns
- =packages/coding-agent/test/tools/org-format-result.test.ts= — tests =formatOrgResult()= with 5 existing scenarios: mutation success, create result, error result, error without code, JSON fallthrough. Uses direct function import.
- =packages/coding-agent/test/tools/org.test.ts= — tests =OrgTool= class. =renderCall()= tested for query and update commands. Uses session mock.
- =packages/coding-agent/src/tools/org.ts= line ~325: =bodyLength= rendered as =body_length: N= in success branch
- =packages/coding-agent/src/tools/org.ts= lines ~305-314: validate-plan result formatted as =valid: true/false
issues: N
category: message
- item=
- =packages/coding-agent/src/tools/org.ts= lines ~212-218: delete/validate-plan grouped with get/note for preview metadata (pushMeta id + pushPathMeta file)

* Tests

File: =packages/coding-agent/test/tools/org-format-result.test.ts=
- Scenario 1: success result with =bodyLength: 123= → output includes =body_length: 123=
- Scenario 2: success result without bodyLength → output does NOT include =body_length=
- Scenario 3: validate-plan result with =valid: true, issues: []= → output is =valid: true
issues: 0=
- Scenario 4: validate-plan result with =valid: false, issues: [{category: "thin-child-body", message: "Add details", items: ["FEAT-101"]}]= → output includes category, message, and item

File: =packages/coding-agent/test/tools/org.test.ts=
- Scenario 5: =renderCall()= with =command: "delete", id: "FEAT-001"= → preview description is "delete", meta includes "id:FEAT-001"
- Scenario 6: =renderCall()= with =command: "validate-plan", id: "PLAN-001"= → preview description is "validate-plan", meta includes "id:PLAN-001"

* Implementation
** Write formatter result tests
:PROPERTIES:
:CUSTOM_ID: FEAT-547-add-regression-tests-for-new-org-formatt::formatter-result-tests
:END:
- File: =packages/coding-agent/test/tools/org-format-result.test.ts=
- Add scenarios 1-4 as new test cases in the existing describe block
- Follow existing pattern: direct import of =formatOrgResult= from ="../../src/tools/org"=
- Test exact output strings to catch regressions

** Write command preview tests
:PROPERTIES:
:CUSTOM_ID: FEAT-547-add-regression-tests-for-new-org-formatt::command-preview-tests
:DEPENDS: FEAT-547-add-regression-tests-for-new-org-formatt::formatter-result-tests
:END:
- File: =packages/coding-agent/test/tools/org.test.ts=
- Add scenarios 5-6 using existing =createSession()= pattern
- Follow existing pattern: instantiate =OrgTool(session)=, call =renderCall()=, check rendered text content
* Edge Cases
- =bodyLength: 0= is a valid value (empty body) → should still render =body_length: 0=
- validate-plan issues array with 0 items → should render =issues: 0= with no item lines
- =formatOrgResult= with a record that has =valid= but no =issues= array → should NOT match the validate-plan branch (guard is =Array.isArray(record.issues)=)

* Acceptance Criteria
- [ ] =bun test packages/coding-agent/test/tools/org-format-result.test.ts= passes with 4 new assertions
- [ ] =bun test packages/coding-agent/test/tools/org.test.ts= passes with 2 new assertions
- [ ] Each new test fails when the corresponding formatting code is commented out (proves it's not vacuous)

NOTE [2026-04-14]: Completed in PLAN-229 on 2026-04-14. Added regression coverage for org wrapper result formatting and delete/validate-plan previews.