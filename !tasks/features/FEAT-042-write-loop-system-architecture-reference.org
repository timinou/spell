#+TITLE: Write Loop System Architecture Reference
#+STATE: DONE
#+SESSION_ID: 14a60f2471639474
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-spell/2026-03-29T19-17-04-581Z_14a60f2471639474.jsonl]]
#+CUSTOM_ID: FEAT-042-write-loop-system-architecture-reference
#+EFFORT: 2h
#+PRIORITY: #B
#+LAYER: docs

NOTE [2026-03-29]: Architecture reference written at packages/coding-agent/src/loop/ARCHITECTURE.md (869 lines). Covers all 12 subsystem sections: system overview, directory map, state machine (full ALLOWED_TRANSITIONS table), LoopSnapshot data model, iteration flow, gate system, domain system, persistence layer, recursion system, safeguards, integration points, replay/debug, and constants reference.

* Initial Message

I recently created a loop system. Can you write a complete tutorial about it please? Investigate and give me your summary, then ask me questions, then plan to write the tutorial

* Goal
Write a detailed architecture reference for the Spell Loop system targeting contributors who need to understand, modify, or extend the internals.

File: =packages/coding-agent/src/loop/ARCHITECTURE.md=

* Content Outline

** System Overview
- Purpose and design philosophy
- 16+ subsystem inventory with file paths
- How LoopManager orchestrates everything

** State Machine
- LoopKernel finite state machine
- States: idle, planning, iterating, reflecting, validating, complete, failed, paused, cancelled, killed
- ALLOWED_TRANSITIONS table with explanation
- State transition diagram (ASCII or description)
- How ~done()~ advances through the state machine based on current state

** LoopSnapshot Data Model
- Full field reference for LoopSnapshot (all 40+ fields)
- Field groups: identity, timing, iteration state, children, gates, budget, progress tracking
- How fields are updated through the lifecycle

** Iteration Flow
- PhaseCoordinator.runIteration() walkthrough
- Plan phase -> Code phase -> Review phase
- HandoffArtifact creation between phases
- LoopRoleResponder interface
- PromptBuilder template rendering (iteration.md, reflection.md)
- LlmSwitcher role resolution

** Gate System
- GateEvaluator architecture
- GateRegistry per-loop gate storage
- Trigger matching via shouldFire()
- 4 executor types with their configs:
  - CommandGateExecutor: shell execution, passPattern, timeout
  - LlmReviewGateExecutor: criteria-based LLM assessment
  - ArtifactGateExecutor: file existence, regex, JSON schema
  - HumanGateExecutor: Promise.withResolvers, auto-approve timer
- FindingDedup for gate result deduplication
- Gate failure -> escalation flow

** Domain System
- LoopDomainRegistry architecture
- LoopDomainDefinition interface: name, description, guidelinesTemplate, defaultGates, evidenceCollector
- 6 built-in domains with their gate configurations
- How domains inject gates at loop start

** Persistence Layer (3 tiers)
- Checkpoint: state.json per loop, PersistedLoopState shape
- Event log: NDJSON event sourcing, appendLoopEvent, replayLoopEvents
- Org sync: .org file generation, state-to-keyword mapping (DOING/BLOCKED/DONE)
- Session hooks: restoreLoopSnapshots recovery flow

** Recursion System
- ChildSpawner depth enforcement (limit 3)
- LoopDag: cycle detection, addEdge, topologicalOrder
- applyChildCompletionPolicy: retry/block/skip/escalate
- collectKillTree: cascading kill traversal

** Safeguards
- Budget checking: wall-clock, tree iterations
- Runaway detection: progress hash comparison, idle iteration counter
- Kill switch: collectKillTree parent-child traversal
- Spec drift: hash-based file change detection during reflection

** Integration Points
- Session wiring: LoopManager on ToolSession
- Tool registration: conditional in tools/index.ts
- Slash command dispatch: executeLoopCommand
- Dashboard bridge: LoopDashboardBridge, event channels, panel lifecycle
- EventBus channels: loop:{id}:state, loop:{id}:iteration, loop:{id}:gate
- Prerequisites validation: modelRoles.review required

** Replay & Debug
- replayLoopState: reconstruct snapshot at any iteration
- formatLoopTimeline: event trace formatting
- handleLoopDebugCommand: filtered event inspection

** Constants Reference
- LOOP_SCHEMA_VERSION: "1.0.0"
- DEFAULT_LOOP_MAX_ITERATIONS: 200
- DEFAULT_LOOP_REFLECT_EVERY: 3
- DEFAULT_LOOP_DEPTH_LIMIT: 3
- DEFAULT_LOOP_CONCURRENCY_LIMIT: 2
- DEFAULT_HUMAN_GATE_TIMEOUT_MS: 300000 (5 min)
- DEFAULT_BUDGET_LIMITS: 4h wall-clock, 200 tree iterations, 5 idle iterations

* Acceptance Criteria
- File exists at =packages/coding-agent/src/loop/ARCHITECTURE.md=
- Covers all 12 content sections listed above
- Every file path, type name, and constant value matches the source code
- Includes the full ALLOWED_TRANSITIONS table
- Includes a directory map of all loop/ subdirectories and key files
- No invented behavior: every claim is traceable to the implementation
- Cross-references TUTORIAL.md where appropriate for user-facing usage
