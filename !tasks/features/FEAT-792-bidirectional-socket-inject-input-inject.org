#+TITLE: Bidirectional socket: inject_input + inject_ack frames
#+STATE: DOING
#+SESSION_ID: 14fbf5dd98cd8683
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-spell-spell--remote-2/2026-06-04T12-36-00-739Z_14fbf5dd98cd8683.jsonl]]
#+CUSTOM_ID: FEAT-792-bidirectional-socket-inject-input-inject

* Initial Message

You are in a repo that I would like you to use as a free reign to build a server that I can use to control **all** my sessions as long as one server is open in the computer.

You need to look at ../spell-remote-example, this is where the config will live.

I would like a web interface that is easy to use. For now, don't think about permissions and auth; we will end up just using Firezone for making a local network.

The web interface should contain everything needed to steer the control of all existing sessions on this computer. If I send a message from that interface to a running session, it needs to act as if I had sent it from the Terminal. You are free to experiment with whatever architecture makes sense, although the idea of interconnected sessions that know each other through sockets is what makes most sense to me.

I am going for lunch, so you will have to do a deep deep deep exploration on your own, build a wave manifest alongside FEAT and BUG recorded in a PLAN in waves (use the org tool), create mockups using GenerateUIScreen (for desktop and for mobile), and go through all the architectural challenges as you go. Once that is done and settled, write the org files and start work by yourself, continuing all the way to the final delivery. To help you out, use the main thread to do architectural work, and subtasks only for mechanical work. Use reviewer swarms in between waves to check your work, and surface, file, and fix/implement ANY and ALL FEAT/BUG that you find. Check-in in chat, showing what's going on, what's been done, and what you're going to do, so that I can read the whole conversation when I come back.

Go for it, godspeed!

:PROPERTIES:
:DEPENDS:
:END:

Part of [[id:PLAN-326-remote-control-server-steer-all-local-se]] — Wave 1 (spine).

* Scope

Extend the line-delimited JSON socket protocol so the server can push a free-form input
message to a registered external (terminal) session, and the client can acknowledge.

Two files MUST stay byte-identical for the shared types (there is a SYNC comment):
- =packages/spell-server/src/socket/types.ts=
- =packages/coding-agent/src/session-bridge/types.ts=

** New server→client message
=InjectInputSocketServerMessage=:
- =type: "inject_input"=
- =injectId: string= (server-generated correlation id)
- =text: string=
- =deliverAs: "steer" | "followUp" | "auto"= (auto = steer if streaming else normal submit)
- =timestamp: number=

Add to =SocketServerMessage= union + any =isSocketServerMessage= / type-guard surface.

** New client→server message
=InjectAckSocketClientMessage=:
- =type: "inject_ack"=
- =injectId: string=
- =accepted: boolean= (false if session could not accept, e.g. bash running / disposed)
- =reason?: string=
- =timestamp: number=

Add to =SocketClientMessage= union and to =SOCKET_CLIENT_MESSAGE_TYPES= set + =isSocketClientMessage=.

* Existing patterns

- Mirror the existing =EventResponseSocketServerMessage= (server→client) and
  =event_resolved= (client→server) shapes precisely.
- Keep the union additive; do not reorder or rename existing members.

* Tests

- =packages/spell-server/test= (or new =socket=-scoped test): =isSocketClientMessage= accepts
  =inject_ack=, rejects malformed.
- A round-trip type test that an =inject_input= object satisfies =SocketServerMessage=.

* Acceptance

- Both =types.ts= files contain identical new types (SYNC preserved).
- Type guards updated; =bun run check:ts= green.
- No behavior change yet (frames defined, not wired) — wiring is FEAT-793/794.

* Verification

- =bun run check:ts=
- =bun --cwd packages/spell-server test=
