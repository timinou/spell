#+TITLE: Secret Management with Age Encryption
#+STATE: DONE
#+SESSION_ID: 14ac2c51463b8cb3
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-spell/2026-04-03T12-12-59-484Z_14ac1e06b73b8ca4/9-CreateFeat4.jsonl]]
#+CUSTOM_ID: FEAT-269-secret-management-with-age-encryption
#+PRIORITY: A
#+LAYER: library
#+EFFORT: 4h
#+DEPENDS: FEAT-267-ssh-and-rsync-operations-module

NOTE [2026-04-03]: Completed: Age encrypt/decrypt, SSH stdin secret push with chmod 600 + atomic mv. 6 command builder tests + conditional integration test.

* Scope
- IN: Age CLI detection, decrypt .env.age files locally, push decrypted .env to remote via SSH stdin (no plaintext on disk), encrypt new .env.age from plaintext input, key recipient management
- OUT: SSH key management, server.kdl env() resolution (existing), secret rotation automation

* Tests
- File: packages/spell-deploy/test/secrets/age.test.ts
- Scenarios:
  1. Detect age binary in PATH
  2. Decrypt .env.age produces valid key=value pairs
  3. Encrypt roundtrip: plaintext → age → decrypt → matches
  4. Push via SSH stdin (mock SSH, verify piped content)
  5. Missing age binary produces clear error
  6. Invalid .env.age file (wrong key, corrupted) produces clear error
  7. Multiple recipients (team key distribution)

* Implementation
1. Define secret types (FEAT-269-secret-management-with-age-encryption::define-types)
   - File: packages/spell-deploy/src/secrets/types.ts
2. Write secret tests (FEAT-269-secret-management-with-age-encryption::secret-tests, depends on define-types)
   - File: packages/spell-deploy/test/secrets/age.test.ts
3. Implement age wrapper (FEAT-269-secret-management-with-age-encryption::implement-age, depends on secret-tests)
   - File: packages/spell-deploy/src/secrets/age.ts
   - Shell out to `age` CLI for encrypt/decrypt
   - Stream-based: decrypt to memory, pipe to SSH
4. Implement secret push (FEAT-269-secret-management-with-age-encryption::implement-push, depends on implement-age)
   - File: packages/spell-deploy/src/secrets/push.ts
   - Decrypt locally → pipe via SSH → write .env on remote
   - Atomic: write to .env.tmp → rename to .env

* Edge Cases
- age not installed: error with install instructions
- SSH key as age recipient (age supports this natively)
- .env.age with BOM or Windows line endings
- Empty .env.age file
- Remote .env permissions (0600, owned by service user)

* Acceptance Criteria
- Full encrypt/decrypt roundtrip works
- Secrets reach remote without plaintext on local disk (piped)
- Missing age binary gives actionable error
- Remote .env has correct permissions (0600)
