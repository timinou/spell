#+TITLE: Markdown frontmatter authored as ---kdl by default
#+STATE: ITEM
#+SESSION_ID: 14ea0e0f03e26235
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-spell/2026-05-21T14-49-03-247Z_14ea0e0f03e26235.jsonl]]
#+CUSTOM_ID: FEAT-755-markdown-frontmatter-authored-as-kdl-by

* Initial Message

the settings thing doesn't work. I'd like a simple toggle on top for user/project/session. right now, when I do settings, it just shows [user] ... and keeps printing it, fucking up the layout of everything. I also don't see settings persisted when they need to be added to the kdl (please reproduce the error)

investigate.

* Goal

Spell-authored markdown (=src/prompts/agents/*.md=, =modes/*.md=, =rules/*.md=, =skills/*.md=) uses =---kdl= frontmatter blocks, not =---= YAML.

The parser =src/utils/frontmatter.ts:119-160= already supports both delimiters. This is purely a content + authoring tool change.

* Surface

- =packages/coding-agent/src/cli/agents-cli.ts:55-68= — =toFrontmatter()= emits =---kdl= blocks
- Batch-migrate authored .md files, one commit per directory
- =packages/coding-agent/src/prompts/agents/frontmatter.md= rewritten to teach KDL

* Why retain YAML branch in parser

Skills, third-party plugins, and external content may still use =---=. The parser branch is small and benign. Only authoring switches.

* Tracking

Subsumed by =FULL_KDL_SPELL= plan, WAVE 3.
