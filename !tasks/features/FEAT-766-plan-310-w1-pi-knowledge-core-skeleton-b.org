#+TITLE: PLAN-310 W1 — pi-knowledge-core skeleton (bm25, graph, fusion, cache)
#+STATE: DONE
#+SESSION_ID: 14ea0d7add095343
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-spell/2026-05-21T14-46-31-540Z_14ea0d7add095343.jsonl]]
#+CUSTOM_ID: FEAT-766-plan-310-w1-pi-knowledge-core-skeleton-b
#+LAYER: natives
#+PARENT: PLAN-310-pi-knowledge-core-convergence-delete-tan
#+PRIORITY: #A

* Initial Message

Bro tantivy is super super overkill; what is its purpose in this codebase? is it actually being used in spell sessions? what is its
 promise, and can it be fulfilled more leanly in an environment in which many (tens) of sessions are running on the same laptop, across
 workspaces? the ONLY case where you're allowed to have tantivy is if it provides a REAL advantage, and in that case, memory and CPU
 usage needs to be taken care of, and the agent interface and tools need to reflect the promise actually delivered.

 This is an investigation session for now. Gather the information, learn online, and come back to me with real data

Look at the org tool from first principle, as an agentic memory+PRD system (look up online). org recall isn't used but maybe the plan is
 to actually either:
 have a simpler FTS that is plugged into the pi-org-* machinery that enables the kind of graph+FTS that Surreal was enabling, by
 bascially keeping the org files themselves as sources of truth, and pooling hte resources e.g. with a user-level daemon?

 Think from first principles. I want lower memory usage and actually used tools out of this. Just because it wasn't used in the past
 doesn't mean that it should be removed. This has a lot of potential, especially when we start leveraging more of the org syntax, to have
 a clean model out of this. Right now it's opaque and unfinished; i'd like something powerful, ergonomic for the agent, and where the
 text file is the source of truth. Thanks.

 push back and ask me questions

Wave 1 of PLAN-310. New crate =pi-knowledge-core= with the four shared modules. No consumer changes. Crate compiles + tests pass in isolation.

* Scope (6 sub-tasks)

- T1.1 Cargo scaffold + lib.rs re-exports (depended on by all others)
- T1.2 =bm25.rs= \u2014 extracted from =pi-code-graph::search::SearchIndex=, made generic over =Document=
- T1.3 =graph.rs= \u2014 petgraph wrapper with unified =EdgeKind=
- T1.4 =fusion.rs= \u2014 RRF + recency/backlinks/confidence
- T1.5 =cache.rs= \u2014 re-export =pi-workspace-cache=
- T1.6 Unit tests at leaf level (95% line coverage on bm25/graph/fusion)

* Gate

=cargo test -p pi-knowledge-core= green; =cargo check --workspace= green (no consumer change yet).

* Non-goals

- No usearch yet (W2)
- No bge-m3 retarget yet (W2.5)
- No consumer rewiring (W4, W5)
- No notify ingestion (W5)
- No embedder client (W3)

* Completion [2026-05-21]

W1 (skeleton) + W1.5 (review + 8-finding gap fix) landed together. 53 tests, clippy clean.

** Crate shape (final)

#+begin_src
crates/pi-knowledge-core/
\u251c\u2500\u2500 Cargo.toml          # petgraph 0.8 serde-1, serde_json, bincode 1.3, chrono 0.4, thiserror 2, tempfile (dev)
\u2514\u2500\u2500 src/
    \u251c\u2500\u2500 lib.rs          # module wiring; W2/W3/W5 stubs flagged
    \u251c\u2500\u2500 error.rs        # Error enum (io / bincode / cache / other)
    \u251c\u2500\u2500 bm25.rs         # SearchIndex<D>, Document trait, Unicode tokenizer
    \u251c\u2500\u2500 graph.rs        # TypedGraph<P, E>, EdgeKind union (25 + Other), Neighbor/PathStep
    \u251c\u2500\u2500 fusion.rs       # RRF + recency + backlinks + confidence; FusedHit::why
    \u2514\u2500\u2500 cache.rs        # re-export pi-workspace-cache + KnowledgeMeta + save_all
#+end_src

** Review (W1.5)

Reviewer artifact: =!tasks/plans/plan-artifacts/PLAN-310/W1-5-review.md=. Eight HIGH-confidence findings, all fixed. Two deviations from reviewer's exact patches, both for cleanliness:

1. =Neighbor= and =PathStep= made generic over =E= (=Neighbor<E = EdgeKind>=) rather than hardcoded; keeps the public surface consistent with =TypedGraph<P, E>=.
2. =bincode_round_trip= test switched to a typed payload (=MyStruct=) instead of =serde_json::Value= because bincode can't deserialize untyped =Value= without =deserialize_any= \u2014 the JSON-string workaround was solving exactly that; with the generic surface, callers carry their own typed payload.

LOW-confidence findings deferred to FUP (documented in the review's "Deferred to FUP" section).

** Forward references

- W2 (usearch swap) consumes =KnowledgeMeta::embedder_dim= for cache stale detection
- W4 (org-engine retarget) consumes =TypedGraph<P, E>= with org-specific =P=
- W5 (tantivy delete + ingest) consumes =save_all= for atomic multi-blob writes
- W6 (memory tool) consumes =Neighbor::via= and =PathStep::kind= for the "why this was returned" agent prompt

** Next

W2 \u2014 usearch swap-in.