#+TITLE: Demote Read Edit Write to standard tier
#+STATE: DONE
#+SESSION_ID: 14af0ad97271e2fa
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-spell/2026-04-05T18-44-38-985Z_14af0ad97271e2fa.jsonl]]
#+CUSTOM_ID: FEAT-350-demote-read-edit-write-to-standard-tier
#+EFFORT: 1h
#+PRIORITY: A
#+LAYER: coding-agent
#+DEPENDS: FEAT-349-rename-emacs-code-to-code-and-update-all

* Initial Message

make Emacs Code a core tool and the Edit/Write/Read tools standard. Tell me what needs to be added to the Emacs Code tool to make it feature-complete. Remember it uses combobulate so even for files like .md there's https://github.com/tree-sitter-grammars/tree-sitter-markdown and for typst too and whatnot. Also rename it to Code, not Emacs Code. Tell me everything that needs to be done to make it the premiere way to read and edit code, and Edit/Write/Read become fallbacks

* Context
Move =read=, =edit=, and =write= from =core= tier to =standard= tier in =TOOL_TIERS=. The =code= tool becomes the primary way to read and edit source files; =read=/=edit=/=write= become fallbacks for non-code resources, text-only edits, and new file creation.

Tier changes:
- =read=: =core= → =standard=
- =edit=: =core= → =standard=
- =write=: =core= → =standard=
- =code=: stays =core=

* Scope

** =packages/coding-agent/src/tools/index.ts=
Move three entries in =TOOL_TIERS= from core section to standard section:
#+begin_src typescript
// Before:
read: "core",
edit: "core",
write: "core",

// After (in standard section):
read: "standard",
edit: "standard",
write: "standard",
#+end_src

** =packages/coding-agent/test/tools/index.test.ts=
The test at line ~253 asserts "assigns core tier to essentials" — update the assertion set. These three tools should now appear in the standard tier assertions instead.

** System prompt (=packages/coding-agent/src/prompts/system/system-prompt.md=)
Update the Precedence section:
- Currently lists =read=, =edit= as "Specialized" (top priority). This now changes:
  - =code= should be listed as the primary tool for reading/editing source files
  - =read=, =edit=, =write= listed as fallbacks with clear guidance on when to use each
- Add a new subsection after "LSP knows; grep guesses" that establishes =code= as the premiere source file tool
- Update the "Search before you read" section to prefer =code read= over =read= for source files

* Behavioral Impact
The tier system controls description verbosity only — all tools stay active. Moving to =standard= means:
- Full descriptions still shown (standard tools are not compacted)
- No loss of functionality
- The system prompt wording changes to steer the agent toward =code= first

* Acceptance Criteria
- =bun check:ts= passes
- =bun test test/tools/index.test.ts= passes
- =getToolTier("read") === "standard"=
- =getToolTier("edit") === "standard"=
- =getToolTier("write") === "standard"=
- =getToolTier("code") === "core"=
- System prompt renders correctly with code tool guidance
