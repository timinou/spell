#+TITLE: Harden PLAN-101 cutover audit findings
#+STATE: DONE
#+SESSION_ID: 14ab788ccb62d28c
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-spell/2026-04-03T00-10-00-877Z_14ab788ccb62d28c.jsonl]]
#+CUSTOM_ID: PLAN-102-harden-plan-101-cutover-audit-findings


* Initial Message

Implement these audit recommendations from org://AUD-023-audit-implementation:

1. **What**: Action-only goals still do not execute through Spell Server; they only validate and then fail at runtime  
   **Where**: `packages/spell-server/src/executor/goal-executor.ts`, near `#executeAttempt()` and `resolveGoalPrompt()`  
   **Why**: `growth.discovery`, `growth.feed.send`, `growth.export.publish`, and `growth.curation.writeback` load successfully, but actual execution still goes through prompt-only flow. Any triggered action-only goal will throw instead of running, so the cutover contract is not actually honored by the live server.  
   **Effort**: `medium`

2. **What**: Telegram quick-action replay can leave completed items claimed  
   **Where**: `packages/spell-server/src/telegram/workflow-inbox.ts`, `applyTelegramQuickAction()`  
   **Why**: The helper claims the item before dedupe/state checks. A duplicated Telegram callback can re-claim an already-completed item and then return a duplicate result, leaving stale claim state behind. That can block later operators or create misleading shared state.  
   **Effort**: `small`

3. **What**: Duplicate workflow requests are not audited even though the audit contract defines `request-duplicate`  
   **Where**: `packages/spell-server/src/workflow/engine.ts`, top duplicate branch in `applyAction()`  
   **Why**: The engine returns `duplicate: true` but does not append a `request-duplicate` audit entry. This weakens the idempotency/audit story and makes duplicate replays invisible in the canonical history.  
   **Effort**: `trivial`

4. **What**: Coding-agent “goals” and “approvals” surfaces are helpers only, not real registered tools  
   **Where**: `packages/coding-agent/src/tools/goals-tool.ts`, `packages/coding-agent/src/tools/approvals-tool.ts`, and `packages/coding-agent/src/tools/index.ts`  
   **Why**: The new modules exist and are tested directly, but they are not wired into `BUILTIN_TOOLS`. In real sessions there is still no reusable goals/approvals tool surface for operators.  
   **Effort**: `small`

5. **What**: `growth.*` actions are registered without typed parameter schemas  
   **Where**: `packages/spell-growth/src/index.ts`, `GROWTH_ACTIONS`  
   **Why**: The manifest/action system now claims typed action validation, but all growth actions are registered as bare IDs with no param or prompt-slot contracts. Invalid growth action configs can pass load-time validation and fail only later.  
   **Effort**: `small`

6. **What**: Action parameter contract advertises `json` support that the manifest parser/serializer does not actually implement  
   **Where**: `packages/spell-server/src/actions/types.ts`, `packages/spell-server/src/manifest/parser.ts` near `parseActionNode()` / `resolveActionScalarValue()`, and `packages/spell-server/src/manifest/serializer.ts` near `appendActionChildren()`  
   **Why**: `ActionParameterType` includes `"json"`, but the parser only handles scalars and scalar lists, while the serializer stringifies objects on output. That means the public contract is misleading and round-trip behavior is lossy.  
   **Effort**: `small`

7. **What**: Approvals API accepts shallowly validated payloads and then casts them into workflow objects  
   **Where**: `packages/spell-server/src/http/routes/approvals.ts`, `parseCreatePayload()` and `parseActionPayload()`  
   **Why**: Only top-level fields are checked. Nested `actions`, `artifacts`, and related structures are trusted via casts. A malformed client can create broken workflow items that persist successfully and fail later in harder-to-debug ways.  
   **Effort**: `small`

8. **What**: Feed digest approval metadata is chunk-level and can point at the wrong item when a message contains multiple articles  
   **Where**: `packages/spell-growth/src/actions/feed-send.ts`, around `previousItem = input.items[messages.length] ?? item` and final inline keyboard construction  
   **Why**: The callback target is derived from message count rather than the actual items accumulated in the chunk. Once chunking happens, the approval button can map to the wrong article.  
   **Effort**: `small`

9. **What**: `spell-growth` depends on `spell-server` internals via relative source imports instead of a stable package boundary  
   **Where**: `packages/spell-growth/src/index.ts`, `packages/spell-growth/src/actions/discovery.ts`, `packages/spell-growth/src/actions/types.ts`, `packages/spell-growth/src/workflow/presets.ts`  
   **Why**: This creates brittle cross-package coupling to `packages/spell-server/src/...` layout. Refactors, packaging, or external consumption can break `spell-growth` even when the intended public contract has not changed.  
   **Effort**: `medium`

* Context
PLAN-101 (spell-growth config-native cutover) landed the manifest foundation, workflow engine, growth action pack, operator surfaces, and growth repo migration. A post-implementation audit (AUD-023) identified 9 findings ranging from trivial to medium effort. This plan addresses all 9 in priority order.

** Key decisions
- Action-only goal execution uses structured-prompt generation through the existing session/RPC flow rather than a parallel native execution path. True native execution is deferred.
- JSON action parameters are supported via a ~param-json~ node type that round-trips through ~JSON.parse~ / ~JSON.stringify~.
- Cross-package imports from spell-growth to spell-server are replaced with ~@oh-my-pi/spell-server~ workspace package imports, accepting the bidirectional workspace dependency (spell-server -> spell-growth for registration; spell-growth -> spell-server for types).
- Coding-agent tools are registered as conditional builtins gated on spell-server URL availability.

** Findings addressed
1. Action-only goals throw at runtime -> [[id:PROJ-026-implement-action-only-goal-execution-in]]
2. Telegram replay leaves stale claims -> [[id:BUG-123-fix-telegram-quick-action-replay-leaving]]
3. Duplicate requests not audited -> [[id:BUG-122-audit-duplicate-workflow-requests]]
4. Tools not registered as builtins -> [[id:FEAT-243-register-goals-and-approvals-as-coding-a]]
5. Growth actions lack param schemas -> [[id:BUG-124-add-typed-parameter-schemas-to-growth-ac]]
6. JSON param round-trip lossy -> [[id:BUG-125-fix-json-action-parameter-round-trip-los]]
7. Approvals API shallow validation -> [[id:BUG-126-validate-nested-approvals-api-payloads-b]]
8. Feed chunking points to wrong article -> [[id:BUG-127-fix-feed-digest-approval-button-targetin]]
9. Relative cross-package imports -> [[id:PROJ-025-replace-spell-growth-relative-imports-wi]]

* Verification
** Focused test commands
- ~bun test packages/spell-server/test/workflow/idempotency.test.ts~ (finding 3)
- ~bun test packages/spell-server/test/telegram/approval-callbacks.test.ts~ (finding 2)
- ~bun test packages/spell-growth/test/action-registration.test.ts~ (findings 5, 9)
- ~bun test packages/spell-growth/test/feed-action.test.ts~ (finding 8)
- ~bun test packages/spell-server/test/manifest/action-registry.test.ts~ (finding 6)
- ~bun test packages/spell-server/test/http/approvals-api.test.ts~ (finding 7)
- ~bun test packages/coding-agent/test/tools/goals-tool.test.ts packages/coding-agent/test/tools/approvals-tool.test.ts~ (finding 4)
- ~bun test packages/spell-server/test/executor/goal-executor.test.ts~ (finding 1)

** Negative proof
- ~grep -r 'spell-server/src/' packages/spell-growth/src/~ returns no matches after finding 9.
- No ~"does not execute yet"~ string remains in ~resolveGoalPrompt~ after finding 1.

** Regression
- ~bun test ../growth/test/cutover/manifest-load.test.ts ../growth/test/cutover/discovery-to-feed.test.ts ../growth/test/cutover/checkpoint-to-export.test.ts ../growth/test/cutover/shared-surface-state.test.ts ../growth/test/cutover/no-legacy-bridge.test.ts~ still passes.

* Execution Manifest
** trivial wave (no blockers)
- [[id:BUG-122-audit-duplicate-workflow-requests]] Audit duplicate requests (1h)

** small-fixes wave (no blockers, parallelizable)
- [[id:BUG-123-fix-telegram-quick-action-replay-leaving]] Fix Telegram replay stale claims (2h)
- [[id:BUG-124-add-typed-parameter-schemas-to-growth-ac]] Add growth action param schemas (2h)
- [[id:BUG-125-fix-json-action-parameter-round-trip-los]] Fix JSON param round-trip (2h)
- [[id:BUG-126-validate-nested-approvals-api-payloads-b]] Validate nested approvals payloads (2h)
- [[id:BUG-127-fix-feed-digest-approval-button-targetin]] Fix feed chunking button target (2h)

** cross-package wave (depends on BUG-124)
- [[id:PROJ-025-replace-spell-growth-relative-imports-wi]] Replace relative imports with package boundary (3h, depends BUG-124)

** execution wave (depends on PROJ-025, BUG-124)
- [[id:PROJ-026-implement-action-only-goal-execution-in]] Implement action-only goal execution (6h, depends PROJ-025 + BUG-124)

** tools wave (no blockers from above)
- [[id:FEAT-243-register-goals-and-approvals-as-coding-a]] Register goals/approvals as coding-agent tools (3h)


** Completion [2026-04-02]
*** Verification
- `bun test packages/spell-server/test/workflow/idempotency.test.ts` -> passed (`1 pass`, `0 fail`).
- `bun test packages/spell-server/test/telegram/approval-callbacks.test.ts` -> passed (`2 pass`, `0 fail`).
- `bun test packages/spell-growth/test/action-registration.test.ts` -> passed (`1 pass`, `0 fail`).
- `bun test packages/spell-growth/test/feed-action.test.ts` -> passed (`2 pass`, `0 fail`).
- `bun test packages/spell-server/test/manifest/action-registry.test.ts` -> passed (`5 pass`, `0 fail`).
- `bun test packages/spell-server/test/http/approvals-api.test.ts` -> passed (`4 pass`, `0 fail`).
- `bun test packages/coding-agent/test/tools/goals-tool.test.ts packages/coding-agent/test/tools/approvals-tool.test.ts` -> passed (`5 pass`, `0 fail`).
- `bun test packages/spell-server/test/executor/goal-executor.test.ts` -> passed (`11 pass`, `0 fail`).
- `grep 'does not execute yet' packages/spell-server/src/executor/goal-executor.ts` -> no matches found.
- `grep 'spell-server/src/' packages/spell-growth/src/` -> no matches found.
- `bun test ../growth/test/cutover/manifest-load.test.ts ../growth/test/cutover/discovery-to-feed.test.ts ../growth/test/cutover/checkpoint-to-export.test.ts ../growth/test/cutover/shared-surface-state.test.ts ../growth/test/cutover/no-legacy-bridge.test.ts` -> passed (`5 pass`, `0 fail`).
*** Artifacts
- None. No UI/browser artifacts were required for this plan.
*** Deviations
- None.
