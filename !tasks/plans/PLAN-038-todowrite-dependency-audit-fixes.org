#+TITLE: TodoWrite Dependency Audit Fixes
#+STATE: DONE
#+SESSION_ID: 14a70c37602039a2
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-spell/2026-03-30T13-42-46-400Z_14a70c37602039a2.jsonl]]
#+CUSTOM_ID: PLAN-038-todowrite-dependency-audit-fixes


NOTE [2026-03-30]: All 5 audit findings implemented. 2 code fixes + 4 new tests (3 specified + 1 for BUG-023 behavioral change).

* Initial Message

Implement these audit recommendations from org://AUD-004-audit-implementation:

Good — the parser already has coverage for BLOCKER extraction. The missing test is specifically the end-to-end "create org item with BLOCKER property, read it back" flow, which tests the org *write* path, not just the *parser*.

---

Here are my findings:

## Audit Findings

### 1. Gate rejection silently drops non-status field updates
- **What**: When the smart gate rejects an `in_progress` transition, the `break` exits the entire `update` case, discarding any co-submitted field changes (content, notes, details, blockers, etc.) without mentioning them in the error.
- **Where**: `packages/coding-agent/src/tools/todo-write.ts` ~line 373 (the `break` statement)
- **Why**: A caller sending `{op: "update", id: "task-2", status: "in_progress", notes: "starting this"}` gets an error about the status, but the notes update is silently lost. The whole op is rejected, which is defensible, but the error message only mentions the status transition — the caller doesn't know other fields were dropped. Low real-world impact since agents rarely combine `in_progress` with other fields, but it's a contract nuance.
- **Effort**: small (apply non-status fields before the gate check, or mention "entire update op rejected" in the error)

### 2. "Errors: Warning:" double prefix on dangling ref messages
- **What**: Dangling blocker ref warnings use the format `"Warning: task-1 references non-existent blocker task-99"` and are pushed into the `errors` array, which renders as `"Errors: Warning: task-1 references..."`.
- **Where**: `packages/coding-agent/src/tools/todo-write.ts` ~line 416
- **Why**: The model sees a slightly confusing "Errors: Warning:" prefix. Not user-visible (model text only), but muddies the distinction between real errors and warnings.
- **Effort**: trivial (drop the "Warning:" prefix from the message, or use a different rendering path for warnings)

### 3. Missing test: `remove_task` creating dangling blocker ref
- **What**: The plan specified testing that `remove_task` on a task referenced as a blocker by another task produces a dangling-ref warning. This test was not implemented.
- **Where**: `packages/coding-agent/test/tools/todo-write.test.ts`
- **Why**: Low risk — the dangling ref validation is a single post-ops pass that doesn't depend on which op caused the dangle. But the plan explicitly specified this scenario.
- **Effort**: trivial

### 4. Missing test: `add_phase` with `in_progress` blocked task demotion
- **What**: The plan specified testing that `add_phase` ops with `in_progress` and unresolved blockers get demoted by `normalizeInProgressTask`. Only `replace` is tested.
- **Where**: `packages/coding-agent/test/tools/todo-write.test.ts`
- **Why**: Low risk — both `replace` and `add_phase` go through the same `normalizeInProgressTask` demotion pass. But the plan explicitly specified both.
- **Effort**: trivial

### 5. Missing test: BLOCKER property round-trip integration
- **What**: FEAT-098 plan specifies an integration test: create org item with `properties: { BLOCKER: "FEAT-001-test" }`, read it back via `org get`, verify `parseOrgDependProperties()` extracts blockers. Not implemented.
- **Where**: Should be in `packages/coding-agent/test/plan-mode/plan-mode-approved-prompt.test.ts` or a new test file
- **Why**: The org-depend parser already has unit test coverage for BLOCKER extraction. The missing test covers the org *write* → *read* path specifically, which is already proven by other org tests. Low risk since no code changes were needed to the org infrastructure.
- **Effort**: small

* Context

Post-implementation audit of PLAN-036 (TodoWrite Dependency Hardening) identified five findings. All are low-risk polish items — no behavioral regressions, no broken contracts. Three are code/test fixes, two are test-only additions.

*Key decisions*:
- Finding 1 (gate rejection drops fields): Fix by reordering field application before the gate check. The =probe= spread becomes unnecessary since =task.blockers= is already updated. Net code simplification.
- Finding 2 (double prefix): Drop the "Warning:" prefix — the =errors= array already renders as "Errors: ...", so the prefix is redundant.
- Findings 3-5 (missing tests): Add the three tests specified in PLAN-036 child items. All test existing behavior — no production code changes needed. The BLOCKER round-trip test uses the parser directly (no org server needed) since the parser contract is what the prompt relies on.

*Ordering*: BUG-023 and BUG-024 first (they change code and test assertions), then FEAT-103 (adds tests that depend on updated assertion format).

* Verification

#+begin_src shell
# All affected tests
bun test packages/coding-agent/test/tools/todo-write-gates.test.ts
bun test packages/coding-agent/test/tools/todo-write.test.ts
bun test packages/coding-agent/test/loop/org-depend-parser.test.ts

# Type check
bun check:ts
#+end_src

Manual verification:
- Read gate rejection test to confirm non-status fields are preserved
- Read dangling ref warning to confirm no "Warning:" prefix

* Execution Manifest

** Phase 1: Code fixes (parallelize — non-overlapping changes)
- [[id:BUG-023-gate-rejection-applies-non-status-fields]] — Reorder field application in =update= case (30m), no dependencies
- [[id:BUG-024-dangling-blocker-ref-warning-uses-confus]] — Drop "Warning:" prefix from dangling ref message (15m), no dependencies

** Phase 2: Missing tests
- [[id:FEAT-103-add-missing-todowrite-dependency-tests-f]] — Add 3 missing tests from PLAN-036 spec (30m), depends on BUG-023 and BUG-024 for assertion format

Total effort: ~1h15m


** Completion [2026-03-30]

*** Verification

All three test suites pass:

- =bun test packages/coding-agent/test/tools/todo-write.test.ts= — 19 pass, 0 fail (37 expects)
- =bun test packages/coding-agent/test/tools/todo-write-gates.test.ts= — 36 pass, 0 fail (62 expects)
- =bun test packages/coding-agent/test/loop/org-depend-parser.test.ts= — 12 pass, 0 fail (31 expects)
- =bun check:ts= — clean (1466 files, no fixes)

Manual verification:
- Gate rejection test confirms =task.notes= is ="starting this"= while =task.status= remains =pending= — non-status fields preserved on gate rejection
- Dangling ref message is now ="task-1 references non-existent blocker task-99"= — no "Warning:" prefix

*** Artifacts

Code changes:
- =packages/coding-agent/src/tools/todo-write.ts= — reordered field application (BUG-023), dropped Warning: prefix (BUG-024)
- =packages/coding-agent/test/tools/todo-write.test.ts= — updated assertion, added 4 new tests
- =packages/coding-agent/test/loop/org-depend-parser.test.ts= — added 1 new test

*** Deviations

Added an extra test (=gate rejection preserves co-submitted field updates=) beyond the 3 specified in FEAT-103, since BUG-023 introduced a behavioral change that needed its own test coverage. This test directly validates the BUG-023 fix.
