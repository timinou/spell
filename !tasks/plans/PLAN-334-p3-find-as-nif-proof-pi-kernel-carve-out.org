#+TITLE: P3: find-as-NIF proof — pi-kernel carve-out + OwnerId + rustler skin + 3 gates (WS-B derisk)
#+STATE: DONE
#+SESSION_ID: 1503ad6112fe682a
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-spell/2026-06-10T12-27-09-771Z_1503ad6112fe682a.jsonl]]
#+CUSTOM_ID: PLAN-334-p3-find-as-nif-proof-pi-kernel-carve-out
#+COMPLETION_HASH: 16e9b8d5be48a18d

* Initial Message

P3 (NIF): Can you try it out? Will the warm kernel keep mini-sessions separate? What else besides improvements can it do? Is it strong improvements?
W4 (stored programs): explain what that means please, what are the possibilities? How hard is the integration?
Deferred, documented: D3, E4: what does it do?

Think deeply

* What this is

The load-bearing WS-B spike: expose ONE tool (`find`, the pure-kernel read path) to a
BEAM runtime via rustler, over the SAME `execute_code_path_inner` the NAPI uses, and
prove three contracts. Per spec N-1 this is NOT a toy — its scope includes carving a
host-agnostic pi-kernel crate. A green P3 means the extraction is DONE, not just derisked.

Reads with: specs/beam-orchestrator/01-vision.md (target arch, the contracts),
03-roadmap.md (P3 section + dependency graph), 06-execute-substrate.md (N-1/N-2/N-3).

* Honest framing (decided — do not re-litigate)

P3 is a PERFORMANCE + ARCHITECTURE bet, NOT error-relief. W2's offload already killed
the heap-error class OVER THE EXISTING BRIDGE — the big term is parked once in the Peer
store and never crosses to the sandbox. The NIF's marginal win there is one JSON+pipe
round per large result, not a correctness fix. P3's UNIQUE value:
  1. one warm kernel, N agents (the code-graph index — 100s of MB — built ONCE/node,
     shared; today every Node session re-indexes).
  2. true zero-copy handles (D-2 phase 2 / N-3): a term lives in Rust, projected via
     NIF calls, bytes copied once at final materialize.
  3. lock-liveness across a fleet (OwnerId + monitor → agent crash anywhere reclaims locks).
∴ sequence P3 for the WS-B prize. It gates NOTHING we ship now (W4/D3/the BUGs are
all independent). Do it when WS-B is the goal, not to fix today's errors.

* Verified preconditions (all GREEN — this is why P3 is reachable)

- Host-neutral entry EXISTS: `pub fn execute_code_path_inner(...)` at
  crates/pi-natives/src/code_path/napi.rs:396. NAPI is a thin skin OVER it; rustler is
  a SECOND skin over the SAME fn — not a reimplementation.
- Greenfield: grep `rustler` across crates → 0 hits; no pi-kernel crate exists.
  Nothing to undo; pure addition.
- Toolchain present: cargo + elixir + mix + erl all on PATH.
- ISOLATION PRIMITIVE ALREADY EXISTS (the biggest unknown, now answered): in
  crates/pi-code-engine/src/buffer.rs the BufferRegistry partitions by an OPAQUE
  `session_id: String` — edits keyed by it (L119/225/316), undo/redo SESSION-SCOPED
  (`undo_scoped(session_id)` L245), conflict detection keys off `entry.session_id != sid`
  (L385). ∴ "will the warm kernel keep mini-sessions separate?" → YES, by construction
  already shipped. N-1's OwnerId is a RENAME (session_id → opaque owner token; today a
  Node session id, tomorrow a BEAM pid string) + a reclaim HOOK, not a redesign.

* The three acceptance gates (this IS the acceptance — "find works" is NOT)

1. correctness   NIF find returns the SAME chunks as the NAPI path for a corpus of
                 targets (paths, globs, slices, symbols).
2. panic-safety  a deliberately panicking find → {:error, ...}; the BEAM NODE SURVIVES.
                 (NAPI panic kills 1 Node proc; NIF panic kills the WHOLE node → every
                 NIF boundary MUST catch_unwind → {:error}. This is the new contract.)
3. lock-liveness owning BEAM proc killed mid-op → file lock reclaimed via process
                 monitor + ResourceArc Drop calling the OwnerId reclaim hook.

* Scope (N-1 — the kernel split is PART of P3)

- Carve a host-agnostic `pi-kernel` crate out of pi-natives (napi.rs is already a
  marshalling skin; this makes that boundary a CRATE boundary). Audit that no
  orchestration/state logic leaked into the NAPI layer; if it did, extract to core
  first and LIST what moved (the "kernel_truth: unsure" item from the roadmap).
- OwnerId abstraction in the lock table: owner = opaque token; reclaim hook the host
  registers (Node: async-context teardown; BEAM: monitor + ResourceArc Drop).
- rustler skin over execute_code_path_inner; the three gates proven against the REAL
  core, not a throwaway.
- Scheduler classification: tag which kernel ops are <1ms (regular sched) / DirtyCpu
  (resolve/edit/graph) / must-not-NIF (external-proc). find-read is DirtyCpu.

* N-2 (interim read scoping — keep in this plan, do not skip)

While Node remains session owner (pre-P5), the NIF lane serves ONLY non-index,
non-mutable paths: file reads/slices, org queries, memory search. Graph edges
(def→/ref→), ALL writes, anything touching buffer_registry or undo history stay
bridge-served. Zero index duplication, zero undo-history forking. Cutover of graph +
writes is P5's job, not P3's.

* N-3 (handles → ResourceArc — the payoff, after gates green)

Once NIF reads land, swap the HandleStore backing ETS → ResourceArc: tool results live
in Rust; handle-aware builtins project via NIF calls; transfer cost for served paths →
0. The LANGUAGE SEMANTICS (D-2) DO NOT CHANGE — this is why W2 and P3 ran in parallel
without invalidating each other. The HandleStore behaviour (put/project/realize/
release/rehome/stats) is already backing-agnostic by design.

* Files (indicative)

- NEW crates/pi-kernel/ (carved from pi-natives).
- crates/pi-natives/src/code_path/napi.rs — becomes a thin re-export over pi-kernel.
- crates/pi-code-engine/src/buffer.rs — session_id → OwnerId + reclaim hook.
- NEW beam-side rustler NIF crate (beam/ or a new crate) + Elixir wrapper.
- crates/pi-edit-broker/ — OwnerId reclaim wiring.

* Acceptance

3 gates green against the EXTRACTED pi-kernel (not a throwaway); a killed BEAM owner
has its lock reclaimed via the OwnerId hook; kernel-cleanliness audit says core is
clean OR lists exactly what to extract; scheduler classification documented.

* Relationships

- Gates P5 (tool surface → NIF) and thereby P6 (subagents on BEAM). Gates NOTHING in
  the execute-substrate error work. P0' (fup-095-nuke-lsp-tool merge) gates only the
  lsp slice of P5, not P3. Multi-day/greenfield — sequence behind W4/D3 unless WS-B is
  the active goal.

* ARCHITECTURE PASS COMPLETE (2026-06-12) → spec 09-p3-pi-kernel-carve-out.md

File-verified boundary audit done (NOT via the explorer agent, which had web-only
tools + no local access; all GT below checked by direct file read). Execution spec:
specs/beam-orchestrator/09-p3-pi-kernel-carve-out.md.

Verified ground truth (GT-1..9):
- GT-2: CodePathTaskOptions (napi.rs:212) has ONLY #[allow(dead_code)]+#[derive(Default)],
  NO napi derive → moves to pi-kernel as-is.
- GT-3: CancelToken struct (task.rs:108) = {Option<Instant>, Arc<Flag>} plain; only
  ::new takes napi Unknown/AbortSignal → struct moves, ctor stays in skin.
- GT-4: CodePathChunk (napi.rs:79) IS #[napi(object)] → the ONE real refactor: kernel
  returns a plain KernelChunk twin, each skin maps twin→DTO.
- GT-5: clean read/write branch in execute_code_path_inner — manage(L405)/edit(L499)/
  else=read. Read path is NIF-eligible; one branch point, no tangle.
- GT-6: scheme dispatch (L951) — native declarative schemes need NO callback; only
  runtime TS schemes do. N-2 read lane uses declarative only → NIF needs no callback bridge.
- GT-7: read-path statics share-safe (FS_CACHE DashMap concurrent; *_LEXER OnceLock
  read-only-after-init). Two-kernels RAM hazard = the code-graph index, which N-2 excludes.
- GT-9 (BIGGEST): TWO lock layers. (a) file_lock::with_exclusive_lock = RAII-scoped,
  auto-released, NO reclaim needed. (b) edit-broker intent/registration = persistent,
  session-keyed, and ALREADY self-reclaims (conn.rs:134 deregister-on-disconnect +
  TTL'd record_intent). ∴ gate-3 reclaim hook = wire BEAM process-monitor death to the
  broker's EXISTING deregister, NOT new lock machinery. Much smaller than the plan feared.

∴ OwnerId = a newtype RENAME of session_id (GT-8 confirmed the partition exists) +
reclaim(owner) delegating to broker deregister. Build order P3.1 (carve) → P3.2
(OwnerId+reclaim) → P3.3 (rustler, gate1) → P3.4 (panic, gate2) → P3.5 (liveness,
gate3) → P3.6 (docs). Next: P3.1 the carve.

* PROGRESS (2026-06-12): P3.1 + P3.2 + P3.3a SHIPPED (reviewer-gated, committed)

3 milestones done, each reviewer-clean + committed:
- P3.1 (a732eae0c): carved pi-kernel crate (host-agnostic read resolver). NativeResolver
  newtype (Deref) carries the write methods (orphan-rule-safe). N-2 PROVEN: zero
  napi/rustler/buffer in pi-kernel dep tree. pi-kernel 25 tests, pi-natives 435+, green.
- P3.2 (a45428986): OwnerId(String) newtype on CoordClient lock-table params +
  reclaim() trait method (no-op stub). #[serde(transparent)] = wire byte-identical
  (broker protocol untouched). EMPIRICAL: today's SocketCoordClient sends Bye after
  every op (nothing held across calls) \u2192 functional reclaim needs the persistent-conn
  client (P3.5), reclaim is a documented stub now.
- P3.3a (2b1727710): carved the parse path (dialect_registry + select_lexer +
  read-dispatch) into pi-kernel `resolve_target(registry,target,root,extractors,cancel)
  -> ResolveOutput{nodes,diagnostics}`. GATE 1 (IN-PROCESS) PROVEN: 7 parity tests
  (plain/glob/slice/symbol/outline/glob-diag + semantic-exclusion) assert byte-identical
  nodes AND diagnostics vs execute_code_path_inner. Reviewer-gated, fixed P1 (stray
  erl_crash.dump) + P2 (semantic-dispatch guard).

REMAINING: P3.3b (rustler NIF + Elixir wrapper + cross-runtime gate 1) \u2192 P3.4 (panic
catch_unwind, gate 2) \u2192 P3.5 (functional reclaim atop persistent client, gate 3) \u2192
P3.6 (docs). All three need a LIVE BEAM rustler NIF.

ENVIRONMENT NOTE: the pi-natives INTEGRATION-TEST binaries (worker_mode/semantic_live_e2e/
edge_dispatch_e2e) hit a linker OOM (ld signal 7 Bus error) building in parallel \u2014 the
~40 tree-sitter language crates + napi make them huge. A rustler NIF .so over pi-kernel
links LESS (pi-kernel dep tree 315 lines vs pi-natives 1340; 25 ts-language crates via
pi-code-engine builtins vs ~40), and 20Gi RAM is free, so a single NIF link built ALONE
is plausibly feasible \u2014 but the heavy-link risk is real and P3.3b should build the NIF
in isolation (not alongside the integration tests). The decision point: P3.3b is a
greenfield Mix project + rustler crate + cross-runtime test, the spike's BEAM half.

NOTE [2026-06-12]: P3 COMPLETE 2026-06-12. All 3 gates green, reviewer-gated (4 review passes: P3.1, P3.2, P3.3a, final P3 series), 6 commits (a732eae0c..e952ac98f). pi-kernel carved (host-agnostic, N-2 isolation proven), OwnerId on the lock table (wire-neutral), resolve_target parse+read-dispatch (in-process parity 7/7 byte-identical to NAPI), rustler NIF skin on live BEAM OTP 28 (cross-runtime gate-1 6/6), panic-safety gate-2 (catch_unwind, BEAM survives \u2014 kills error class #4), lock-liveness gate-3 (dead owner\u2192broker reclaim\u2192reacquire). Final review correct conf 0.85. DEFERRED to future BEAM-integration phase (honestly scoped in spec 09): NIF-side :DOWN-monitor\u2192ResourceArc-Drop trigger (socket-per-owner; the broker reclaim it relies on IS proven), napi read branch calling resolve_target (proven-equivalent twins today), and P5 (graph+writes cutover, index moves wholesale resolving two-kernels RAM, N-3 HandleStore ETS\u2192ResourceArc zero-copy). The warm-kernel WS-B architecture is de-risked end-to-end: one kernel, two skins (NAPI+rustler), proven identical.

* P3 FULLY FINISHED (2026-06-12) — P3.7 cutover + P3.8 functional gate-3

The two items I'd scoped as "P3 finish" (NOT P5) are now DONE + reviewer-gated:

- P3.7 (598e6002c, 400c0f630): CUTOVER. NAPI execute_code_path_inner read branch
  now DELEGATES to pi_kernel::resolve_target \u2014 the carve is ADOPTED, the duplicate
  read-dispatch is DELETED, single source of truth. Kept semantic+#diff host-only
  checks first. resolve_target gained gitignore:Option<bool> (a real fidelity seam
  the cutover surfaced). Behavior-preserving proven by ABSOLUTE anchors (routing 22,
  op_matrix 2, unified 9 \u2014 the napi-new==napi-old proof, not just cross-skin parity).
  Reviewer correct conf 0.82; 1 P3 fixed (pre-delegate host-abort bridge; full
  mid-walk = FUP-132).

- P3.8 (c9507f230, f64df8c39, bac994dee): FUNCTIONAL gate-3. beam/pi_kernel_nif
  BrokerConnection rustler resource holds a per-owner broker connection;
  env.monitor + Resource::down fire on the owner's :DOWN \u2192 close \u2192 broker reclaim.
  Proven END TO END (broker_reclaim_test.exs): A holds, A killed, B acquires.
  Reviewer caught the credibility gap (test couldn't isolate :DOWN monitor from
  ResourceArc GC-drop) \u2192 STRENGTHENED: parent pins A's resource term so GC can't
  reclaim \u2192 ONLY the monitor can close \u2192 now genuinely proves the :DOWN path.
  Also fixed DirtyIo + spec shape. 8/8 NIF tests.

EMPIRICAL note for any future BEAM work: System.put_env on the Elixir side does
NOT reach a NIF's std::env::var_os \u2014 pass paths as NIF args. And building anything
in pi-natives' dep closure needs NK_TARGET_DIAMOND=0 (numkong/GCC16 -mavx10.2-512).

∴ P3 is COMPLETE: kernel carved + ADOPTED (one read path, two skins), 3 gates green
INCLUDING the functional BEAM trigger. Only P5 (graph+writes cutover, index
wholesale move, N-3 zero-copy) + FUP-132 (mid-walk abort) remain \u2014 genuinely later.