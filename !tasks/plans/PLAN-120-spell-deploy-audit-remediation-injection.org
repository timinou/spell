#+TITLE: Spell-deploy audit remediation injection hardening and DRY cleanup
#+STATE: DONE
#+SESSION_ID: 14ac30ed20bb8cd1
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-spell/2026-04-03T13-35-34-018Z_14ac30ed20bb8cd1.jsonl]]
#+CUSTOM_ID: PLAN-120-spell-deploy-audit-remediation-injection


* Initial Message

Implement these audit recommendations from org://AUD-032-audit-implementation:

`sanitizeUnitName` is only in `systemd.ts`. The `lifecycle.ts` `buildInstallUnitCommand` takes raw `unitName` and interpolates it directly into a shell string without sanitization. And `buildServiceCommand` does `sudo systemctl ${action} ${unitName}` — also unsanitized. Since unit names come from user config (sync.kdl `service unit="spell-growth"`), a malicious value like `foo; rm -rf /` would be injectable.

Now I have enough findings. Let me compile the report.

---

## Audit Findings

### 1. Duplicated `buildQuotedPath` and `executeRsync` helpers
- **What**: Identical implementations of `buildQuotedPath()` and `executeRsync()` exist in both `push.ts` and `pull.ts`
- **Where**: `packages/spell-deploy/src/sync/push.ts:5-19` and `packages/spell-deploy/src/sync/pull.ts:5-19`
- **Why**: Violates DRY-at-2 rule from AGENTS.md. When one copy gets fixed (e.g., a bug in quoting or error reporting), the other stays broken. Extract to a shared `sync/utils.ts`.
- **Effort**: `trivial`

### 2. Barrel uses `export type { ... } from` instead of `export *`
- **What**: `src/index.ts` uses `export type { DeployContext } from "./cli/types"` and named re-exports from `./sync/types`, violating AGENTS.md star re-export rule
- **Where**: `packages/spell-deploy/src/index.ts:6,22-30`
- **Why**: The named re-export exists to avoid a `CommandResult` name collision between `cli/types.ts` and `sync/types.ts`. The fix is to remove the dead `CommandResult` from `cli/types.ts` (it's unused), then use `export *` for both modules.
- **Effort**: `trivial`

### 3. Dead `CommandResult` interface in `cli/types.ts`
- **What**: `CommandResult` is defined in `cli/types.ts` but never imported or used anywhere
- **Where**: `packages/spell-deploy/src/cli/types.ts:10-13`
- **Why**: Dead code that caused the barrel to use named re-exports to avoid collision. Delete it.
- **Effort**: `trivial`

### 4. `execSsh` is `async` but uses `Bun.spawnSync`
- **What**: Function is declared `async` and returns `Promise<CommandResult>` but only uses synchronous `Bun.spawnSync`. The `async` keyword is misleading.
- **Where**: `packages/spell-deploy/src/sync/ssh.ts:40`
- **Why**: Contract confusion — callers `await` a function that never yields. Either drop `async`/`Promise` (if intentional sync) or switch to `Bun.spawn` with `await proc.exited` (if truly async is desired). Currently both `push.ts` and `pull.ts` call `executeRsync` (which is sync) but then `await execSsh` for swap commands — the mixture is inconsistent.
- **Effort**: `small`

### 5. Shell injection in `initCommand` — unquoted `projectRoot`
- **What**: `initCommand` interpolates `target.projectRoot` directly into a remote shell command without quoting: `mkdir -p ${target.projectRoot} ...`
- **Where**: `packages/spell-deploy/src/cli/commands.ts:138`
- **Why**: If `projectRoot` contains spaces or shell metacharacters, the command breaks or executes unintended code. The push/pull modules use `buildQuotedPath` for this exact reason, but the init command skips it.
- **Effort**: `small`

### 6. Shell injection in `buildSecretPushCommand` — unquoted `remotePath`
- **What**: `remotePath` and `remoteTmp` are interpolated into a remote shell command without quoting: `cat > ${remoteTmp} && chmod 600 ${remoteTmp} && mv ...`
- **Where**: `packages/spell-deploy/src/secrets/push.ts:10-11`
- **Why**: Same quoting issue as finding 5. Paths with spaces would break the remote command.
- **Effort**: `small`

### 7. Unsanitized `unitName` in `lifecycle.ts` remote commands
- **What**: `buildServiceCommand` and `buildInstallUnitCommand` interpolate `unitName` into shell strings without sanitization, while `systemd.ts` has a `sanitizeUnitName` function that only runs in the unit generator
- **Where**: `packages/spell-deploy/src/service/lifecycle.ts:7,27`
- **Why**: A unit name from config containing shell metacharacters would be injected into `sudo systemctl ${action} ${unitName}` and `sudo tee ${remotePath}`. Since values come from user-controlled `sync.kdl`, this is a defense-in-depth gap. Apply `sanitizeUnitName` at the boundary or when building the command.
- **Effort**: `small`

### 8. `BundleUploadOptions` type is exported but never used
- **What**: `BundleUploadOptions` interface exists in `bundle/types.ts` but no function signature references it
- **Where**: `packages/spell-deploy/src/bundle/types.ts:20-24`
- **Why**: Dead type. `buildUploadCommands` uses an inline opts type instead. Either use the exported type or delete it.
- **Effort**: `trivial`

### 9. SSH key path with spaces breaks rsync `-e` transport
- **What**: `buildRsyncSshTransport` interpolates `sshOptions.sshKey` into a single string passed as rsync's `-e` flag. If the key path contains spaces, the resulting ssh command becomes malformed because rsync parses the `-e` value with shell word splitting.
- **Where**: `packages/spell-deploy/src/sync/rsync.ts:4`
- **Why**: Paths like `~/.ssh/my key` would produce `-e "ssh -p 22 -i /home/user/.ssh/my key ..."` where `key` becomes a separate argument. Should quote the key path.
- **Effort**: `small`

### 10. `shouldExclude` is too broad — matches prefix rather than path segments
- **What**: `shouldExclude("node_modules_backup/foo", ["node_modules/"])` returns `true` because the check `filePath.startsWith(normalized)` matches `node_modules` as a prefix of `node_modules_backup`. The `||` branch `filePath.startsWith(normalized)` subsumes and weakens the segment-aware `filePath.startsWith(normalized + "/")` branch.
- **Where**: `packages/spell-deploy/src/sync/watcher.ts:17`
- **Why**: False-positive exclusions — files in directories whose names begin with an excluded prefix are silently skipped from sync.
- **Effort**: `small`

* Context

** Problem
Post-implementation audit of =packages/spell-deploy/= (PLAN-119) found 10 issues across two categories:

1. *Shell injection / quoting gaps (5 findings, priority A)*: User-controlled config values (=projectRoot=, =unitName=, =remotePath=, =sshKey=) are interpolated into remote shell commands without quoting or sanitization. The push/pull modules already use =buildQuotedPath= for this, but other modules skip it. =sanitizeUnitName= exists in =systemd.ts= but is not applied in =lifecycle.ts=.

2. *DRY violations and dead code (5 findings, priority B)*: =buildQuotedPath= and =executeRsync= are duplicated in push.ts and pull.ts. Dead types exist (=CommandResult= in cli/types, =BundleUploadOptions= in bundle/types). Barrel uses named re-exports instead of =export *=. =shouldExclude= has a false-positive prefix bug. =execSsh= is misleadingly async.

** Approach
Two sequential child items:
1. *BUG-142 (DRY cleanup)* — Extract shared utilities, remove dead code, fix =shouldExclude= and =execSsh=. This creates the shared =sync/utils.ts= that the injection fixes need.
2. *BUG-141 (Injection hardening)* — Apply quoting/sanitization everywhere, using the shared =buildQuotedPath= from step 1.

** Key Decisions
- =buildQuotedPath= and =executeRsync= move to =packages/spell-deploy/src/sync/utils.ts=
- =sanitizeUnitName= becomes a named export from =systemd.ts= (was private)
- =execSsh= drops =async= — it uses =Bun.spawnSync= so the promise wrapper is misleading. Callers that =await= it still work (=await= on non-promise is no-op).
- =shouldExclude= fix: remove the third =||= branch. Correct logic is =startsWith(normalized + \"/\") || equals normalized=.
- Dead types deleted outright (=CommandResult= in cli/types, =BundleUploadOptions= in bundle/types).
- Barrel converts fully to =export *= after removing the =CommandResult= name collision.

** Files Modified
All changes scoped to =packages/spell-deploy/=:

| File | Change |
|------+--------|
| =src/sync/utils.ts= | NEW — shared =buildQuotedPath=, =executeRsync= |
| =src/sync/push.ts= | Remove local helpers, import from utils |
| =src/sync/pull.ts= | Remove local helpers, import from utils |
| =src/sync/ssh.ts= | Drop =async= from =execSsh= |
| =src/sync/rsync.ts= | Quote SSH key path in =-e= transport |
| =src/sync/watcher.ts= | Fix =shouldExclude= prefix matching |
| =src/cli/types.ts= | Remove dead =CommandResult= |
| =src/cli/commands.ts= | Quote =projectRoot= in =initCommand= |
| =src/bundle/types.ts= | Remove dead =BundleUploadOptions=, unused import |
| =src/secrets/push.ts= | Quote =remotePath= in shell commands |
| =src/service/systemd.ts= | Export =sanitizeUnitName= |
| =src/service/lifecycle.ts= | Sanitize =unitName= in commands |
| =src/index.ts= | Convert to =export *=, add =sync/utils= |
| =test/sync/watch.test.ts= | Add =shouldExclude= regression test |
| =test/service/systemd.test.ts= | Add sanitization tests |
| =test/secrets/age.test.ts= | Add quoting test |
| =test/sync/rsync.test.ts= | Add SSH key quoting test |

* Verification

** Type Check
#+begin_src shell
cd packages/spell-deploy && bunx tsgo -p tsconfig.json 2>&1 | grep -v logger.ts
#+end_src
Must produce zero errors for spell-deploy files.

** Package Tests
#+begin_src shell
bun test packages/spell-deploy/test/
#+end_src
All tests pass, including new regression tests.

** Lint
#+begin_src shell
bunx biome check packages/spell-deploy/
#+end_src
Clean (no errors).

** Specific Regression Checks
- =shouldExclude("node_modules_backup/foo", ["node_modules/"])= returns =false=
- =buildServiceCommand= with =unitName= containing =;= produces sanitized output
- =buildSecretPushCommand= with =remotePath= containing spaces produces quoted output
- rsync =-e= transport with SSH key containing spaces produces quoted output
- =initCommand= =mkdir -p= with =projectRoot= containing spaces produces quoted output

* Execution Manifest

** wave-1: DRY cleanup                                              :wave:
- [[id:BUG-142-dry-violations-and-dead-code-cleanup-in]] DRY violations and dead code cleanup (1h)

** wave-2: Injection hardening                                      :wave:
- [[id:BUG-141-shell-injection-and-quoting-gaps-in-spel]] Shell injection and quoting gaps (2h, depends BUG-142)

** Effort Summary
| Wave | Items | Total Effort |
|------+-------+--------------|
| wave-1 | 1 | 1h |
| wave-2 | 1 | 2h |
| *Total* | *2* | *3h* |

** Parallelization Notes
- Wave 1 must complete first: wave 2 imports =buildQuotedPath= from the =sync/utils.ts= that wave 1 creates.
- Within each wave, work is a single item (no intra-wave parallelism needed).

** Completion [2026-04-03]

*** Verification
- =bun test packages/spell-deploy/test/=: 68 pass, 1 skip, 0 fail (69 tests across 9 files)
- =bunx tsgo -p tsconfig.json=: 0 spell-deploy errors (1 pre-existing error in =packages/utils/src/logger.ts= outside scope)
- =bunx biome check packages/spell-deploy/=: Clean, no fixes applied
- =shouldExclude(\"node_modules_backup/foo\", [\"node_modules/\"])= returns =false= (confirmed by new test)
- =buildServiceCommand= with =unitName=\"foo; rm -rf /\"= produces =sudo systemctl start foo-rm--rf-= (no semicolons, confirmed by new test)
- =buildSecretPushCommand= with =remotePath=\"/srv/my app/.env\"= produces properly quoted paths (confirmed by new test)
- rsync =-e= transport with =sshKey=\"/home/user/.ssh/my key\"= produces ='-i /home/user/.ssh/my key'= (confirmed by new test)
- =initCommand= =mkdir -p= now uses =buildQuotedPath= for =projectRoot=

*** Artifacts
- =packages/spell-deploy/src/sync/utils.ts= — new shared module with =buildQuotedPath= and =executeRsync=
- =packages/spell-deploy/test/sync/watch.test.ts= — =shouldExclude= regression test added
- =packages/spell-deploy/test/service/systemd.test.ts= — sanitization regression tests added
- =packages/spell-deploy/test/secrets/age.test.ts= — path quoting regression test added
- =packages/spell-deploy/test/sync/rsync.test.ts= — SSH key quoting regression test added

*** Deviations
None.
