#+TITLE: Spell server dashboard audit fixes
#+STATE: DONE
#+SESSION_ID: 14ac5caee772dbc3
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-spell/2026-04-03T16-46-44-637Z_14ac5caee772dbc3.jsonl]]
#+CUSTOM_ID: PLAN-125-spell-server-dashboard-audit-fixes

NOTE [2026-04-03]: All 3 child items completed. 28 tests pass. No new type errors.


* Initial Message

Implement these audit recommendations from org://AUD-033-audit-implementation:

`ManifestGoal.stateStores` is a `Map` — `JSON.stringify` will serialize it as `{}` (empty object), silently losing the data. Same for `ManifestSetup.stateStores`. The `Object.fromEntries(manifest.goals)` in `toManifestJson` converts the top-level Map, but the nested Maps inside each goal/setup remain as Maps and won't serialize correctly.

This was actually a pre-existing issue (the old code also did `Object.fromEntries(manifest.goals)` without handling nested Maps), but the new manifest fields also have the same risk. Let me check the setup serialization:

The `toManifestJson` does `setups: Object.fromEntries(manifest.setups)` which converts the top-level Map, but each `ManifestSetup` value may contain `stateStores: Map<string, NamedStateStore>`. `JSON.stringify` on a Map produces `{}`. This is pre-existing but worth noting.

Let me now compile my findings:

---

## Audit Findings

### 1. Two existing tests broken by manifest API change

- **What**: `test/http/frontend.test.ts` has 2 failing tests: (a) the manifest API test expects only `name/version/setups/goals` but now gets 8 additional arrays; (b) the frontend HTML test expects the old auth prompt text "Enter the dashboard Basic Auth credentials to load goals, runs, and manifest data." but the new frontend says "Enter Basic Auth credentials to access the dashboard."
- **Where**: `packages/spell-server/test/http/frontend.test.ts`, lines 122 and 133
- **Why**: These tests fail right now. Any CI run will catch this. The commit shipped broken tests.
- **Effort**: `trivial` — add the 8 empty arrays to the expected manifest object; update the expected auth prompt string

### 2. `StateStoreManager` constructor throws on missing SQLite file

- **What**: `new Database(resolvedPath, { readonly: true })` throws a synchronous error if the file doesn't exist. The constructor has no try/catch. If a KDL manifest declares a state store pointing to a not-yet-created database, the entire server startup crashes.
- **Where**: `packages/spell-server/src/state/store-manager.ts`, line 39
- **Why**: Production crash if the state store path is wrong or the DB hasn't been created yet. Startup failure prevents the entire server from running, including goals unrelated to state stores.
- **Effort**: `small` — wrap in try/catch, log warning, skip the connection

### 3. State store manager only collects from setups, misses goal-level state stores

- **What**: The constructor iterates `manifest.setups.values()` but `ManifestGoal` also has an optional `stateStores` field. Goal-level state stores are silently ignored.
- **Where**: `packages/spell-server/src/state/store-manager.ts`, lines 29-41
- **Why**: If a project declares state stores at goal level, they won't appear in the API. Currently growth doesn't use goal-level state stores, so no immediate impact, but the contract is incomplete.
- **Effort**: `small` — add a second loop over `manifest.goals.values()`

### 4. `queryTable` columns come from schema, not actual DB — possible mismatch

- **What**: `queryTable` returns `columns: table.columns.map(c => c.name)` from the KDL schema, but `rows` come from `SELECT *` on the actual SQLite table. If the schema is out of sync with the real table (e.g., a column was added via migration but not declared in KDL), the `columns` array won't match the row keys, and the frontend will render blank cells for undeclared columns.
- **Where**: `packages/spell-server/src/state/store-manager.ts`, lines 95 and 87-89
- **Why**: Silent data loss in the UI. Users see rows with data in columns not listed in the header.
- **Effort**: `small` — derive columns from `Object.keys(rows[0])` when rows exist, fall back to schema

### 5. `renderCellValue` double-parses JSON objects

- **What**: For strings starting with `{`, the function calls `JSON.parse(value)` on line 984 to validate, then calls `JSON.parse(value)` again on line 985 inside the template. Wastes CPU on every JSON cell render.
- **Where**: `packages/spell-server/src/http/frontend/index.html`, lines 984-985
- **Why**: Minor perf issue. Doubles parse cost for every JSON-valued cell in the state store data browser.
- **Effort**: `trivial` — capture the result of the first parse

### 6. `handleTableCount` runs a full `SELECT *` query just for count

- **What**: `handleTableCount` calls `manager.queryTable(storeName, tableName, 0, 0)` which executes both `SELECT * ... LIMIT 0 OFFSET 0` and `SELECT count(*)`. The `LIMIT 0` query is harmless but unnecessary. A dedicated count method would be cleaner.
- **Where**: `packages/spell-server/src/http/routes/state.ts`, line 51
- **Why**: Negligible impact since `LIMIT 0` returns no rows, but the pattern is misleading — it looks like it queries data when it only needs a count.
- **Effort**: `trivial` — either accept the current approach (it works) or add a `countTable` method

### 7. `toManifestJson` serializes Maps inside goals/setups as `{}`

- **What**: `Object.fromEntries(manifest.goals)` converts the top-level Map, but each `ManifestGoal` and `ManifestSetup` contains nested `stateStores: Map<string, NamedStateStore>`. `JSON.stringify` on a `Map` produces `{}`. State stores declared at goal/setup level are silently lost in the API response.
- **Where**: `packages/spell-server/src/http/routes/goals.ts`, lines 24-25
- **Why**: Frontend consumers never see state store configurations per goal/setup. This is pre-existing (the old code had the same issue), but now that the manifest API is the primary data source for the dashboard, the information gap is more visible.
- **Effort**: `medium` — need a recursive serializer that converts nested Maps to plain objects

### 8. No tests for the state store query API

- **What**: No test file for `state.ts` routes or `store-manager.ts`. The state store API was verified manually via curl but has no automated test coverage.
- **Where**: Missing `packages/spell-server/test/http/state.test.ts` and `packages/spell-server/test/state/store-manager.test.ts`
- **Why**: Regression risk — future changes to routing or query logic have no safety net. The FEAT-292 acceptance criteria called for tests.
- **Effort**: `medium` — need a fixture SQLite DB, tests for happy path, 404s, pagination, count

* Context

Post-implementation audit of PLAN-121 (spell-server dashboard overhaul) found 8 issues across 3 categories:

** Broken tests + silent data loss (Critical)
- Two tests in =frontend.test.ts= fail: manifest API response now includes 8 new arrays, auth prompt text changed.
- =toManifestJson()= and =handleGetGoal()= pass =Map= fields (=ManifestSetup.stateStores=, =ManifestGoal.stateStores=) to =Response.json()=. =JSON.stringify= on a =Map= produces ={}=, silently dropping all state store config from the API response.

** StateStoreManager robustness (High)
- Constructor throws on missing SQLite file (crashes entire server)
- Goal-level stateStores not collected (only setup-level)
- =queryTable()= columns from KDL schema may not match actual DB
- =renderCellValue= double-parses JSON objects
- =handleTableCount= uses full query instead of count-only

** No automated test coverage (Medium)
- State store API has zero tests. Manual curl verification only.

** Key decisions
- Add a recursive =serializeValue()= in =goals.ts= that converts nested Maps to plain objects. Scoped to the two known Map fields (=stateStores= on setups/goals); all other manifest types use arrays/primitives.
- Constructor wraps =new Database()= in try/catch, logs warning, skips connection. The store still appears in the API (it was declared) but queries return empty results.
- Column derivation: use =Object.keys(rows[0])= when rows exist, fall back to schema columns when table is empty.
- Test fixture: in-memory SQLite DB via =new Database(\":memory:\")= for unit tests, temp file for HTTP integration tests.

** Critical file paths
- =packages/spell-server/src/http/routes/goals.ts= — =toManifestJson()=, =handleGetGoal()=
- =packages/spell-server/src/state/store-manager.ts= — constructor, =queryTable()=, new =countTable()=
- =packages/spell-server/src/http/routes/state.ts= — =handleTableCount()=
- =packages/spell-server/src/http/frontend/index.html= — =renderCellValue()=
- =packages/spell-server/test/http/frontend.test.ts= — broken test expectations
- =packages/spell-server/test/state/store-manager.test.ts= — new
- =packages/spell-server/test/http/state-api.test.ts= — new
- =packages/spell-server/test/http/workflow-test-helpers.ts= — extend =startWorkflowHttpServer= or create new helper for state store

* Verification

#+begin_src sh
# Fix verification — all tests pass
bun test packages/spell-server/test/http/frontend.test.ts
bun test packages/spell-server/test/state/store-manager.test.ts
bun test packages/spell-server/test/http/state-api.test.ts

# Type check — no new errors
cd packages/spell-server && bunx --bun tsgo --noEmit

# Manifest Map serialization — stateStores not empty
# (manual or test): create manifest with stateStores Map, confirm API returns plain object not {}

# Server startup resilience — missing DB doesn't crash
# (test): StateStoreManager constructor with non-existent path logs warning, doesn't throw
#+end_src

* Execution Manifest

** Wave 1: Fix broken tests + Map serialization (sequential)
- [[id:BUG-147-fix-broken-frontend-tests-and-manifest-m]] Fix broken frontend tests and manifest Map serialization (2h, backend, #A)

  Fixes the two shipping test failures and the silent Map-to-JSON data loss. Must land first because later items add tests that assume a passing baseline.

** Wave 2: Harden StateStoreManager (depends on Wave 1)
- [[id:BUG-148-harden-statestoremanager-robustness]] Harden StateStoreManager robustness (2h, backend, #B)

  Constructor error handling, goal-level store collection, column derivation fix, countTable method, renderCellValue double-parse fix. Depends on Wave 1 because test expectations must be correct first.

** Wave 3: Test coverage (depends on Wave 2)
- [[id:FEAT-297-add-state-store-api-test-coverage]] Add state store API test coverage (3h, test, #B)

  Unit tests for StateStoreManager and integration tests for state routes. Depends on Wave 2 because it tests the hardened behavior (constructor resilience, column derivation, countTable).

Total effort: ~7h. All sequential due to dependencies.

** Completion [2026-04-03]

*** Verification

All 8 audit findings addressed. Verification evidence:

- =bun test packages/spell-server/test/http/frontend.test.ts= — 2 pass, 0 fail (15 expect calls)
- =bun test packages/spell-server/test/state/store-manager.test.ts= — 17 pass, 0 fail (32 expect calls)
- =bun test packages/spell-server/test/http/state-api.test.ts= — 9 pass, 0 fail (17 expect calls)
- =bunx --bun tsgo --noEmit= — no new type errors (pre-existing errors in approvals.ts, tool-progress.ts, logger.ts unchanged)
- Combined run: 28 tests pass across 3 files in 187ms

*** Artifacts

Files modified:
- =packages/spell-server/test/http/frontend.test.ts= — updated auth prompt string and manifest API expectations
- =packages/spell-server/src/http/routes/goals.ts= — added =serializeValue()= recursive Map serializer, applied to =toManifestJson()= and =handleGetGoal()=
- =packages/spell-server/src/state/store-manager.ts= — try/catch in constructor, goal-level store collection, column derivation from rows, =countTable()= method
- =packages/spell-server/src/http/routes/state.ts= — =handleTableCount()= uses =countTable()=
- =packages/spell-server/src/http/frontend/index.html= — =renderCellValue()= double-parse fixed

Files created:
- =packages/spell-server/test/state/store-manager.test.ts= — 17 unit tests
- =packages/spell-server/test/http/state-api.test.ts= — 9 integration tests

*** Deviations

None.
