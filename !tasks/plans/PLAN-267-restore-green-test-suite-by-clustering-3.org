#+TITLE: Restore green test suite by clustering 34 coding-agent test failures into 12 BUGs
#+STATE: ITEM
#+SESSION_ID: 14c0a2c7e0b0df60
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-spell/2026-04-19T10-42-37-570Z_14c0a2c7e0b0df60.jsonl]]
#+CUSTOM_ID: PLAN-267-restore-green-test-suite-by-clustering-3
#+LAYER: packages/coding-agent
#+PRIORITY: #A

* Initial Message

these tests are failing:

✗ taskToolRenderer report_finding safety > renders abort reason inline for aborted subagent results [1.00ms]
✗ taskToolRenderer report_finding safety > renders efficiency summaries for completed multi-task batches
✗ TaskTool blocker integration > async execution schedules dependents only after predecessors finish [817.00ms]
✗ runSubprocess tool call budget > aborts when tool count exceeds budget and still accepts submit_result reminder recovery [3.00ms]
✗ runSubprocess tool call budget > does not abort when subagent completes within budget
✗ runSubprocess tool call budget > disables enforcement when task.maxToolCalls is set to 0 [1.00ms]
✗ runSubprocess tool call budget > does not abort when tool count is exactly at maxToolCalls
✗ runSubprocess tool call budget > uses default maxToolCalls=200 when not explicitly configured [1.00ms]
✗ runSubprocess delegated model fallback > falls back to the next allowed candidate after startup auth failure [2.00ms]
✗ runSubprocess delegated model fallback > keeps the root auth error when no alternate candidate exists [1.00ms]
✗ python tool docs template > renders dynamic helper docs [1.00ms]
✗ ExitPlanModeTool validation > rejects child items with empty body [5.00ms]
✗ ExitPlanModeTool validation > rejects child items with body under 100 characters
✗ ExitPlanModeTool validation > reports all missing top-level properties in one error [19.00ms]
✗ ExitPlanModeTool validation > rejects broken top-level DEPENDS references [2.00ms]
✗ ExitPlanModeTool validation > rejects child items missing structured sub-outline CUSTOM_IDs [1.00ms]
✗ ExitPlanModeTool validation > rejects sub-outline CUSTOM_IDs outside the owning child namespace
✗ ExitPlanModeTool validation > rejects broken sub-outline DEPENDS references [1.00ms]
✗ ExitPlanModeTool validation > rejects duplicate sub-outline CUSTOM_IDs within a child item
✗ ExitPlanModeTool validation > rejects cyclic sub-outline DEPENDS graphs [1.00ms]
✗ ExitPlanModeTool validation > reports thin bodies and missing properties together
✗ ExitPlanModeTool validation > reports broken top-level deps and bad sub-outline namespaces together [1.00ms]
✗ ExitPlanModeTool validation > reports issues across multiple child items in one response
✗ ExitPlanModeTool validation > accepts valid top-level and sub-outline dependencies within the plan [1.00ms]
✗ code.ts transpiles cleanly under Bun.Transpiler [1.00ms]
✗ coding-agent code tool wiring > routes existing-file edit through freshness diff before save [3.00ms]
✗ coding-agent code tool wiring > routes strict-target create through list then save without diff [1.00ms]
✗ applyManagedBufferContent > includes the native per-file error message in the thrown error [1.00ms]
✗ applyManagedBufferContent > does not throw on successful writes (parseable replacement)
✗ evaluateWriteGuards contract > allows writes that grow a file [175.00ms]
✗ parseArgs --canvas > defaults to fluid when --canvas has no value [4.00ms]
✗ parseArgs --canvas > uses fluid when next arg is another flag
✗ canvas CLI messages > unknown canvas message includes supported modes and usage syntax
✗ SwarmBlackboard > does not emit artifact events without DATA_URI and preserves metadata in reads [13.00ms]
✗ SubagentViewerComponent > forwards tool events to event handler [1.00ms]



Please find the root causes and present them to me. Explain the TDD way of fixing them by cluster. DO not implement yet, just explore

* Context
The coding-agent package has 34 failing tests spread across 14 test files. Exploration showed they cluster into 12 distinct root causes, each independent of the others. No inter-cluster dependencies exist \u2014 all 12 BUGs can proceed in parallel. The plan groups them into 2 waves only to separate trivial mechanical fixes from structural/production changes, so Wave 1 can start without waiting on Wave 0 if preferred, but Wave 0 first produces fast early wins with the lowest risk.

Failing test inventory (34 tests):

| Cluster | Tests | BUG |
|---------|-------|-----|
| Mock session missing refreshBaseSystemPrompt | 7 | [[id:BUG-294-task-executor-crashes-on-mock-sessions-m]] |
| harness-bug-repro helper produces invalid TS | 1 | [[id:BUG-295-harness-bug-repro-bigparseablesource-pro]] |
| ExitPlanModeTool tests lack org.enabled | 12 | [[id:BUG-296-exitplanmodetool-validation-tests-never]] |
| SwarmBlackboard.read drops entries | 1 | [[id:BUG-297-swarmblackboard-read-returns-only-the-la]] |
| Async blocker scheduling stalls | 1 | [[id:BUG-298-async-task-blocker-scheduling-stalls-aft]] |
| SubagentViewer theme singleton uninitialized | 1 | [[id:BUG-299-subagentviewercomponent-depends-on-unini]] |
| Task renderer missing abort/efficiency details | 2 | [[id:BUG-300-task-renderer-omits-abortreason-and-effi]] |
| code-module-integrity test path wrong | 1 | [[id:BUG-301-code-module-integrity-test-resolves-code]] |
| Canvas defaults + usage-syntax order | 3 | [[id:BUG-302-canvas-default-is-fluid-align-parseargs]] |
| applyManagedBufferContent bypasses callCodeBuffer | 2 | [[id:BUG-303-applymanagedbuffercontent-bypasses-callc]] |
| Python tool docs destructured-import mock leak | 1 | [[id:BUG-304-python-tool-docs-binding-defeats-vi-spyo]] |
| Code tool tests miss coord_peer_activity call | 2 | [[id:BUG-305-code-tool-wiring-tests-miss-the-post-sav]] |

** Settled design decisions
1. **One BUG per cluster**; C7 and C8 merged because they encode the same decision (fluid is the default canvas).
2. **BUG-305 (C12)**: peer activity is part of the edit flow's observable side effects \u2014 production code unchanged; tests widen to expect the trailing `coord_peer_activity` call.
3. **BUG-299 (C10)**: structural DI refactor \u2014 `viewer-component.ts` receives `theme` via its existing `SubagentViewerContext` seam instead of reading the uninitialized module singleton.
4. **BUG-304 (C13)**: minimal fix \u2014 switch `python.ts` to namespace import so `vi.spyOn` works.
5. **BUG-303 (C5)**: full cutover \u2014 `applyManagedBufferContent` routes through `callCodeBuffer` with a required `SessionIdSource`; all 4 call sites updated; no parallel APIs left behind.
6. **BUG-300 (C3)**: additive rendering only; existing "Total:" line untouched.

** Key file touchpoints
- Test-only edits: packages/coding-agent/test/{task,tools,modes,cli,swarm}/
- Production edits: packages/coding-agent/src/{task/render.ts, task/index.ts, tools/managed-code-buffer.ts, patch/index.ts, tools/write.ts, cli/args.ts, main.ts, tools/python.ts, modes/components/subagent-viewer/*, swarm/blackboard.ts}
- Possibly packages/org/src/native-ops.ts or crates/pi-natives (only if BUG-297 diagnosis lands there).

* Verification
Final verification gate (applies to all waves):
#+BEGIN_SRC bash
bun test packages/coding-agent/
#+END_SRC
Expect:
- All 34 previously failing tests pass.
- No previously passing test regresses.
- No unexpected change to the test count.

Per-BUG verification commands are embedded in each BUG item's `verify-green` sub-outline.

Type safety gate (after Wave 1 completes):
#+BEGIN_SRC bash
bun check:ts
#+END_SRC
Expect: no new type errors. BUG-299 (context type change) and BUG-303 (signature change) carry the highest type-level risk.

Not run during implementation (per AGENTS.md rules): `bun run dev`, any release scripts, any git commits. Gates use the focused commands.

* Execution Manifest
** :wave: Wave 0 \u2014 mechanical fixes (parallel, low risk)
Seven BUGs. Each touches 1-3 files with no cross-coupling. Agents can run simultaneously under `task` isolation. Estimated: XS/S per BUG.

- [[id:BUG-301-code-module-integrity-test-resolves-code]] \u2014 test path fix
- [[id:BUG-302-canvas-default-is-fluid-align-parseargs]] \u2014 string literal fixes (2 files)
- [[id:BUG-294-task-executor-crashes-on-mock-sessions-m]] \u2014 mock session additions (2 files)
- [[id:BUG-296-exitplanmodetool-validation-tests-never]] \u2014 settings init in test factory
- [[id:BUG-295-harness-bug-repro-bigparseablesource-pro]] \u2014 test helper rewrite
- [[id:BUG-304-python-tool-docs-binding-defeats-vi-spyo]] \u2014 namespace import in python.ts
- [[id:BUG-305-code-tool-wiring-tests-miss-the-post-sav]] \u2014 widen mock chain + expectations

** :wave: Wave 1 \u2014 structural and production changes (parallel, higher risk)
Five BUGs. Each carries a structural production change or an invariant investigation. All are still independent of each other \u2014 they can run in parallel, but grouping them together isolates the ones that most deserve a focused reviewer.

- [[id:BUG-300-task-renderer-omits-abortreason-and-effi]] \u2014 additive renderer extensions
- [[id:BUG-303-applymanagedbuffercontent-bypasses-callc]] \u2014 wrapper cutover + 4 call sites
- [[id:BUG-299-subagentviewercomponent-depends-on-unini]] \u2014 theme DI refactor
- [[id:BUG-298-async-task-blocker-scheduling-stalls-aft]] \u2014 scheduler bug fix
- [[id:BUG-297-swarmblackboard-read-returns-only-the-la]] \u2014 diagnose + fix at invariant site
