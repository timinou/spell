#+TITLE: Replace Emacs code intelligence with native engine
#+STATE: DONE
#+SESSION_ID: 14b6d50334ba84c7
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-spell/2026-04-11T19-57-16-882Z_14b6d50334ba84c7.jsonl]]
#+CUSTOM_ID: PLAN-183-replace-emacs-code-intelligence-with-nat
#+TODO: INIT(I) ITEM(i) DOING(d) REVIEW(r) | DONE(D) BLOCKED(b)


* Initial Message

Emacs (spell-emacs-a09f6243d02d): installing tree-sitter grammars on first run, may take ~60s...


do we even need that anymore? look at the recent commits in the crates

* Context

The =code= tool currently delegates all file-local code intelligence (read, outline, navigate, edit, diff) to an Emacs 29+ daemon running tree-sitter + combobulate, communicating via JSON-RPC over socat. This requires:
- Emacs 29+ with =--with-tree-sitter= build flag
- =socat= binary for JSON-RPC transport
- Tree-sitter grammar compilation on first run (~60s startup delay)
- A long-lived daemon process with circuit-breaker lifecycle management

The new Rust crate =pi-code-engine= provides a complete native replacement:
- Tree-sitter grammars for TypeScript, Rust, Python, Elixir compiled into the binary
- Rope-based buffer management with undo/redo history
- All file-local operations: outline, resolution-aware read, navigate (node-at, defun-at, parent, siblings, children, references), structural edits (replace, insert, kill, splice, drag, clone, transpose), diff, save
- NAPI binding =executeCodeBuffer= already exists in =pi-natives=

The org tool runs a separate Emacs daemon for org-mode operations — it is unaffected by this migration.

** Key Decisions

1. *Thin sync wrapper*: Direct NAPI call from CodeTool, no worker thread. Typical parse is <10ms.
2. *Auto-open buffers*: NAPI layer auto-opens on first access via =get_or_open= semantics. Agent never calls open/close.
3. *No Emacs fallback*: Unsupported languages return error. Agent uses =read= tool for non-TS/Rust/Python/Elixir files.
4. *Remove code warmup*: No =emacsSessionManager=, no =warmupCode=, no daemon lifecycle for code tool. Native engine is instant.
5. *Clean pi-emacs*: Delete code-intel exports (client.ts, types.ts, createCodeTool, warmupCode). Keep daemon/detection/session-manager for org.
6. *Rename =references-local= to =references=*: Update system prompt and CodeTool wrapper.
7. *Drop =install_grammar=*: Grammars are compiled in. Keep read-only =languages= command listing built-in profiles.
8. *Buffer lifecycle*: Native BufferRegistry persists buffers across calls. Agent sees =buffers=, =diff=, =undo=, =redo=, =save=.

* Verification

** Type check
#+begin_src sh
bun check:ts
#+end_src

** Rust check
#+begin_src sh
bun check:rs
#+end_src

** Unit tests
#+begin_src sh
bun test packages/coding-agent/test/tools/code.test.ts
bun test packages/emacs/test/session-manager.test.ts
bun test packages/emacs/test/daemon-survival.test.ts
bun test packages/emacs/test/detection.test.ts
#+end_src

** Rust tests
#+begin_src sh
cargo test -p pi-code-engine
cargo test -p pi-natives
#+end_src

** Absence checks
- =grep -r "createCodeTool" packages/coding-agent/src/= returns zero hits
- =grep -r "warmupCode" packages/coding-agent/src/= returns zero hits
- =grep -r "emacsSessionManager" packages/coding-agent/src/= returns zero hits
- =grep -r "references-local" packages/coding-agent/src/prompts/= returns zero hits
- =grep -r "install_grammar" packages/coding-agent/src/prompts/= returns zero hits

** Org tool sanity
- Org tool still works: org daemon starts independently, org MCP operations unaffected

* Execution Manifest

** Wave 1 — Foundation (parallel)

- [[id:FEAT-446-native-code-buffer-typescript-wrapper-in]] — Create =code-buffer= TypeScript wrapper in pi-natives + add =languages= command and auto-open to Rust NAPI (2h)
- [[id:FEAT-450-update-system-prompt-for-native-code-eng]] — Update code tool system prompt: =references-local= → =references=, remove Emacs references, drop =install_grammar= (30m)

** Wave 2 — Rewire (depends on FEAT-446)

- [[id:FEAT-447-rewire-codetool-to-native-engine]] — Rewire CodeTool to call =executeCodeBuffer= instead of Emacs, update schema, rewrite tests (3h). Depends on FEAT-446.

** Wave 3 — Remove SDK code warmup (depends on FEAT-447)

- [[id:FEAT-448-remove-code-warmup-and-emacssessionmanag]] — Remove code warmup from sdk.ts, remove =emacsSessionManager= from ToolSession (1h). Depends on FEAT-447.

** Wave 4 — Slim pi-emacs (depends on FEAT-448)

- [[id:FEAT-449-slim-pi-emacs-package-to-org-only]] — Delete code-intel exports from pi-emacs, remove dead tests (2h). Depends on FEAT-448.

** Wave 5 — Deferred cleanup

- [[id:FUP-045-clean-up-dead-elisp-code-intelligence-fi]] — Remove dead Elisp files (1h). Depends on FEAT-449.

** Total effort: ~9.5h

** Completion [2026-04-11]

*** Verification

All verification steps from the plan have been executed:

**** Type check
=bun check:ts= passes cleanly (1893 files, no errors).

**** Rust check
=cargo fmt -p pi-natives -- --check= passes after formatting.
=cargo clippy -p pi-natives -- -D warnings= passes.
Note: =bun check:rs= has a pre-existing formatting failure in =crates/pi-code-engine/src/buffer.rs= (not modified by this plan).

**** Unit tests
- =bun test packages/coding-agent/test/tools/code.test.ts= — 11 tests pass
- =bun test packages/emacs/test/session-manager.test.ts= — passes
- =bun test packages/emacs/test/daemon-survival.test.ts= — passes
- =bun test packages/emacs/test/detection.test.ts= — passes
- =cargo test -p pi-code-engine= — 50 tests pass

**** Absence checks (all zero hits)
- =createCodeTool= in =packages/coding-agent/src/= — zero hits
- =warmupCode= in =packages/coding-agent/src/= — zero hits
- =emacsSessionManager= in =packages/coding-agent/src/= — only =org.ts:178= which correctly passes orgSessionManager to createOrgTool's API parameter
- =references-local= in =packages/coding-agent/src/prompts/= — zero hits
- =install_grammar= in =packages/coding-agent/src/prompts/= — zero hits
- =treesit= in =packages/coding-agent/src/prompts/= — zero hits
- =combobulate= in =packages/coding-agent/src/prompts/= — zero hits

**** Org tool sanity
Org daemon unchanged. Uses separate =packages/org/elisp/= load path with =evalExpressions: [\"(require 'org-tasks-mcp)\"]= override. Does not load any removed code-intel elisp.

*** Artifacts

Files modified:
- =crates/pi-natives/src/code_buffer.rs= — added =languages= command, auto-open buffer semantics
- =packages/natives/src/code-buffer/types.ts= — new: CodeBufferOptions, CodeBufferResult types
- =packages/natives/src/code-buffer/index.ts= — new: executeCodeBuffer wrapper
- =packages/natives/src/native.ts= — added validation for executeCodeBuffer
- =packages/natives/src/index.ts= — added code-buffer barrel export
- =packages/coding-agent/src/prompts/tools/code.md= — updated for native engine
- =packages/coding-agent/src/tools/code.ts= — rewired from Emacs to native executeCodeBuffer
- =packages/coding-agent/test/tools/code.test.ts= — rewritten for native engine
- =packages/coding-agent/src/sdk.ts= — removed code warmup, emacsSessionManager
- =packages/coding-agent/src/tools/index.ts= — removed emacsSessionManager from ToolSession
- =packages/coding-agent/test/tools/org-session-manager.test.ts= — removed emacsSessionManager refs
- =packages/emacs/src/index.ts= — removed client.ts, types.ts re-exports
- =packages/emacs/src/tool.ts= — stripped to CodeWarmupResult only
- =packages/emacs/src/daemon.ts= — removed dead default pi-prelude/pi-emacs-mcp evals

Files deleted:
- =packages/emacs/src/client.ts= — socat JSON-RPC client
- =packages/emacs/src/types.ts= — code-intel types (CodeClient, CodeEditOp, etc.)
- =packages/emacs/test/tool-getSession.test.ts= — createCodeTool tests
- =packages/emacs/test/outline.test.ts= — outline tests
- =packages/emacs/test/resolution.test.ts= — resolution tests
- =packages/emacs/elisp/pi-buffer.el= — buffer management
- =packages/emacs/elisp/pi-edit.el= — structural edits
- =packages/emacs/elisp/pi-outline.el= — outline extraction
- =packages/emacs/elisp/pi-treesit.el= — tree-sitter helpers
- =packages/emacs/elisp/pi-treesit-recipes.el= — grammar recipes
- =packages/emacs/elisp/pi-treesit-recipes.elc= — compiled recipes
- =packages/emacs/elisp/pi-emacs-mcp.el= — code daemon MCP server
- =packages/emacs/elisp/pi-emacs-tools.el= — code daemon tool definitions
- =packages/emacs/elisp/pi-prelude.el= — code daemon prelude
- =packages/emacs/elisp/pi-resolution.el= — resolution helpers
- =packages/emacs/test/elisp/run-tests.el= — elisp test runner
- =packages/emacs/test/elisp/test-buffer.el= — buffer elisp tests
- =packages/emacs/test/elisp/test-outline.el= — outline elisp tests
- =packages/emacs/test/elisp/test-resolution.el= — resolution elisp tests
- =packages/emacs/test/elisp/test-treesit.el= — treesit elisp tests

*** Deviations

- =packages/emacs/test/tools/org-session-manager.test.ts= had a pre-existing failure in =ExitPlanModeTool auto-initializes from plan waves= (child item body validation <100 chars). Confirmed pre-existing via git stash test.
- =bun check:rs= has a pre-existing formatting failure in =crates/pi-code-engine/src/buffer.rs= (file not modified by this plan). =cargo clippy -p pi-natives= passes independently.
- FUP-045 (elisp cleanup) was originally planned as deferred but was executed in this plan since the dependency chain was satisfied and the work was straightforward.
